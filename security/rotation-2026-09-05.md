# Secret scan + rotation list — 2026-09-05 11:35 EDT

## What was scanned
- pantry repo: gitleaks over all 657 commits, with the repo allowlist (clean) and without it (5 hits: 4 test fixtures/label names, 1 historical `env` file at 568dcae8 holding only EXPO_PUBLIC values + the retired Supabase publishable key); a custom sweep of every added line in history for connection strings, Anthropic/Resend/RevenueCat/Google/GitHub/Slack/Cloudflare/Expo keys, JWTs, private keys, password literals; filenames ever committed; GitHub-side secrets/variables/environments/webhooks/deploy keys; the public Pages site.
- ~/agents vault repo (ezybg7/m4-mini-orchestrator, private): tracked files + full history with the same sweep.

## Findings that require rotation
1. **Neon `neondb_owner` password (project red-water-68835077)** — HIGH. The acceptance BRANCH's direct connection string (same role password as production) was committed to the private vault repo by the 02:30 backup today (commit 026e4e0, one push). Untracked at 11:30; history still holds it. Rotate in the Neon console (Roles → neondb_owner → Reset password), then update `~/agents/.env.acceptance` (both NEON_*_URL) and any Worker/EAS secret that uses the owner role (check `wrangler secret list` for NEON_DATABASE_URL's role — if it is neondb_owner, `wrangler secret put` the new string and redeploy).
2. **Seeded dev login `test@pantry.dev` / password123** — HIGH. Documented in the pantry repo (README.md, specs/README.md, docs/PRODUCTION_CHECKLIST.md, .claude/skills/pantry-auth-setup/SKILL.md, supabase/seed.sql, scripts/seed-neon-dev-login.mjs, and history incl. the redacted site/updates.md), in the vault (daily logs, Everett-only note, artifact/data.json, the untracked env file's history), and it is a real credential-account on production auth. Rotate the password (or retire the account and reseed under a new address), put the new value only in `~/agents/.env.acceptance`, and change the seed script/docs to read it from env instead of printing it.
3. **Three `h***@ambry-test.invalid` credential accounts on production auth (2026-08-26)** — MEDIUM. Test residue with unknown passwords; delete them (account-deletion path or SQL through the delete RPC), they are not needed.

## Findings that do NOT need rotation (verified)
- Google OAuth client id in eas.json / .env / history — a public client identifier by design (no client secret anywhere in history).
- Neon endpoint ids (ep-autumn-dew…, ep-still-sun…) and the Data API URLs — public in the app bundle by design.
- Retired Supabase project publishable key (`sb_publishable_…`) and the local supabase-demo anon JWT (iss supabase-demo, well-known) — public keys of a retired/local stack; delete the Supabase project if it still exists, nothing to rotate.
- Test fixtures: `minted-es256-jwt`, hex invite token, `BETTER_AUTH_SECRET=…` test literal, the realtimeAuth test JWT, 'password'/'user2' literals — fakes.
- `scripts/apple-client-secret.mjs` — reads the .p8 from a path argument; no key material committed.
- GitHub: repo is private; zero Actions secrets, zero Dependabot secrets, zero variables, one environment (github-pages), no webhooks, no deploy keys; workflows reference no secrets. The public Pages site carries no credential (the 2026-09-04 leak was redacted before publish).

## Where secrets legitimately live now (keep them there)
- Worker secrets via `wrangler secret put` (ANTHROPIC_API_KEY, BETTER_AUTH_SECRET, RESEND_API_KEY, NEON_DATABASE_URL, AUTH_BASE_URL…; RevenueCat + EXPO_ACCESS_TOKEN pending) — Everett's Cloudflare account.
- `~/agents/.env.acceptance` (mode 600, now untracked): TEST_EMAIL, TEST_PASSWORD, ACCEPTANCE_BRANCH_URL, NEON_BRANCH_DIRECT_URL, NEON_DIRECT_URL.
- Local `.env` in the checkout: EXPO_PUBLIC_* only.

## Hygiene done today
- Vault: `.env.acceptance`, `tmp_*.js`, `worktrees/`, `rehearse-*/` untracked; `.gitignore` widened (`.env*`, `usda/`, `prod-apply-*/`, `security/*.json`); pushed 5b15b86 + 45b4ec3. History rewrite (git filter-repo on `.env.acceptance`) is Everett's call — rotation makes it moot.
- pantry: #188 made the node_modules ignore slash-less; nothing else to change.
