# Overnight run — 2026-09-12, kickoff 03:00 (Fable 5.1 resets)

Rule for tonight: agents run only from 03:00. Everything below the line is organized now; nothing is promoted before then.
Concurrency: daemon cap 6, planner max 2. Promotion = `promote.sh <KEY> <column>` (unassigns first so the router's
"same assignee → no-op" guard cannot swallow a re-kick).

## Wave 1 — 03:00, five tasks in parallel
| # | Issue | Column → actor | Why first | Done means |
|---|---|---|---|---|
| 1 | AMBR-2  | code → codex-implementer | gate is open (spec 61 on main); cheapest proof of Code→review→merge | #216 merged (ADR conflict cited), issue done |
| 2 | AMBR-5  | todo → claude-planner (FABLE) | the never-run half: PRD/spec → Astra review → PR; migration 0064 | spec PR merged, then Code → 0064 rehearsed+applied → #PR merged |
| 3 | AMBR-21 | todo → claude-planner (FABLE) | spec-only deliverable; planner max 2 so runs beside #2 | spec amendment PR merged |
| 4 | AMBR-23 | todo → squad research | first marketing run; no Fable | docs/research/competitors.md PR merged, autopilot decision noted |
| 5 | AMBR-30 | comment → research-lead | file docs/research/agent-orchestration.md from its own ruling | PR merged, issue done |

## Wave 2 — as wave 1 lands (orchestrator actions + board)
| Step | What | Gate |
|---|---|---|
| A | #219: rehearse 0066 on base copy (already done by agent) → APPLY 0066 to production → merge #219 → AMBR-12 done → **re-dump base** | assert string exact |
| B | AMBR-13 → code: renumber 0066/0067 → 0067/0068 (not on main, not in open PRs), re-rehearse, in_review → review squad → APPLY → merge #220 → done | assert strings exact |
| C | AMBR-5 spec PR → orchestrator reviews the Decisions section → merge → promote AMBR-5 to code → implementer (0064 + rehearsal) → review → APPLY 0064 → merge → done | |
| D | AMBR-21 spec PR → review → merge → done | spec-only |
| E | AMBR-23 / AMBR-30 research PRs → review → merge → done | |

## Wave 3 — only if waves 1–2 are done
| AMBR-33 | code → codex-implementer | test toolchain upgrade (jest 30 / RNTL 14 / eslint 10), supersedes #204 | PR merged, #204 closed |

## Everett-only — prepared, not faked
AMBR-15 (decision brief posted: apply all 19 / the 10 matches only / a subset), AMBR-16 (phone), AMBR-17 (ASC), AMBR-18 (billing), AMBR-22 (load testing, behind ASC), AMBR-24 (cloud token).
Done by the orchestrator before 03:00: AMBR-19 (#176 merged — main had dangling references to spec 56), AMBR-20 (#204 converted into AMBR-33), AMBR-25, AMBR-26.

## Orchestrator chores — done before 03:00
AMBR-26: retire ~/agents/queue + com.user.claude-worker; archive pipelines/runs/reviews/briefs → ~/agents/_archive/2026-09-12/.
AMBR-25: weekly Multica rebuild job (launchd, Sundays); watchman → `--watchman=false` documented in AGENTS.md via PR (also fixes AGENTS.md's stale "no network / no push" sandbox text the probe flagged).

## Setup risks to watch at 03:00
- Planner @mention → codex-spec-reviewer → @mention back: never exercised. If the reviewer's reply does not wake the planner, nudge with a comment.
- Two planner runs at once (AMBR-5, AMBR-21): both write `specs/README.md` rows → merge conflict on the second spec PR. Accept; resolve at merge.
- Every production apply re-stales `base` — re-dump after each (`~/agents/scripts/refresh-rehearsal-base.sh`).
