Spec 61 §History — one line to append to `specs/multica-integration.md` (`## History`, newest last),
staged for the orchestrator's next docs push. Nothing in this change was committed or pushed.

- 2026-09-12 13:45 — **Codex account usage limit: Claude-runtime stand-ins and a router outage mode, so a dead runtime no longer stops the pipeline.** At 13:28 every run on runtime `cf00cbc7` (Codex (m4-mini)) began failing within seconds with one comment — "You've hit your usage limit. Visit https://chatgpt.com/codex/settings/usage to purchase more credits or try again at Sep 19th, 2026 4:09 AM." — which is the **account's** credits, not a model's capacity, so the existing Astra→Sol fallback (keyed on "at capacity") could not help and was deliberately left unable to fire on it: both Codex models sit on the exhausted account, and flipping them would have hidden the real cause behind an hour of pointless probing. Built: **`claude-implementer` `8342261f-9b94-428e-8b24-73533914993b`** (Claude runtime `8ac9e2e1`, `claude-opus-5`, thinking `max`, 2 concurrent, codex-implementer's 17 skills verbatim; instructions are `codex-implementer.md` plus one stand-in line — that file names no runtime and no agent, so job, gates and the HANDOFF contract are byte-identical) and **`claude-spec-reviewer` `8d32d26f-1b25-43fa-bec3-5054b4fa298a`** (same runtime and model, thinking `high` to match both reviewer seats, codex-reviewer's 4 skills; only its self-identifying first line changed — the lens split it used to justify by *model* difference is restated as a split by **lens**, because every seat in the squad is Opus 5 while the stand-in holds it). Squad `review` `074e7a7d-832d-4fda-aca4-38c90794905d` now reads review-lead `acfbf147` (leader) · claude-reviewer `f37fdc49` (`claude-lenses-A-logic-security-B-integration-style-business`) · claude-spec-reviewer `8d32d26f` (`spec-conformance`, replacing codex-reviewer `be4d47df`); `review-lead.md` needed no edit — it dispatches from its roster and names no reviewer. `multica-router.py` gained outage mode (243 → 467 lines): state file `~/agents/logs/.multica-codex-outage.json` = `{until, reason, set_by}`, and while its `until` is in the future `Code` routes to `claude-implementer`, a card already handed to codex-implementer is re-routed once (its own `<KEY>:codex-outage` key in the retry memory, so it never spends the card's ordinary retry budget), and the model fallback is skipped; `--codex-outage-until <ISO>` and `--codex-back` drive it by hand, the router writes it itself on any `task_failed` whose newest comment carries "hit your usage limit" (with `until` parsed from the message, else +7 days), and it closes itself when `until` passes — logging *codex outage over — routing code back to codex-implementer* plus the reminder that the squad seat is a manual swap. `PIPELINE_ACTORS` now holds both implementers, or the "assigned elsewhere on purpose" guard would have skipped the card in both directions. **Evidence** — `--dry-run` clean before and after, 34 assertions on the state machine green in an isolated harness (one defect found and fixed in review: a corrupt state file was ignored while still sitting on disk looking authoritative), and the live LaunchAgent tick did the whole thing unattended: `13:38:58 codex outage mode ON until 2026-09-19T04:09:00-04:00 — code -> claude-implementer (codex usage limit hit on AMBR-38 (reset time read from the message); set by multica-router.py (auto-detected))` / `13:38:58 codex outage — AMBR-38 (code) is with codex-implementer, whose account is out of credits` / `13:38:59 assigned AMBR-38 (code) codex-implementer -> claude-implementer`, then `13:40:44 codex outage mode ON until 2026-09-19T04:15:00-04:00 — code -> claude-implementer (codex account usage limit; set by --codex-outage-until)`. AMBR-38 (round 3, PR #236) is in `code` assigned to `claude-implementer`, which went `working` at 13:38; AMBR-35 keeps its lead's ruling that round 1 ran without a spec-conformance lens. Open: neither stand-in carries `NEON_REHEARSAL_URL` (the Claude runtime needs no `GH_TOKEN` — `dependabot-maintainer` works GitHub with no custom env at all; `GH_TOKEN` is the Codex allowlist's requirement), so it must be set on `claude-implementer` before it is given a migration card. Undo: `--codex-back`, `multica squad member remove/add` to restore `be4d47df` to the `spec-conformance` seat, `multica agent archive` on each stand-in.

- 2026-09-12 13:50 — **the outage hand-back now waits for the stand-in's run instead of racing it.** Closing outage mode flips `Code` back to `codex-implementer` on that tick, and Multica starts a task on assignment without cancelling the one already running — so a card still being built by `claude-implementer` would have got a second implementer and a second push on one branch (the AMBR-30 shape). `multica-router.py` (467 → 538 lines) now asks, before every hand-back, whether the stand-in still has a run in flight: `hand_back_held()` reads `multica issue runs <KEY> --active --output json` for each `code` card assigned to `8342261f-9b94-428e-8b24-73533914993b` and counts `queued|dispatched|running|waiting_local_directory` (the statuses are re-checked in the router, so a CLI that ever answered with the full history could not make a finished run read as a live one; an unreadable or unparseable answer fails **closed** — holding one more tick costs a minute, handing back under a live run costs a branch). While one is in flight the state file is kept, the log says `codex outage over but <KEY> has a claude-implementer run in flight — holding the hand-back`, and later ticks re-check until the run ends; `--codex-back` prints the same line and exits 0 without deleting the file, and `--codex-back --force` clears it regardless for the human who has checked. No symmetric guard in the other direction: while the account is out of credits a Codex run dies in seconds. The probe costs nothing per tick — an `until` still in the future returns before the check, so it only runs at the real expiry or on `--codex-back`. **Evidence** — `--dry-run` clean before and after; the assertion harness now lives beside the script as `~/agents/scripts/multica-router.test.py` and is **67 assertions, all green** (every in-flight status, a finished run, both malformed answers, a card in another column, a card assigned elsewhere, expiry holding and then handing back, and `main()` driven through `--codex-back` held / `--force` / idle / no-file against a taught CLI that records assignments and refuses any call it was not taught, so a new mutation cannot slip through unasserted); and the guard held **live** on the real board while AMBR-38's stand-in run was running — `13:48:55 codex outage over but AMBR-38 has a claude-implementer run in flight — holding the hand-back` then `13:48:55 --codex-back: holding, the state file stays — re-run it when the run has finished, or pass --force to clear it anyway`, exit 0, with the state file and AMBR-38's assignee untouched. Re-run the harness before and after any later change to the router.
