# Board run — 2026-09-12, kickoff 03:05 EDT (Fable 5.1 credits reset 03:00)

Owner: orchestrator session cb1599dd (the /goal session). A second orchestrator session worked the
board 00:50–01:00 (merged #176, made AMBR-33, retired the queue, scheduled the weekly rebuild for
Sunday 04:30 — deliberately after this run). If you are that session: this file is the queue; append,
don't fork it.

## Ground rules for the night
- Nothing runs before 03:05. The kickoff is `~/agents/scripts/board-run-3am.sh` (cron), not a hand.
- Production DB applies stay Everett's. Rehearse, stage the pending block, stop.
- Spec PRs: after the Astra review and the planner's `## Decisions`, the orchestrator **merges them**
  so the loop can continue overnight — flagged in the morning summary so Everett can reverse the gate.
  Implementation PRs still wait for him.
- Fable is the scarce resource: one planner issue at a time. Codex/Opus work runs in parallel (cap 6).
- Every setup failure gets fixed in the agent files/scripts *and* written to spec 61 §History.

## Queue, in order
| # | Issue | Column at kickoff | Actor | Why this position | Gate after |
|---|---|---|---|---|---|
| 0 | PR #221 (AMBR-30 research doc) | merge | orchestrator | done work, docs only | close AMBR-30 |
| 1 | **AMBR-5** frozen category (spec 59) | Todo | claude-planner → codex-spec-reviewer | first real planner + Astra run; migration 0064 reserved | orchestrator merges spec PR → Code |
| 2 | **AMBR-33** jest 30 / RNTL 14 / eslint 10 | Code | codex-implementer | no spec needed (dependency migration); unblocks Dependabot #204 | review squad → Everett merges |
| 3 | **AMBR-2** categorize.ts count (#216) | Code | codex-implementer | spec 61 is on main now, so the gate that blocked it is open; tiny | review → Everett merges #216 |
| 4 | **AMBR-23** competitor research | assign `research` | research squad | independent of Fable; first marketing-loop run | PR to docs/research |
| 5 | **AMBR-21** CI triage boundary spec | Todo — **after** AMBR-5's spec PR is up | claude-planner | second Fable job; spec only, no build | orchestrator merges spec PR |
| 6 | AMBR-13 renumber | Code — **after** Everett merges #219 | codex-implementer | #219 and #220 both claim 0066; the second to merge renumbers | review → Everett |
| 7 | AMBR-5 implementation | Code — after its spec PR merges | codex-implementer | rehearses 0064 (renumber if taken), Frozen category end to end | review → Everett |
| 8 | AMBR-21 implementation | Code — after its spec PR merges | codex-implementer | re-enable ci-triage behind the boundary the spec names | review → Everett |

## Waiting on Everett (organised, not runnable)
AMBR-12 (#219) and AMBR-13 (#220) approved — merge + apply 0066/0067 · AMBR-15 USDA tranche 2 apply ·
AMBR-16 device passes · AMBR-17 App Store Connect · AMBR-18 Actions billing · AMBR-24 Multica Cloud token ·
AMBR-22 load testing (his go, behind AMBR-17) · PR #222 (the other session's AGENTS.md doc).

## What "done" means tonight
Every runnable item above has reached `in_review` assigned to Everett with a green, rehearsed PR — or is
`blocked` with a HANDOFF that says exactly why. The morning summary lists each with its PR and what he
must do.
