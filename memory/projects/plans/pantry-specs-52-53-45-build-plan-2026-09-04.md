---
type: plan
title: pantry specs 52+53+45 build plan (2026-09-04)
description: Cross-cutting mechanics (tripwires); Spec 52 — comments → migration 0055recipecomments.sql; Spec 53 — following → migration 0056userfollows.sql; +2 more
tags: [pantry, infra, database, acceptance]
timestamp: 2026-09-04T00:00:00Z
permalink: agents/projects/plans/pantry-specs-52-53-45-build-plan-2026-09-04
---

# Build plan — spec 52 (comments), spec 53 (following), spec 45 (catalog nutrition matching)
_Produced 2026-09-04 by a read-only planning agent on the mini. Specs 52/53 are NOT being implemented by the mini: Everett's "recipes feature spec" chat owns 47–53 and amended 52/53 on main the same night — verify against the current spec text before use. Spec 45 IS being built by the mini (its own branch; catalog domain, no recipe overlap)._

## Cross-cutting mechanics (tripwires)
- Migration + `db/asserts/NNNN_*.sql` + `specs/MIGRATIONS.md` registry row (⏳ while unapplied) + §Pending applies entry whose psql line sits in the **first** bash block in numeric order (`tests/migrations-registered.test.ts`) + `db/neon-grants.sql` tail block, all in one PR. Every `create table` needs `enable row level security` (`tests/rls-enabled.test.ts`).
- `src/lib/database.types.ts` is hand-maintained; every new `PantryRepository` method must be classified in `src/features/data/broadcast.test.ts` (SILENT/BROADCASTS/CONDITIONAL); `char_length` caps map through `src/lib/limits.ts` + `limits.test.ts` MIGRATIONS list; profile embeds FK-qualified, never `select('*')` on profiles (ADR recipes D3).
- Apply-before-merge is load-bearing whenever the new client names a new column/RPC.

## Spec 52 — comments → migration **0055_recipe_comments.sql**
`recipes.comments_enabled` + `profiles.comments_default` booleans (D7 exception recorded in D9); SECURITY DEFINER `comments_open(uuid)` (+ row-type overload for a PostgREST computed column — VERIFY on the Data API, fallback `rpc`); `recipe_comments` (body ≤500 not blank; indexes recipe/created_at desc + author/created_at desc); policies select/insert/delete via the gate (no UPDATE anywhere); DEFINER cap trigger 30/hour (invoker would forget rows hidden by a closed gate); `comment_reports` with the full lifecycle shape, insert-only, not-self, only for comments the reporter can see; grants: comments select/delete + INSERT (recipe_id, author_id, body) — `created_at` never client-writable; `comments_enabled` joins the 0050 column lists; `profiles` SELECT regrant must be the UNION with 0054's list. Asserts Q1–Q10 (behavioural truth table, cap, cascades, privilege matrix, prosecdef/search_path pins). Client: repo methods comments/postComment/deleteComment/reportComment/commentsDefault/setCommentsDefault; hooks in `comments.ts` (infinite query 20/page, optimistic post, `commentFailureMessage` mapping 23514/42501); `CommentsSection` under YOUR RATING (visible trash button, not swipe; report on others'); `RecipeForm` Comments switch (Public only) + global switch on Profile tab; `relativeTime.ts`; Maestro `52-comments.yaml` single-account golden path (own public recipe), leaves nothing behind. Depends on 51's profile route + 0054's `profiles_select using (true)` for stranger bylines (null-drop fallback "an Ambry cook").

## Spec 53 — following → migration **0056_user_follows.sql**
`user_follows (follower_id, followee_id) pk`, no-self CHECK, index followee/created_at desc; RLS select `true` (product decision, authenticated only), insert/delete own; INVOKER cap trigger 2000; INSERT column-scoped (created_at not forgeable). Asserts R1–R10 (R5 cap as a text pin). Client: `communityFeed` gains `authorIds` (`.in('owner_id', …)`, ceiling 200 ids → the `following_feed` RPC is the follow-up); follow/unfollow/isFollowing/counts/lists/myFolloweeIds; `FollowButton`, live stats on 51's profile screen, `followers.tsx`/`following.tsx` list screens, All/Following chips on Community with a guidance empty state and `enabled:false` when no followees; thin single-account Maestro `53-following-scope.yaml`. Hard dependency on 51's screen; lists degrade without 0054.

## Spec 45 — catalog nutrition matching → migration **0047_usda_foods.sql** (reserved number)
Server-only `usda_foods (fdc_id pk, description ≤300, data_type in (foundation, sr_legacy, survey_fndds), nutrition jsonb object, imported_at)`, RLS on, zero policies, zero client grants, service_role select. Seed `db/apply/usda-foods-seed.sql` (~3 MB, committed) generated offline by `scripts/usda-match/export-usda-foods.py` (reuse extract.py `slim()` + tranche2.py `panel()` rules; skip empty panels; `on conflict do update`; count verification tail). Worker: in `workers/src/routes/shelfLife.ts` a fail-open `fillUsdaNutrition(sql, catalogId)` after the `ai` insert and in the `user` promotion path — eligibility read (`nutrition is null and cardinality(barcodes)=0 and nutrition_source is distinct from 'off'`), `ilike any(tokens)` prefilter limit 400, pure `supabase/functions/_shared/usdaMatch.ts` (`prefilterTokens`, `toCandidates` = first two comma segments as name + full description as alias, `matchUsda` via `matchCatalog` with a kcal ±10% tie-abstention), guarded update stamping `'usda'`; 42P01 → one warn, route unchanged; `nutrition_filled` counter added to the 200 body. Asserts U1–U7. Tests: `usdaMatch.test.ts`, extend `shelfLife.test.ts` mock. Docs: spec §History, README row 45, MIGRATIONS 0047 row + pending block + seed data-apply entry, grants tail, catalog.md D4 note + new O "no live FDC lookups", SPEC.md line 41 ("Generic items carry no nutrition" is false since the backfill), site/updates.md, usda-match README. Everett: apply 0047 + seed (psql, direct endpoint); Worker deploy needs wrangler auth. Eval: run seed_foods.csv names through matchUsda offline and report matched/abstained/wrong vs the human review sheet.

## Order
45 now (independent) → 51 → 52 → 53 (rehearse 0054→0055→0056 on one branch, re-apply grants, all suites) ; 50's migration any time, dashboard on Everett's call; comments must not reach external users before 50's queue exists (PRD §6).
