---
title: '2026-09-04'
type: note
permalink: agents/daily-log/2026-09-04
---

# 2026-09-04

## Production-readiness program (Everett: "go through all findings… spec, implement, review, merge everything… every single feature")
- Merged: #138 (workers deps), #116 (actions/checkout 7), #143 (dependabot ignore react-native-* + typescript), #140 (acceptance flow fixes + testIDs), #144 (Lists rhythm; #141 auto-closed when #140's branch deleted — retarget dependents first next time), #145 (gitleaks allowlist + CI secret scan, pinned binary), #147 (ADR stack D13 crash reporting, needs review). Closed #137 by policy. #142 (spec 54 load testing, other session) stays Everett's.
- Three inventory sweeps (board/tasks/device-plan 162 items; audits/checklist/ADR 239; specs/code 127) saved in the session scratchpad and consolidated into a program note; a docs truth pass, a privacy-accuracy fix, a July-audit housekeeping fix, a simulator QA pass and two build-plan agents are running in worktrees/background.
- Verified from the mini: bundle carries only public endpoints; env files ignored; secret history clean (5 allowlisted non-secrets); H2/H3 already closed by 0007/0046.
- Everett-only list forming: install dev build 2925130a + device passes; Cloudflare token for the mini (wrangler unauthenticated); Sentry project; ASC record/API key; LLC → RevenueCat chain; production applies (USDA tranche 2; wave-2 migrations when built); decisions (spec 7 jump, 9 bucket, 30 multi-list, 40 copy, 44 spread, 50 dashboard timing, #100 wording, import fetch cap, DES-06/07/08/15/16, J3, phone sign-in, terms acceptance spec, D1/D13 needs-review); scratch accounts on prod auth for §10/[2-account] rows need his OK.

## Ambry (pantry) — docs truth pass, PR #149 (agent worktree session)

**Done.** Branch `docs/truth-pass-2026-09-04` → **PR #149** (open, both CI checks green: `checks` 13m47s, `gitleaks` 9s). 29 files, docs + comments only — no behavior, tests or migrations. Gates run in-worktree: typecheck, lint, jest 110/1715, tripwires 5/59.

**What it corrected** (all present-tense claims; every dated/historical note kept, corrections marked with strike-throughs + dates):

- `specs/README.md` (13 edits): rows 47/48/49 "pending apply" → applied to production 2026-08-30; row 46 "specced/go-no-go" → built + cut over 2026-08-22; row 17 "⛔ parked on Neon's provider enum" → **LIVE 2026-08-22**; row 16 APNs key → set 08-11; rows 15/§build-order/§blockers "run eas build" → the builds exist (`29e5a6c0`, `2925130a`), the **install** is what's open; PR #97 → closed 08-23; #19 decisions → all four decided; 0046 "pending" → applied 08-22; banner → 2026-09-04.
- 9 Maestro headers: "authored, not yet executed" → the four green runs (08-23 8/8, 08-26 8/8, 08-31 10/10, 09-03 10/10). **48-folders**' "REQUIRES 0051" → applied 08-30.
- Spec headers 47/48/49, `docs/adr/recipes.md` D6/D7/D8 → migrations applied 08-30.
- `specs/audit-2026-08-22.md`: auth go/no-go + TST-03 (suite never ran) closed; spec 12 built; `aud` enforcement recorded **partially verified** (accept proven live on prod + a child branch 2026-09-04; foreign-`aud` rejection NOT probed).
- `specs/app-store-release.md`: Pages flip ticked (08-13); bundle-grep ticked (2026-09-04, clean — only the Neon prod host, Worker origin, Google iOS client id; `localhost` hits are library internals).
- `site/tasks.md`: "make the preview build" → install `2925130a`; Universal Links live since 08-23; Neon apple re-check struck; Apple unparked (3 device checks left); PR #97 closed.
- `docs/PRODUCTION_CHECKLIST.md`: Expo Go QR box → dev client; Supabase box → Neon+CF pointer; StaleBanner ticked (#131); Maestro box ticked.
- Audits 07-23 D9 (spec 4 built 08-08), 07-25 H2 (migration 0007 + 0046 §10) and H3 (0046 §1) closed. I1 was already marked fixed.
- Smaller: community.md open question (spec 50, 08-29), auth.md Google live on iOS 08-22, monetization per-household entitlements, recipes-crud recipe-import shipped.
- 4 code comments: `recipes.tsx` placeholder cards → both built 07-27; `workers/src/index.ts` `/auth/*` DARK → set in prod since 08-22; `tranche2.py` PR #93 pending → answered 08-22 (ADR catalog O1 rev 2, permanently null); `sign-up.tsx` Google dark → eas.json carries the id in every profile since #119.

**Two gotchas worth keeping:**

1. **`jest.config.js` has `testPathIgnorePatterns: ['/node_modules/', '/.claude/']`**, so an agent worktree at `.claude/worktrees/<id>/` **self-ignores every suite** — `npm test` there says "No tests found" and exits 1. That is not a green gate. Workaround: `npx jest tests/ --testPathIgnorePatterns=/node_modules/`.
2. **Flag order matters and fails silently.** `npx jest --testPathIgnorePatterns=/node_modules/ tests/` puts `tests/` into the *ignore array* (space-separated array arg) and runs 105 suites with the tripwires **excluded** — looks green, proves nothing. Positional first (`npx jest tests/ --testPathIgnorePatterns=…`) is correct: 5 suites / 59 tests.

**Left alone deliberately (in the PR's "Left alone" section):** `14-recipe-ideas.yaml` is genuinely unexecuted (tagged `needs-mock-worker`, excluded from every green run) so it was NOT marked green; Expo Go references in `push-digest`/`hosted-supabase`/`screen-tests` (a verbatim Expo quote and a dated decision record — `eas-dev-builds.md:112`'s rule says prose waits, only operative files get fixed); README "What is NOT built" row 19's "what is left is 15"; the `aud` reject path.

**Correction to the brief:** it said the acceptance suite ran green three times; `docs/ACCEPTANCE_TESTS.md` records **four** (the 2026-08-26 8/8 post-hardening run). Used "four" throughout.

---

## PR #142 (spec 54, load testing) — rebased, review round applied, ready for Everett

**Commit `89ce971`** on `feat/load-testing-spec`, force-pushed with `--force-with-lease`. PR is **MERGEABLE**; docs only (`specs/load-testing.md`, `docs/adr/stack.md` D12, `docs/adr/README.md`, `specs/README.md`). Comment posted: <https://github.com/ezybg7/pantry/pull/142#issuecomment-5535261650>.

**Rebase:** the `docs/adr/stack.md` conflict was main gaining **D13** (Sentry) at the same insertion point between D11 and `## Deliberate omissions`. Resolved by keeping **both**, D12 first then D13 — D13's text already says "(D12 is claimed by the pending spec 54 PR.)".

**Two reviews, one decision:** advocate said merge-with-changes, critic said merge-with-changes-or-defer; the lead chose **merge as spec, build deferred**. Eleven amendments landed.

**The one that matters — the load branch was going to hold production credentials.** The 2026-09-03 draft truncated `neon_auth.jwks` on the forked branch. Wrong schema (the 2026-08-22 cutover moved identity to **`app_auth`**, migrations 0048/0049) *and* the wrong half of the problem: a fork copies `app_auth."user"`, `.account` (password hashes), `.session`, `.verification` and `.jwks`, and the load Worker mounts the **same Better Auth service** over that branch — so `pantry-api-load/auth/sign-in/email` would have verified real users' real passwords on an unmonitored host built to be hammered. Now all five are truncated `cascade` before seeding, every re-fork re-purges, `load/purge-auth.sql` is a repo file, and an acceptance box proves a production account is refused there.

**`cascade`'s reach is stated, not hidden:** `profiles.id` references `app_auth."user"(id) on delete cascade` (0049 §2 — the App Store 5.1.1(v) deletion path), so the purge also empties `profiles` and transitively every household/inventory/recipe row the fork carried. That is the intent. `catalog_items` survives (`created_by` is `on delete set null`) — the catalog is the one thing worth forking production for.

**Other amendments, briefly:** guard fails closed when `.env` is absent (it is gitignored — a fresh clone/worktree gave it nothing to compare and the draft called that a pass) and now also compares `LOAD_AUTH_URL` vs `EXPO_PUBLIC_AUTH_URL`; nightly → **weekly Sunday 04:00**; alerting **log-only for four runs**; a dated vault heartbeat with a **"no load run in 10 days"** check on the weekly maintenance job (because that job itself died silently 08-24 and 08-31); a **counted** request ceiling dropped to **2,000/run** until the Workers-plan answer; the unused `pantry-recipe-photos-load` R2 bucket dropped from `env.load`; **no standing Cloudflare API token** — the run now **FAILS** on a stale load Worker instead of warning (needs a `WORKER_BUILD` var + `GET /version`; the Worker exposes **no** version route today); scenario 4 labelled as quota-ledger/advisory-lock under mock, **not** AI latency; **scenario 3 only** for phase 1, build behind Everett's go and the App Store Connect record; D12 tagged `active (needs review …)` mirroring D13.

**Stale citation fixed:** `neon-branch-keepalive.mjs` was cited as a repo script. `scripts/` has never held it — it is **`~/agents/scripts/neon-branch-keepalive.mjs`**, a vault script on this mini. The spec now cites the keep-alive *practice* in `docs/ACCEPTANCE_TESTS.md` and names the vault script as the mini's implementation.

**Open for Everett (defaults picked, nothing blocked):** which Workers plan the account is on (now load-bearing — Free keeps 2,000/run, Paid returns 10,000); the threshold table (first defaults, four runs calibrate); whether `GET /version` on the production Worker is acceptable (the alternative needs the API token amendment 6 exists to avoid).

**Gates:** `npx jest tests/ --testPathIgnorePatterns=/node_modules/` → **5 suites / 59 tests pass**; `npm run lint` → clean, exit 0. Both jest gotchas already logged above were hit and confirmed real (worktree self-ignore, and flag order silently excluding the tripwires).

---

## PR #152 (audit-2026-07-25 §I housekeeping) — opened, CI green, awaiting Everett

Branch `chore/audit-housekeeping-2026-09-04` off `origin/main` @ `6138924`, 8 commits, tip `3e8e0fc`. <https://github.com/ezybg7/pantry/pull/152> — MERGEABLE, both checks pass. **Not merged** (task said open only).

**Six audit items worked; three of them were already closed and are reported rather than "fixed".**

- **I2 (Node pin) — real.** No `.nvmrc`/`engines` existed; CI installs 22. Added `.nvmrc` = 22, `engines: {"node": ">=22"}`, and hand-mirrored the same block into `package-lock.json`'s `packages[""]` so the lockfile is not re-hoisted by a stray `npm install`. **No `engine-strict`, no `.npmrc`** — deliberate, the mini runs Node 26 and `npm ci` + all three gates were re-run on it after the change.
- **I5 (dead assets) — real, 14 files / 446,089 B.** `logo-glow` (324 KB) dominates. All trace to scaffold commit `abd5b4b`, zero refs. **Kept two the audit listed:** `favicon.png` was regenerated by the Ambry icon commit `9ca8616` and `specs/app-store-release.md:41` records it as shipped; `icon-light.png` is the deliberate light-appearance master (`dc103f8`, `4ec1b89`). `assets/` 624 KB → 148 KB.
- **I6 (CATEGORY_ORDER) — real.** Now `as const satisfies readonly FoodCategory[]` + an exported `CATEGORY_ORDER_IS_EXHAUSTIVE` typed `[Exclude<FoodCategory, …>] extends [never] ? true : false`. **Probe-verified both directions and reverted:** a fake enum member fails typecheck at `grouping.ts(39,14)`; drifting the runtime lists fails 2 of the 3 new tests (the third is types-only by design).
- **I4 (count-era residue) — real, all three.** `receipt.test.ts` cited the deleted `quantity.ts`; rewritten around the rationale that survived (the "N x" shape is what the duplicate-line merge instruction writes). `mock.ts` `'* 2'` → `'2 x'`. `mock.test.ts` now imports `FOOD_CATEGORIES`. **`specs/item-quantity.md` left alone on purpose** — marked superseded/"kept for history".
- **I7 — half real.** 3 of 5 `fontSize: 16` remained (Input, ReceiptCapture, index) — all == `fontSize.rowTitle`. `ItemRow.showChevron` was genuinely dead, removed. **Already fixed:** ReviewList is gone and its successor `CaptureEditor.tsx:118` has `accessibilityLabel="Item name"`; `shortDate()` has had tests since (`expiry.test.ts:108-147`).
- **I9 — recorded, not edited.** Hard rule honored: both nits live in APPLIED migrations, so corrections went into `specs/MIGRATIONS.md` rows 88/89. 0016's comment calls both quantity CHECKs column-level — only `_nonnegative` is (0011 inline); `_max` was `alter table add constraint` (0014), table-level. Effect unchanged. 0017's missing "no grants" comment is a non-issue: it creates an INDEX, not a grantable object. **`supabase/snippets/` is no longer empty/untracked** — it holds tracked `catalog-review.sql`.

**Verify-only items — one was actually wrong.**

- **Camera permission string WAS wrong; fixed in `app.json`.** Two plugins both write `NSCameraUsageDescription` and disagreed (image-picker: receipts+recipes; camera: barcodes). `@expo/config-plugins` `applyPermissions` does `permissions[k] || infoPlist[k] || default`, so an explicit plugin string always wins — **setting `ios.infoPlist` by hand would NOT have won.** `npx expo config --type introspect` resolved to the image-picker string, i.e. the shipped build asked for the camera without mentioning the barcode scanner. Both plugins now pass the same complete sentence, so it is order-independent. Re-introspected; `NSMicrophoneUsageDescription` still correctly absent.
- **Public recipe photo constraint exists and is STRONGER than the checklist says.** `docs/PRODUCTION_CHECKLIST.md:245` credits migration 0026 — but `0033_photo_object_path.sql:29` DROPS `recipes_photo_url_shape` and replaces it with `recipes_photo_path_shape` (`0033:35-38`), a bare owner-prefixed object path with no host expressible at all. Property holds; only the migration number is stale. **Not edited — that file is owned by another agent.** One-word fix (`0026` → `0033`) for whoever owns it next.

**Gate gotcha, now fixed in-repo (8th commit).** `jest.config.js` had `testPathIgnorePatterns: [..., '/.claude/']`, matched against the ABSOLUTE test path — so `npm test` from inside a worktree ignored all 110 of its own suites and exited 1 with "No tests found". Anchored to `<rootDir>/.claude/`; verified via `npx jest --showConfig` that a main-checkout run still expands to `<repo>/.claude/` and keeps skipping nested worktree copies. Plain `npm test` now gives **110 suites / 1720 tests** here. This is the same worktree self-ignore gotcha logged earlier today under PR #142 — it is now fixed rather than worked around.

**Gates:** typecheck, lint, `npm test` (110/1720) and `npm --prefix workers run typecheck` all green locally; GitHub `checks` + `gitleaks` both pass on the branch.

**Not touched (other agents own them):** `specs/README.md`, `docs/PRODUCTION_CHECKLIST.md`, `docs/privacy.md`, `site/*`, `workers/src/routes/*`, `src/app/(tabs)/recipes.tsx`, `src/app/(auth)/sign-up.tsx`, `.maestro/*`. Also left `specs/audit-2026-07-25.md` itself unmarked — the ✅ annotations on I2/I4/I5/I6/I7/I9 are for whoever owns the audit doc, to avoid colliding with this PR's review.

## pantry — orchestrator session, feature-review wave 1 (later, 2026-09-04)
- Reviews landed: advocate+critic for A auth, C capture, D inventory (queues in the session scratchpad; consolidated audit doc in progress → specs/audit-2026-09-04.md).
- Fix packages running in worktrees (each opens a PR): fix/auth-hardening (rate limit, secret guard, reset-screen exit, AASA reset URL, forgot-password truth, sign-up enumeration via autoSignIn:false, nonce refusal, pins), fix/inventory-integrity (default onError, location-delete ledger + move, rename via name_override, undo bulk move + partial-failure, tap targets, dates, pull-to-refresh, leftovers gating, expired-vs-used, limits, zone delete confirm, scorecard Maestro flow), fix/capture-moment (in-flight parse survives back-out/hide, photo mode, confidence, purchase_date base, manual barcode, daily cap display, privacy line, retake, a11y announce, user copy, 75s client budget), fix/worker-capture-hardening (refund on any post-claim failure, prompt data block, structural name check + create_catalog_item for AI rows, seed rows never filled, 45s vision budget, raw_text clamp, match hoist, 0030 asserts, ADR fact fix), fix/db-integrity (migration: spice merge category, p_items cap, zone scope trigger; sweep asserts on a Neon branch if a token can be minted), fix/deletion-hardening (transaction + idempotent retry + household lock; 11-account-deletion flow).
- Batch #149/#150/#152/#153: advocate in (merge order: #150 first, then #149 keeping the key-id redaction in site/tasks.md; #152; #153); critic pending; follow-ups agent applying G1/H1/H2/J1/J2/J4/F1/F2/F4/F5 on the branches.
- #142 (spec 54) amended and mergeable — spec approval stays Everett's. #154 (Dependabot routine) merges after the batch; #155 (majors) skipped-with-comment per policy.
- Next areas launched: B households/invites and E grocery (advocate+critic each). F platform, G monetization, H release/ops follow.
- Everett-only (accumulating): approve #142 + its three defaults (Workers plan, thresholds, GET /version); DECISION email verification at sign-up (fixes enumeration + social linking); DECISION barcode_fetch own daily counter vs monetization D1; DECISION PDF page cap; DECISION shelf-life route day semantics (household-scoped, C12 follow-up); `wrangler deploy` after the Worker PRs; psql apply of the integrity migration after rehearsal; FDC dataset download approval for spec 45 seed.
