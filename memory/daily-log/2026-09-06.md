---
type: log
title: '2026-09-06'
description: 'Nightly reflection; Morning: password rotation fallout, simulator; Spice catalog extension — PR #199 (feat/catalog-spices-2026-09-06); +7 more'
tags: [pantry, skills, hermes, infra, database, release, acceptance, memory]
timestamp: 2026-09-06T00:00:00Z
permalink: agents/daily-log/2026-09-06
---

# 2026-09-06

## Nightly reflection
- SKILLS: branch `nightly-2026-09-06` pushed off `nightly-2026-09-03` (chain verified via `merge-base --is-ancestor`, **not merged**, now **88 commits** ahead of `main`), **2 commits** — and this was the **first non-idle nightly since the 08-19 jetson session**, because the 09-04 and 09-05 nightlies both **FAILED silently on a Claude session-limit 429** (`is_error`+`api_error_status:429` "You've hit your session limit", 0 turns/tokens, 778-byte JSON) during the heavy pantry production-readiness PR storm. Recorded that failure mode + its two knock-ons (branch off *live HEAD* not `<yesterday>`; Task-2 must archive **all** logs past the line, not one) in `nightly-maintenance`.
- Gateway finding (the big one): the gateway **RESTARTED twice** (`gateway.start` 30→32: pid 573 @ 09-03 21:13 UTC, pid 667 @ 09-04 21:27 UTC; current **pid 667**) — the first restart since pid 725 came up 08-20 — which finally **RESOLVES the 14-night open watch: MCP-parking did NOT return** (park count frozen 1179, last park still 08-20 13:32:11; `mcp__codegraph__*`/`mcp__basic-memory__*` callable under pid 667 this session). So the empty-park-stream healthy state survives a real restart, not just long uptime. Also the **curator's 7th run fired 09-04 19:18 UTC** (`run_count` 6→7, reverted to `auto: no changes` — the 08-28 `70 marked stale` was a one-time 07-24-seed-cohort batch, not a drip; next ~09-11). Both landed in `hermes-local-gateway-ops`; keepalives + errors.log dates rolled to 09-06.
- MEMORY: **3-log catch-up** (09-04/09-05 429'd, so three crossed *older than 7 days* at once) — `2026-08-27/-28/-29.md` `git mv`'d to `daily-log/archive/`; `2026-08-30` (exactly 7 days) correctly stays. All three were pure `## Nightly reflection` infra notes, verified no-op folds: their durable facts (WS-503 signature, curator `auto:70`, MCP-parking arc) are already in `hermes-local-gateway-ops`; their pantry carry-overs are all in `pantry.md`'s *Retired carry-overs* section (do-not-resurrect). **No provenance lines added** — the 09-03 `pantry.md` ground-truth reset retired that ledger; the archive dir is the record now. Left staged for the 03:00 backup; did not commit memory myself.
- Reflection: this note (created fresh with basic-memory frontmatter). Also confirmed Everett's 09-05 **model-roles rule** (Fable plans/orchestrates, Opus 5 codes; Fable makes no source edits) is durably saved as memory `feedback-model-roles`.
- Standing carry-overs (not mine to merge/PR): the chained `nightly-2026-*` skill branches await Everett's review/merge (88 ahead of `main`). Pantry has a large Everett-only plate from the 09-04/09-05 push (per `pantry.md` §"Everett's plate" / §"State 2026-09-04" / §"Open for Everett": Neon CLI sign-in + disable Neon Auth → 0055, Worker deploy, EAS build + device passes, Sentry/RevenueCat/ASC, and the spec 23/57 coding waves). No PR produced — the SKILLS branch is intentionally not PR'd.
</content>
</invoke>
# 2026-09-06

## Morning: password rotation fallout, simulator
- Everett rotated `neondb_owner` in the Neon console overnight; the Worker's `NEON_DATABASE_URL` still held the old password → JWKS and sign-in answered 500 (found 08:2x EDT while probing). Everett pasted the new direct string into `~/agents/.env.acceptance` (08:34); I derived the pooled string (`-pooler` host, `channel_binding` dropped), verified both with psql, pushed the secret with the mini's wrangler (it IS logged in with Everett's OAuth token — the old "not logged in" note was wrong), JWKS 200 + test sign-in 200 at 08:36. `NEON_BRANCH_DIRECT_URL` still points at the expired acceptance branch — rewrite it when the next branch is cut.
- Simulator: erased + booted iPhone 17 Pro (70893D75…), `npx expo prebuild --platform ios` then a Release xcodebuild of main with the production `.env` baked (needs `SENTRY_DISABLE_AUTO_UPLOAD=true`, otherwise sentry-cli fails the build for want of an org); installed and launched, Welcome screen up. Build log: session scratchpad `sim-build.log`.
- Neon console reachable from the in-app Browser pane (logged in); Cloudflare plugin installed (`claude plugin install cloudflare@cloudflare`); Neon skills installed in-repo (untracked).
- Release pass republished with the rotation marked done.

## Spice catalog extension — PR #199 (feat/catalog-spices-2026-09-06)

Built specs/catalog-seed.md §Spice extension (amendment 2026-09-06). Seed data + docs + tests only; no client code.

- `spices` 23 → **140** rows (117 new); whole seed catalog 421 → **538**.
- 48 USDA SR Legacy panels (47 new rows + `Salt`), read by script from `~/agents/usda/usda-foods-seed.sql`; 92 new rows null by design (ADR catalog O1 gained a line saying so). 25 panels flagged `[same material]` = same spice, other form.
- `db/apply/catalog-spices-2026-09.sql` — **⏳ NOT rehearsed, NOT applied.** Registered in specs/MIGRATIONS.md §Pending applies; psql line at the foot of the FIRST bash block (a data apply has no number, so it goes last — the tripwire only checks that `migrations/NNNN_` args ascend). Apply-before-merge.
- Validated without a DB: pglast 7.18 / libpg_query PG 17.7 parse (67 statements), jest pins the apply file byte-equal to seed.sql, generated block byte-identical after `build-catalog-seed.mjs`.
- `tests/catalog-seed-spices.test.ts` — 147 cases incl. 30 type-ahead probes through the real `matchCatalog`.
- Gates green; CI on #199 green (checks/disclosures/gitleaks). NOT merged.

Lessons worth keeping:
- `scripts/build-catalog-seed.mjs` prunes any GENERATED alias equal to a hand row's canonical name — adding hand rows can silently rewrite the generated block. Always diff the block against HEAD after a hand-block edit.
- `matchCatalog`'s `normalizeTokens` strips every non-[a-z0-9] char, so accented canonical names (`Árbol`, `Sazón`, `Tajín`) tokenise to junk. Keep the ASCII spelling as the name, the accented one as an alias.
- Generated-block rows can only gain aliases in a data apply; the seed's generated block is rewritten from `scripts/data/catalog-rows.json` on every builder run.

## Catalog extension wave (Everett: spices → "also the thin categories" → "what else is missing" + frozen category)
- Spec 13 amendment 1 (spices) written by Fable on `feat/catalog-spices-2026-09-06`; Opus agent built **PR #199**: spices 23 → 140 (117 new, 48 USDA panels, 92 null by design), seed 421 → 538, apply file `db/apply/catalog-spices-2026-09.sql` (⏳ not rehearsed), 198 suites / 3671 tests green. Deviations recorded in the PR (Ground ginger; accent-free canonical names; crushed chiles at 913; buckets get no aliases; 3 items stay in other categories; two rows at 7 aliases; 25 "[same material]" panels).
- Amendment 2 (plant milks, frozen veg/fruit, drinks, fish/seafood) written and committed (a80efa2); second Opus agent extending PR #199 (rename apply → `catalog-extension-2026-09.sql`, test → `catalog-seed-extension.test.ts`).
- Broad gap probe on production (267 items, 12 families): ~48% found; bakery 4/17, international 5/21, grains 8/22, breakfast 3/8, snacks 9/23, condiments 12/30, canned 8/19, baking 11/20, dairy 13/22, meat 12/20, deli 7/11, produce 36/50. Proposed as a third extension pass (~150 rows) — awaiting Everett's go.
- Everett: "frozen vs fridge is a thing" → expiry already keys on the location's storage kind (`expiry.ts:100 shelfLife[kind]`); the missing piece is a `frozen` category → **spec 59** written (`specs/frozen-category.md`, board row 59), **PR #200** (spec only; migration 0064 reserved; supersedes spec 29's "12 categories hold"); builds after #199 lands.
- Rehearsal plan for the data applies: create a Neon branch from production in the console (Browser pane is signed in); the branch inherits the rotated password, so `NEON_BRANCH_DIRECT_URL` = production string with the branch endpoint host swapped in — no new secret needed.

## PR #199 amendment 2 — the thin-category extension (Opus agent, 2026-09-06 evening)

Extended the open PR #199 (`feat/catalog-spices-2026-09-06`) with spec 13's second
amendment. Commit `fba1140`, pushed, CI green (checks 8m10s, disclosures, gitleaks).
NOT merged — apply-before-merge still holds.

- **80 new catalog rows** (floor was 70): 8 plant milks/creamers (`dairy_eggs`, all
  `{}`), 21 frozen veg+fruit (`produce`), 13 fish/shellfish (`meat_seafood`), 4 canned
  fish (`canned`), 34 drinks (`beverages`). Seed 538 → 618. 43 USDA panels, 37 null.
- Files renamed before any apply: `db/apply/catalog-spices-2026-09.sql` →
  `catalog-extension-2026-09.sql`, `tests/catalog-seed-spices.test.ts` →
  `catalog-seed-extension.test.ts`. One transaction, one trailing count block covering
  both amendments (now 3 selects). Registry entry in specs/MIGRATIONS.md updated,
  still ⏳ NOT rehearsed / NOT applied. Expected on production: 197 inserts,
  catalog 431 → 628, spices 140, thin 80/43/37.
- **Key mechanism learned:** `scripts/build-catalog-seed.mjs` prunes any GENERATED
  alias that equals a HAND row's canonical name. So naming a hand row `Cod`, `Clams`,
  `Tater tots` etc. silently rewrites the generated block. Kept the block byte-identical
  by NOT adding rows for cod/haddock/halibut/clams/mussels/oysters/tater tots — the
  existing `White fish`, `Live shellfish` and `French fries` rows already answer those
  phrases by alias, which is the spec's own "alias only if X already answers" idiom.
  **This is the one decision for Everett**; the PR body says flipping it costs exactly
  six pruned aliases. `supabase/snippets/catalog-review.sql` query 7 is the durable list.
- Generated-row aliases (Lox→smoked salmon, Squid→calamari, Scallops→sea scallops,
  Breaded fish→fish sticks, Fish (frozen)→fish fillets, 4 plant milks, red/white wine)
  can only live in the apply file. The test now builds a second `appliedCatalog` =
  seed + apply-file alias updates and probes against it, asserting each probe FAILS
  against the seed alone. Consequence recorded: a from-zero hosted seed lacks them.
- Accents follow the `Arbol chiles` precedent: canonical `Acai packs` / `Rose wine`,
  accented spellings as aliases (normalizeTokens strips non-ASCII).
- Gates: typecheck, lint, jest 198 suites / 3831 tests, five-zone posttest, pglast
  parse (apply file 128 statements clean), builder idempotent + generated block
  byte-identical vs HEAD.
- Worktree `~/agents/worktrees/thin` removed.
- Rehearsal branch **rehearse-2026-09-06** (`ep-bold-voice-a6u2s6nc`, from production, auto-deletes 2026-09-13) created via the console in the Browser pane; env file's `NEON_BRANCH_DIRECT_URL` / `ACCEPTANCE_BRANCH_URL` rewritten to it (password inherited from the rotated production role — no new secret). Verified: catalog 431 / spices 23 / usda 13,587 on the branch. #200 (spec 59) merged.
- **Worker deployed** from main 9f00914 (version 974f0f7c; previous code deploy was 2026-08-24): JWKS 200, sign-in 200. **Readiness check by behaviour on production** (`~/agents/scripts/verify-nutrition-fill.mjs Quince`, as the test account): estimate-shelf-life 200 `{estimated:1, model_calls:1, nutrition_filled:1}` in 4.4 s; catalog row Quince created (`ai`, verified, shelf life per kind, `nutrition_source=usda` from "Quinces, raw"); an `ai_calls` shelf_life row landed; the test inventory row deleted. RECEIPT_PROVIDER is `claude` on the deployed Worker.
- PR #199 after agent 2: 197 new rows (seed 618), 91 panels; third agent (broad extension, 12 families, ≥150 rows) running on the same branch.

## PR #199 amendment 3 — the twelve thin families (agent, 2026-09-06)

Extended `feat/catalog-spices-2026-09-06` with spec 13's third amendment plus the
orchestrator's pruned-seven decision. Commit `b5fb23e`, pushed, CI green
(checks / disclosures / gitleaks). PR #199 retitled and its body extended with an
"Amendment 3" section; NOT merged (apply-before-merge still holds).

- Seed 618 → **811** rows (481 hand + 330 generated). New this commit: **193**
  (186 broad + the 7 promoted thin rows: Cod, Haddock, Halibut, Clams, Mussels,
  Oysters, Tater tots).
- Panels: broad **126/186**; thin half 43 → **49**; apply file now 390 inserts,
  38 alias updates, 223 panel statements, 4 count checks, 270 statements clean
  under pglast 7.18.
- The builder prunes **16** generated aliases (White fish cod/haddock/halibut,
  Live shellfish + Shucked shellfish clams/mussels/oysters, French fries tater
  tots, Mango Papaya, Cherries (sweet) lychee, Stone fruit nectarines, Dried
  fruit raisins, Beef roast brisket, Smoked sausage kielbasa). The generated
  block differs from HEAD by those and nothing else; pinned as PRUNED_ALIASES.
- **Rehearsal note for the orchestrator**: the apply file never removes an alias,
  so production's `White fish` keeps `cod` after the apply while `Cod` exists
  beside it. matchCatalog breaks the tie name-over-alias, so the new row wins.
  catalog-review.sql queries 7 and 8 list every such pair.
- Expected counts at rehearsal: category total 821 · spices 48/92/140 ·
  thin 87/49/38 · broad 186/126/60.
- Working data (roster, FDC picks, generators) kept under `~/agents/runs/tmp-broad/`.
- Gates: typecheck, lint, jest 198 suites / 4168 tests all green.

## PR #199 advocate review (catalog extension: 390 rows) — advocate agent

- **Output**: `~/agents/reviews/pr199-advocate.md` — 16 FINDs + coverage list + "checked, fine".
- **Method**: parsed both seed blocks pre/post (421→811), compiled `match.ts` and ran ~600
  shopper queries through the real `matchCatalog` against seed-only AND post-apply catalogs
  (apply-file alias updates parsed back out of the SQL); extracted all 223 nutrition
  statements; spot-checked ~30 of 168 nulls against `~/agents/usda/usda-foods-seed.sql`.
  Suite green (640 tests).
- **Two blockers before the data apply lands** (the apply file never removes an alias, and
  ADR O1 makes the reasoning permanent):
  1. **FIND-101** — 8 brand aliases in the spice half (`goya adobo`, `sazon goya`,
     `diamond crystal`, `mortons kosher salt`, `lawrys seasoned salt`, `maldon`,
     `accent seasoning`, `aji no moto`). The Q9 brand test iterates `BROAD_ROWS` only —
     186 of 390 rows. The PR asserts "No brand names anywhere"; false.
  2. **FIND-102** — 14 nutrition nulls the FDC mirror contradicts. `Tequila` (2710705,
     231 kcal — identical to the entry the other 4 spirits took), `Kombucha` (2710509),
     `Sparkling wine` (2710687), `Hard seltzer` (2710622), `Dragon fruit` (2709234),
     `Goat cheese` (2705716), `Sports drink, NFS` (2710771), `Catfish` (2684445,
     Foundation farm-raised — same shape as Tilapia, opposite call), `Trout` (173717),
     `Frozen mixed vegetables` (170471, "unprepared"), Frozen mango/cherries/pineapple,
     Sprinkles, Fruit snacks. PR prose says several are "foods FDC does not describe".
- **One-line fixes worth same-PR**: `dill`→Dill seeds (should be Dried dill);
  `butternut squash`→Frozen butternut squash (should be Winter squash, and the frozen row
  has no pantry key so a fresh squash gets NO expiry); the amendment-3 manual row uses
  **green onions** (a canonical name — resolves identically with and without the apply) where
  it must use **scallions**; `peppercorns`→ground Black pepper (1095) not Black peppercorns
  (1460), and ACCEPTANCE_TESTS.md:1714 enshrines it.
- **No regressions found**: 0 rows removed, 0 categories/shelf lives changed; of 858 replayed
  pre-PR phrases only 19 winners changed, 17 clear improvements. 119 recipe ingredient lines:
  13 gained a panel, 0 lost one. All 390 new names resolve to themselves; 793/796 aliases do.
- **Coverage list**: 52 still-missing weekly phrases across 10 families (top: pizza dough,
  biscuits, crescent rolls, mac and cheese, named pasta shapes, capers, bouillon, the three
  named canned soups, chia/flax/hemp seeds, frosting, cream of tartar, named beef cuts).
- Worktree `~/agents/worktrees/review199a` removed. Note: `review199c` (same commit) still
  exists — another reviewer's, left alone.

## PR #199 CRITIC review (catalog extension: spices + thin categories + twelve thin families) — 2026-09-06

**Output:** `~/agents/reviews/pr199-critic.md` (17 FIND items + a verified-clean list + a matching-diff appendix). Review ran in a throwaway worktree off `origin/feat/catalog-spices-2026-09-06` (`b5fb23e9`); both worktrees removed, no repo files left behind.

**What holds up (re-derived mechanically, not taken on trust):**
- 390 inserts byte-equal to `supabase/seed.sql`; 811 rows / 811 distinct `lower(name)`; hosted seed row-for-row identical.
- All 261 updates carry `where lower(name)=… and source='seed'`; 3 inserts carry `on conflict do nothing`; one transaction; idempotent by construction. Alias updates UNION, never replace.
- **All 223 USDA panels verified against `~/agents/usda/usda-foods-seed.sql`: 0 value mismatches, 0 description mismatches, 0 bad keys, 0 missing fdc_ids.** Nothing invented.
- Generated block differs from main by exactly the 16 documented pruned aliases.
- Matching: spec-13 probes 20/20 unchanged, receipt list 15/15 unchanged, grocery categorizer 19/20 (+1 improvement); full sweep of main's 881 surfaces = 19 changes, all bucket→specific improvements.
- Suite green (198 suites / 4168 tests) and NOT vacuous (mutation-tested).

**Top findings (blockers first):**
1. **Type-ahead regression** — `catalogSearch` is `ilike name … order by name limit 20`; 390 new rows overflow the cap so `frozen` (15→36 hits) loses Frozen pizza/shrimp/vegetables/waffles, `canned` loses Canned tuna/vegetables, `nut` loses Peanuts/Walnuts.
2. **Rehearsal expectation wrong** — the spice count check is scoped `category='spices'` but the expected 48/92/140 counts only new rows; 16 existing spice rows already carry usda panels from the applied tranche-1 backfill. Predict **63/77/140**. `Salt`'s panel statement is a no-op on production.
3. **4 of 7 `[manual]` acceptance rows can't pass** — the Add tab's search is name-only ILIKE, aliases never reach it (haldi/seltzer/potstickers return nothing; "green onions" proves nothing about section (h)).
4. `plant-based burger` confidently matches **`Plant-based creamer` (dairy_eggs)** against the seed alone; the test's `.not.toBe('Veggie burgers')` accepts it.
4b. **ADR O1's "null by design, not a queue" is unenforced** — spec 45's deployed `shelfLife.ts` filler would overwrite ~15 of a 113-row sample of the abstentions, incl. `Orange blossom water` → "Orange Blossom" 98 kcal (wrong food).
5. `curry` regresses Curry powder→**Curry leaves**, `chili` regresses Chili powder→**Canned chili** (alphabetical tie-break).
6. Brand names in 4 spice aliases (`goya adobo`, `sazon goya`, `diamond crystal`/`mortons kosher salt`, `maldon`); the Q9 brand test only covers the 186 broad rows.
7/8. Docs-truth: "92 new spice rows null" is 70 (ADR + spec + registry); ADR O1's thin paragraph still says 80/43/37 instead of 87/49/38; `catalog-review.sql` still says "Expect seed = 538"; "167 null" is 168.

**Cross-PR:** `origin/main` moved to `9f00914` mid-review — **spec 59 (`frozen` category) merged as #200**. #199 still merges cleanly (`git merge-tree`, no conflicts). But spec 59's re-file list predates the broad extension, so #199's eight frozen-sold `other` rows (Bao, Dumplings, Gyoza, Pierogi, Spring rolls, Falafel, Frozen breakfast sandwiches, Seitan) are not in it — the frozen re-file builder needs #199's Q10 list.

**Open for the orchestrator:** decide whether FIND-001/002/003 block the apply. FIND-002 must be fixed before rehearsal or the operator reads a false mismatch.
- **PR #199 review round 1** (critic `~/agents/reviews/pr199-critic.md`, advocate `pr199-advocate.md`): all 223 panels verified against the mirror (nothing invented), matching regressions minimal (19 changes, all bucket→specific). Lead decisions → fix agent (Opus): (1) the suggestion list (`useCatalogSearch`, used by Add/grocery/recipes) was a server `ilike name … order by name limit 20` — name-only, alphabetical, capped; now a pure `rankSuggestions` over the cached alias-aware index (prefix tiers → fuzzy), server search only as the first-launch fallback; index widened with shelf_life/image_url; manual rows rewritten to achievable probes. (2) Brand names out everywhere incl. rows (Tajin → Chili lime seasoning, Old Bay → Seafood seasoning), brand test over all new rows. (3) Fill the 14 nulls the mirror can fill; FIND-004b dropped (spec 45 filler is guarded `source in ('user','ai')`). (4) Confusables: fresh Dill and Butternut squash rows, `crab` alias, veggie-burger alias fix, curry/chili aliases (or renames), Black pepper loses `peppercorns` (recorded exception). (5) Counts/docs truth (spices 63/77 before fills; Salt panel no-op). (6) Feta 30; 13 aliases; a ~50-row mini-pass from the advocate's coverage list. Spec 59 builder must include the eight frozen-sold `other` rows from #199.

## Catalog extension: applied, merged, readiness (evening)
- **Fix agent was killed mid-run** by a process exit; 5 of 6 decision groups were committed in `~/agents/worktrees/fix199`, the 6th (counts/docs truth) sat uncommitted and complete — gates re-run, committed as c0ec888, pushed. Lesson: check the worktree before assuming a killed agent lost work.
- **Correction found by rehearsal:** the round-1 registry predicted spices 63 panels / 77 null on the assumption that 16 pre-existing rows carried usda panels. Production has **all 23** carrying them (the 16 from the 2026-08-08 backfill + the 7 FoodKeeper class buckets), so the category-scoped check is **70/70/140**. Registry + apply-file comment corrected (a70fdda).
- **PRODUCTION APPLY 2026-09-06 17:31 EDT**, `db/apply/catalog-extension-2026-09.sql` from head a70fdda: 32 s, one transaction, COMMIT. **431 → 884** (884 not 883 because the Quince row from the readiness check made it 432 before). Live: spices 70/70/140 · thin 87/60/27 · broad 186/129/57 · round1 62/37/25. Untouched: 2 profiles / 34 inventory / 7 grocery; RLS 25/25; anon 0; 0053 column scoping intact. No DDL → no grants replay, no PostgREST reload. Log: `~/agents/runs/prod-apply-2026-09-05/catalog-extension-apply.log`.
- **Merged #199** (e861b5f) after the registry flip to ✅; branch deleted; worktrees clean. Ledger appended.
- **Review round 1 headline:** the Add-tab/grocery/recipe suggestion list (`useCatalogSearch`) was a server `ilike name … order by name limit 20` — name-only, alphabetical, capped; 390 rows overflowed it and every alias was invisible there (the suite only probed `matchCatalog`). New `src/features/catalog/rankSuggestions.ts` ranks the cached alias-bearing index. **Verified against the LIVE production index after the apply**: frozen→Frozen corn/peas/fruit/mango/pizza, nut→Peanuts/Pine nuts, haldi→Turmeric, potstickers→Dumplings, seltzer→Sparkling water, peppercorns→Black peppercorns, curry→Curry powder, chili→Chili powder, dill→Dill.
- Client index is now 884 rows / **163 KB / 1.7 s** on the day-long staleTime cache (hooks.ts comment still says "~421 rows, a few tens of KB" — worth updating in the next catalog PR).
- Branded row `Dill Flavored Cashews — Emerald` is `source=off` with a barcode — legitimate; the brand-free rule is a seed rule.
- Gates on main e861b5f: 199 suites / 4323 tests + four-zone posttest; gitleaks clean over 408 commits.
- Acceptance: branch `rehearse-2026-09-06` brought up to production's catalog (883 vs 884), Release build baked with the BRANCH url (prod id hits 0), installed, flows started 17:46.
- **Acceptance run 9 (2026-09-06 17:46–17:59): 5/18 FAILED** — 01-auth, 07-profile, 09-grocery-list, 11-grocery-restock, 44-leftover-composition. **Not an app regression** (screenshots show the app rendering correctly in every failure). Two environmental causes:
  1. **iOS "Save Password?" system alert** on 01-auth — the Pantry screen is correct underneath; the alert owns the frontmost window so Maestro's hierarchy sees only status-bar items. Appeared because the simulator was `simctl erase`d fresh this morning.
  2. **Stale fixture state on the shared test account** — 07-profile reads "2 recipes · 0 followers · 0 following" (two leftover public recipes "Spinach and lemon pasta"/"Shakshuka" from earlier runs) against an assertion of "0 recipes"; 09-grocery-list's `notVisible: To Buy` fails because stale grocery rows exist; 11-grocery-restock expects "Restock 1 bought item" with more than one bought; 44-leftover similar. Every branch is cut from production, whose seeded account carries accumulated state.
  Artifacts: `/Users/orchestrator/.maestro/tests/2026-09-06_174646/`. **Lesson: absolute assertions about a shared account's totals are the anti-pattern** — the suite is the go/no-go instrument and must be hermetic. Opus agent on `feat/acceptance-hermetic-2026-09-06` making the flows self-cleaning + neutralising the system dialog; definition of done is 18/18 twice, the second with `--run-only`.

## Acceptance suite: hermetic flows + the iOS Save Password alert (PR #201)

**What.** Run 9 (2026-09-06 17:46 EDT, branch `rehearse-2026-09-06`) was 5/18 with
no app regression. Fixed both causes; suite now 18/18 twice in a row
(run 10 cold-start full cycle 12m30s, run 11 `--run-only` 12m28s on run 10's leftovers).
PR https://github.com/ezybg7/pantry/pull/201 — open, NOT merged, Everett reviews.

**Cause 1 — iOS "Save Password?" alert.** Erased simulator ⇒ first sign-in raises the
keychain save offer; it is another process's window, Maestro's iOS driver is app-scoped,
so the hierarchy dump is just the status bar and every post-sign-in assertion fails.
Fix (proven by erase/repro/erase/fix A-B):
`xcrun simctl spawn booted defaults write com.apple.WebUI AutoFillPasswords -bool false`
— **`com.apple.WebUI`, NOT `com.apple.Preferences`**; the key is named only in the
runtime's `System/Library/DefaultsConfigurations/com.apple.WebUI-cloud-users.defaults`.
Applied in `scripts/run-acceptance.mjs` (covers `--run-only` too) and in
`~/agents/scripts/acceptance-cycle.sh` right after boot. Resets on every `simctl erase`.

**Cause 2 — flows asserting the shared account's totals.** Production's `test@pantry.dev`
now owns real data and every branch forks production: 6 needed grocery rows + 1 checked
Bananas, 5 recipes (2 public: "Spinach and lemon pasta", "Shakshuka"), ~36 inventory rows
(~20 in the Fridge). NOT suite residue — 10-recipes-crud and 48-folders do delete their
recipes. Fixed: new shared include `.maestro/acceptance/_empty-grocery-list.yaml`
(09 + 11 empty the list with the app's own gestures; the tick loop is safe because
`useSetGroceryStatus.onMutate` is optimistic); 44 filters the shelf via "Search this
location" and now deletes its own row; 07 asserts the stats line by shape AND against the
list under it (two arms).

**Facts worth keeping.**
- `endingFor`: out ⇒ used, past date ⇒ expired, else ⇒ **discarded**, and the scorecard
  counts discarded in NEITHER direction (`usedPct = used/(used+expired)`, same for
  `SCORECARD_FLOOR`). That is why 44's delete cannot disturb 13-waste-scorecard.
- Grocery row a11y label is `"<name>, not bought"` / `"<name>, bought"`; suggested rows are
  `"Add <name> to your list, …"`. Location row is `"<name>, added <Mon D>, <urgency>"`.
- Maestro `repeat` takes `times` AND `while` together (`YamlRepeatCommand`), so a
  `while` loop can be capped.
- **Gotcha:** `acceptance-cycle.sh`'s "prod must be 0" guard silently degraded in a
  worktree — `.env` is gitignored so `PROD_ID` was empty and `grep -c ""` counted every
  bundle line (printed 14778) while the real signal `branch id hits: 0` scrolled past.
  Nothing was baked at all; `EXPO_PUBLIC_*` comes from `.env`, not the xcodebuild env.
  Script now refuses to build on an empty `PROD_ID`. **Copy `.env` into any worktree
  before building.**

**Open items.** 07's `0 recipes` empty arm is `[manual]` in practice on this account;
the stats count *moving* on publish is still not proven E2E. 12 still leaves a `needed`
Milk row by design; 06 still leaves the invite code rotated.
- **Acceptance fixed and merged (#201, 0e2b3dc):** runs 10 and 11 both **18/18** (12 m 30 s / 12 m 28 s), the second `--run-only` on the state run 10 left behind — the proof of hermeticity. Dialog fix is `xcrun simctl spawn booted defaults write com.apple.WebUI AutoFillPasswords -bool false` (domain is **com.apple.WebUI**, not com.apple.Preferences; proven A/B; a Maestro "Not Now" tap cannot work because Maestro cannot see the alert). New shared include `_empty-grocery-list.yaml`; 07-profile asserts the stats line by shape AND cross-checks it against the list (two arms); 11-restock keeps its absolute "Restock 1 bought item" by owning the list first; 44-leftover filters and deletes its own row (a `discarded` ending, which the scorecard counts in neither direction).
- **Correction to my run-9 note:** the two public recipes on the test account are **production's data, not suite residue** — 10-recipes-crud and 48-folders both clean up after themselves. The bug was the absolute assertion, not a leak.
- **Runner catch:** `acceptance-cycle.sh`'s "prod must be 0" bake guard silently degraded in a worktree (`.env` is gitignored → empty `PROD_ID` → `grep -c ""` counted every bundle line). It now refuses to build on an empty PROD_ID; a `.env` must be copied into any worktree before building. Script lives outside the repo (`~/agents/scripts/`), so it is not in any diff.