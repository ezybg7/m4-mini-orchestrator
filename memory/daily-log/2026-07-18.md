---
title: '2026-07-18'
type: note
permalink: agents/daily-log/2026-07-18
---

## Pantry capture specs (Claude session)
- Everett merged PR #3 (SDK 54 + device fixes). New global spec-first rule applied for the first time: wrote detailed specs BEFORE implementation for the two new capture paths → **PR #4** (feat/capture-specs): specs/barcode-capture.md (Add-tab hub with Search/Scan/Receipt segments, expo-camera scanner, barcode-lookup Edge Function backed by Open Food Facts with catalog write-back + 'off' source enum value, migration 0002 GIN index, multi-scan loop, debounce, unit-tested OFF mapper) and specs/receipt-capture.md (expo-image-picker → parse-receipt Edge Function → claude-sonnet-5 forced-tool-schema extraction → in-function catalog matching → review checklist UI → bulk insert; ai_calls table + 20/day/user cap in migration 0003; image never persisted; needs ANTHROPIC_API_KEY in supabase/functions/.env — Everett action).
- Awaiting Everett's spec sign-off (merge PR #4) before implementing. Open questions posed: API key availability, cap value, groceries-photo mode now vs fast-follow. Build order after sign-off: barcode first (no external key needed), receipt second.- Provider decision on Everett's cost question: receipt/photo AI switched from Anthropic to **Gemini Flash** (PR #4 updated + repo CLAUDE.md line): free tier ≈1,500 req/day with vision, no card; paid ≈ $0.005–0.01/receipt (Haiku-comparable). Function gets a provider adapter so the vendor is a config swap. Launch blocker recorded: free tier may train on data → paid tier (no training) before any external beta. Everett's action item is now a free AI Studio key (GEMINI_API_KEY in supabase/functions/.env) instead of a paid Anthropic key.
- **Process slip caught:** Everett merged PR #3 minutes before the contrast/test-account commit (78a373a) was pushed to its branch, so that commit never reached main — discovered when `git checkout main` reverted the working tree's fixes. Recovered via **PR #5** (same branch, one-commit delta). Working tree restored to feat/sdk-54 so the device loop still has the fixes. Lesson: before pushing "to a PR", verify the PR is still open (`gh pr view --json state`).
- Everett approved Gemini Flash and has an API key → once he stores it in supabase/functions/.env and merges PRs #4+#5, implementation starts (receipt-first, then barcode). Created supabase/functions/.env.example (untracked for now, ships with the implementation PR).
- **Full end-to-end spec suite** (Everett's directive before usage-window reset): **PR #6** (feat/roadmap-specs) — 19 specs in specs/ incl. master README with status board, mermaid dependency graph, Phase A/B/C build order, migration registry 0001–0010, cold-session resume checklist, and Everett-blocker list (supabase.com project; Apple Developer $99 + Expo account; app name/privacy-host/icon decisions; Gemini paid-tier before beta). New specs: ai-shelf-life, zones-ui, realtime-sync, recipes-crud, recipe-matching, community, recipe-ideas, household-management, account-deletion, offline-cache, catalog-seed, hosted-supabase, eas-dev-builds, push-digest, apple-sign-in, dark-mode, app-store-release; receipt+barcode specs amended with implementation plans (barcode migration renumbered 0002→0004).
- **Receipt capture backend implemented and secured** on feat/receipt-capture (08e77a2, 28/28 tests): migration 0003_ai_calls, parse-receipt Edge Function, gemini adapter (model MUST be gemini-flash-latest — pinned ids retire; verified live: schema-forced JSON works, key valid), plural-tolerant matcher with coverage gate. Client half is fully planned in receipt-capture.md §Implementation status. NOT yet a PR — branch pushed, continue there.
- Next session: merge PR #6, then build receipt client per the spec addendum, then straight down the Phase A order in specs/README.md.

## Nightly reflection
- Mined 07-17/18 logs: gateway is DOWN for agent replies — config.yaml `context_length: 40960` is below Hermes's hard 64K minimum, so every Discord message since ~17:54 on 07-17 fails at agent init (llama3.1:8b was pulled 02:04 as a 128K candidate, but config still points at qwen3-agent — needs Everett's switch + restart).
- New skill on branch `nightly-2026-07-18` (pushed, NOT merged): autonomous-ai-agents/hermes-local-gateway-ops — 64K-minimum incident, http-vs-https base_url, /v1 vs /api/v1 endpoint, config.yaml YAML-ordering silent-default hazard, ~2min cold-start latency, exit-75 restart contract, triage order.
- Same branch: github-workflow gained "verify the PR is still open (`gh pr view --json state`) before pushing to it", from the pantry PR #3→#5 slip.
- Memory maintenance: no daily-log files older than 7 days yet, so nothing was summarized into projects/ or archived this run.
- Open items: Everett to review/merge the skills branch; decide the qwen3→llama3.1 model switch; SSH PasswordAuthentication audit warning still outstanding.
- **Hermes Discord gateway fixed.** Root cause chain: OpenRouter credits ran out (was running claude-opus-4.6) → interim switch to local qwen3-agent (Ollama) → its 40,960 context is under Hermes's hard 64K minimum → every Discord message failed at agent init with the generic error. Fix: pulled llama3.1:8b (131K-native metadata), created `hermes-agent` model with num_ctx 65536, added OLLAMA_FLASH_ATTENTION=1 + OLLAMA_KV_CACHE_TYPE=q8_0 to com.user.ollama plist (resident 9.4 GB vs qwen3's 11 GB), config.yaml → hermes-agent/65536, gateway kickstarted. OpenAI-compat API smoke returns OK. Backups: config.yaml.bak.20260718_020413, com.user.ollama.plist.bak.20260718. Awaiting Everett's Discord ping as final verification. Non-fatal leftovers: openrouter/nous aux warnings, SSH password-auth audit nag, qwen3-agent still on disk (5.2 GB reclaimable).
- Hermes round 2: after the model swap, Discord failed with `"hermes-agent" does not support thinking` — Hermes emits reasoning_effort for the custom provider from config (`agent.reasoning_effort: medium`), qwen3 had the thinking capability but llama3.1 doesn't, and Hermes's capability probe only runs for ollama.com (Cloud), not localhost. Fix: `agent.reasoning_effort: none` in ~/.hermes/config.yaml (verified Ollama accepts "none" harmlessly even if forwarded), gateway kickstarted clean. Memory file hermes-gateway-ops updated implicitly via this log. Awaiting Everett's ping.
- Hermes final fix (Everett confirmed local-first: gateway = cheap async orchestrator for overnight Claude delegation): llama3.1 dead-ended (text-narrated tool calls, tool_turns=0, 132s/turn). Replaced with **qwen3-agent-128k** — unsloth Qwen3-8B-128K GGUF (131K honest metadata) + the original battle-tested qwen3-agent template + num_ctx 65536; config default/context_length/reasoning_effort=medium restored; structured tool_calls verified against /v1 with a tools array; model warm at 10 GB, keep-alive forever. Gateway restarted (PID 10563). Awaiting Everett's Discord smoke test. Overnight-use notes: model stays warm (no cold prefill per task); Mac must not sleep for the overnight queue (pmset/caffeinate — Everett action).
- Hermes round 4: qwen3-agent-128k made structured tool calls ✓ but invoked the `tool_call` deferred-tool dispatcher with malformed args (7 identical failures → guardrail halt, correctly). Root cause: no `tools:` key in config → tool_search auto-deferral at 10% of context (≈6.5K tokens) hid 30 of 56 tools behind search-then-dispatch, which the 8B fumbles. Fix: `tools.tool_search.enabled: off` in config.yaml → all tools directly visible (verified terminal-direct calling works via curl smoke earlier). Gateway restarted (PID 10738). NOT PATH (plist bash wrapper already exports ~/.local/bin + /opt/homebrew/bin — verified in process env). Awaiting Everett retest.
- Hermes round 5 (the capability round): Everett gave the gateway an open-ended "do start to finish" task. The 8B derailed — Hermes's skill-creation nudge (every 15 turns) hijacked it into authoring skill files at invented paths, it hallucinated successful writes (file-mutation verifier caught both), planned to code directly instead of delegating, and asked multiple-choice questions instead of executing. Fixes: (1) `skills.creation_nudge_interval: 0` (0 = disabled, same as Hermes's own background-review fork); (2) new **orchestrator personality** set as default (`display.personality`) that hard-scripts the delegate-to-claude procedure and forbids self-coding/skill-authoring; (3) claude-worker.sh now parses optional `MODEL:`/`EFFORT:` task headers (syntax + parse dry-run verified; backup kept) implementing Everett's standing rule — **fable max for thinking tasks, high for regular** (also saved to session memory + delegate-to-claude skill, committed and pushed); (4) removed stray `~/code/pantry/.hermes/` dir the derailed run created; shared skills repo verified clean. Gateway restarted (PID 11969).
- Honest posture recorded: the 8B is now scoped to relay+delegate only. If it still can't execute the narrow script reliably, the escalation options remain Gemini free tier or OpenRouter top-up (one config line each).
