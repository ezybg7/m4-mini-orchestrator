---
type: log
title: '2026-08-29'
description: Nightly reflection
tags: [pantry, skills, hermes, infra, memory]
timestamp: 2026-08-29T00:00:00Z
permalink: agents/daily-log/2026-08-29
---

# 2026-08-29

## Nightly reflection
- SKILLS: branch `nightly-2026-08-29` pushed off `nightly-2026-08-28` (chain verified via `merge-base --is-ancestor`, **not merged**; now **73 commits** ahead of `main`), **2 commits** (`hermes-local-gateway-ops` + `nightly-maintenance`). SKILLS-idle on the worker axis a **tenth** straight night (last real `done` still the 08-19 jetson session; only lock-exit noise + tonight's own 0-byte `reflect-2026-08-29`) — but **not a no-op**: the "imminent axis" the last six nightlies flagged finally moved.
- **The genuine finding — the curator FIRED.** The projected ~08-28 run landed **08-28 18:44:27** (`run_count` 5→6, 0.64s, 0 tokens, `llm: skipped`), and it jumped **`auto: 2 marked stale` → `auto: 70 marked stale`** (0 archived, 0 reactivated, 72 checked). The 2→70 is **not a mechanism change** — the 70 newly-stale are exactly the never-invoked **07-24 seed cohort** (all share `created_at` 07-24 + `last_used_at: null`), a same-age batch crossing the ~35-day staleness line together. Cadence held a 6th weekly point; next ~09-04. Recorded in `hermes-local-gateway-ops` §"Curator".
- **Watch-item did NOT trip + a model correction.** `.usage.json` now has only **2 `active`** skills: `hermes-local-gateway-ops` (last_used 08-19, `pinned:false`) and `delegate-to-claude`; `nightly-maintenance` + `claude-worker-env` are still absent from the sidecar (immune by absence), so no maintained skill went stale. But `delegate-to-claude` (last_used **07-20**, ~39d idle) being active while `claude-code`/`hermes-agent` (07-18/07-19, 1–2d older) are stale means the boundary is **not** a fixed `last_used`-age cutoff — left as an open anomaly, threshold NOT fabricated. Infra otherwise held: MCP-parking gone a 9th night, errors.log quiet again (08-27 WS-503 did not recur), pid 725 held ~8.5d no restart, keepalives through 02:58.
- MEMORY: **verified no-op fold.** `2026-08-21.md` (8 days, crosses *older than* 7) → `daily-log/archive/` via `git -C … mv`; `2026-08-22` (exactly 7) stays. Walked it section-by-section: 08-21's cycle-ended + DNS-blip-on-restart findings are in `hermes-local-gateway-ops` (superseded since); its MEMORY half was the 08-13 no-op fold (already at `pantry.md` line 36); carry-overs (PR #101, un-PR'd branches, PR #10, ANTHROPIC_API_KEY) all in Status; 08-21 opened no branch of its own. Provenance line added, `_Updated_` bumped to 08-29; left staged for the 03:00 backup, did not commit memory myself.
- Standing carry-overs (not my call to merge/PR): chained `nightly-2026-07-*`/`08-*` skill branches unmerged — Everett's review. Pantry: review **PR #101** (jetson infra spec) and PR #10 (#9 merged); open the never-filed `feat/nightly-pull-routine` + `feat/receipt-parsing` PRs (both `gh`-gated in the worker), run `bash scripts/create-tracking-issues.sh` for the 18 `[spec 2..19]` issues, provision ANTHROPIC_API_KEY for the production vision provider. New watch: `hermes-local-gateway-ops` is `pinned:false` and ages on *invocation* not the nightly's file-reads (last_used stuck 08-19), so `hermes curator pin` it if it ever enters the stale set.
