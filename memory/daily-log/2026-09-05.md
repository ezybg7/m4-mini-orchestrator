---
type: log
title: '2026-09-05'
description: 'Ambry (pantry) — 01:52 EDT: limit reset; everything relaunched (orchestrator session); pantry — PR #142 (spec 54, Artillery load testing) ADVOCATE review (read-only, orchestrator-run); pantry — PR #142 (spec 54 load testing) CRITIC review (read-only, orchestrator-delegated); +34 more'
tags: [pantry, skills, infra, database, release, acceptance, memory]
timestamp: 2026-09-05T00:00:00Z
permalink: agents/daily-log/2026-09-05
---

# 2026-09-05

## Ambry (pantry) — 01:52 EDT: limit reset; everything relaunched (orchestrator session)
- Rate limit hit at ~23:40 (reset 01:50). Research from the first batch SURVIVED: `~/agents/research/screens/{account,pantry,add-capture}.json` (17 screens, 51 directions, 94 app studies) + `lists-recipes.md` (7 screens, no JSON → conversion agent running).
- Running: flow repair (resumes from 4 edited flows), #182 fix (with Everett's six decisions), Worker route tests (resumes from 2 partial files), R4 JSON conversion, mock builders ×3 (account/pantry/add-capture → `~/agents/research/mocks/<group>/<screen-slug>/{today,<direction-slug>}.html` + manifest.json), #142 advocate + critic (Everett: "you review it").
- Generators ready: `~/agents/artifact/build.py` (release pass; Everett's answers render as decided) and `build-gallery.py` (Today + A/B/C mocks per screen, decision recorder via db).
- Queued: lists-recipes mocks (after the JSON), K7 email-verification auth-spec amendment, coverage wave of new Maestro flows (after the flow-repair PR), then publish both artifacts.
## pantry — PR #142 (spec 54, Artillery load testing) ADVOCATE review (read-only, orchestrator-run)
- Read from the existing worktree `.claude/worktrees/agent-a27265cab244e947c` (HEAD 89ce971 = origin/feat/load-testing-spec); created no worktree, made no edits.
- Verdict: approvable as a spec WITH FIXES; build stays deferred (as the PR already says). Not premature: the value now is the fork-purge rule and the no-production guard being on record.
- Blocking fixes found: (1) `load/purge-auth.sql` uses `TRUNCATE … CASCADE` from `app_auth."user"` — Postgres CASCADE truncates every FK-referencing table transitively and ignores ON DELETE SET NULL, so `catalog_items` (created_by → app_auth.user, 0049 §88) and the whole public schema are wiped, contradicting spec:165-171; use `DELETE FROM app_auth."user"` + `TRUNCATE households CASCADE` + count assertions, or build the branch from `db/` + catalog seed like acceptance and skip forking. (2) PR conflicts with main on specs/README.md (git merge-tree CONFLICT) — rebase. (3) main drift: wrangler.jsonc gained AUTH_LIMIT 1002 / AUTH_CREDENTIAL_LIMIT 1003 (10/min) / BILLING_SYNC_LIMIT 1004 + REVENUECAT_ACCEPT_SANDBOX; #164 moved the WS token to `Sec-WebSocket-Protocol: ambry.auth.jwt, <jwt>` — spec's env.load list and scenario 3 recipe are stale. (4) Guard condition 3 compares against gitignored `.env`; eas.json carries the committed production URLs in all four profiles — compare against that instead (also fixes fresh-clone fail-closed and the .env-points-at-a-dev-branch hole). (5) Neon JWKS added without branch_id applies to ALL branches (api-docs addprojectjwks) — spec must require branch-scoped JWKS. (6) Console-created Neon branches auto-delete after 1 day by default — standing branch must be created without expiration.
- Source checks done via WebFetch: Artillery MPL-2.0 / ensure exit code / ws engine / no account (--record opt-in) / Cloud $0-$199-$499 / `report` removed v2.0.22; Workers Free 100k req/day account-wide; wrangler env naming + non-inheritable bindings; crons [] disables; DO SQLite-only on Free, 100k DO req/day; Neon Free 10 branches, 0.5 GB, 100 CU-h; Postgres TRUNCATE CASCADE semantics.
- Open for Everett (defaults fine): Workers plan (ceiling 2,000 vs 10,000); fork+purge vs build-from-db; GET /version anonymous vs behind requireUser (runner has load users, so authenticated is possible); thresholds calibrate over 4 runs.

## pantry — PR #142 (spec 54 load testing) CRITIC review (read-only, orchestrator-delegated)
- Verdict: request changes. PR is CONFLICTING on specs/README.md (42 behind main). Three spec-level defects: (1) `truncate app_auth.* cascade` truncates catalog_items + every FK-linked table (Postgres CASCADE truncates referencing tables; ON DELETE SET NULL is not honored) — contradicts "catalog_items survives"; (2) identity minting collides with the per-IP auth limiter (`authRateLimited` charges credential paths to the binding AND the per-isolate 10/min map, one source IP = the mini) — 225 JWTs/run at 10/min > JWT TTL; scenario 1 alone exceeds the 2,000-request ceiling; (3) Neon JWKS registration is project-wide unless `branch_id` is set — spec must pin branch scope. Also: public open-signup load host shares the Free 100k/day quota; guard's `.env` premise breaks on acceptance machines; scenario 3 lag includes a Data API membership round-trip; drift vs Everett's 09-04 22:19 decisions (free scans 10→8, verification ON).
- No worktree created (read from .claude/worktrees/agent-a27265cab244e947c @ 89ce971). Report returned to the orchestrator.

## Ambry (pantry) — Worker route tests for barcode-lookup / recipe-ideas / import-recipe, PR #184 (2026-09-05, ~01:50–02:10 EDT, worker)

**PR:** https://github.com/ezybg7/pantry/pull/184 — branch `test/worker-route-coverage-2026-09-04` off origin/main (c35ee30), ONE squashed commit `166f128`. Six files, 2,865 insertions: `workers/src/routes/{barcode,ideas,importRecipe}.test.ts` (69 / 60 / 113 tests) + one dated §Test plan citation in each of `specs/{barcode-capture,recipe-ideas,recipe-import}.md`. Pure tests — no route behaviour changed. Merge watcher armed on the new head (pid 85096, log `~/agents/logs/merge-watch-184.log`); worktree `~/agents/worktrees/worker-tests` removed; main checkout untouched. The local branch `test/worker-route-coverage-2026-09-04` still exists in the main repo (fully pushed) — prune after the squash-merge.

**Resumed from the earlier attempt's three uncommitted drafts** (the brief said two; `importRecipe.test.ts` was there too). They were sound and 233/237 green; all four failures were test-side: `recipe: pancakes` PARSES as scheme `recipe:` (the guard refuses it generically, not as `invalid_url`) → moved to the perimeter list, `http://` and `https://exa mple.com/` are the true unparseables; `http:///r` is WHATWG-normalised to host `r` (not a no-host case) → dropped with a comment; the Instagram caption fixture had a trailing space `ogDescription` trims → `.trim()`; the ideas dedupe test used `/^- /gm` (matches bullets, not lines) → `split('\n')`. Also fixed: `barcode.test.ts` carried a literal NUL byte in its "a null byte" fixture (`file` said "data") → replaced with the backslash-u-0000 string escape. Added: the ideas client-supplied-list-ignored test (the spec's code-review gate), object / megabyte / array-body validation cases, and a header note that #182's per-kind daily pool (0063) is SQL-side so the route claims hold either way.

**Gates (worktree, node_modules symlinked to main's for root AND workers):** 242/242 on the three files; `--detectOpenHandles` clean on each; `TZ=Pacific/Kiritimati` rerun green; root `npm run typecheck` 0 — NOTE the root tsconfig EXCLUDES `workers/`, so `cd workers && npx tsc --noEmit` is the gate that actually covers these files (also 0); `npm run lint` 0 (eslint ignores `workers/*` by design).

**gitleaks episode (first push failed the PR's secret scan):** the barcode dispatcher test's unverifiable-token fixture (a JWT-shaped `eyJ…` literal) tripped `generic-api-key` (entropy 4.5) at barcode.test.ts:919; the watcher exited on the `fail` line, as designed. Fix: assemble the token from `{"alg":"EdDSA"}` / `{"sub":"x"}` / `sig` with `btoa` at run time (identical bytes, no literal). Lesson: CI scans EVERY commit in `origin/main..HEAD`, so a fix-up commit does not help — the two commits were squashed (`git reset --soft origin/main`, one commit, `--force-with-lease`), verified locally with the same pin (`gitleaks 8.30.1` is on the mini: `gitleaks git --log-opts="origin/main..HEAD" .` → 1 commit, no leaks). Re-arm the watcher only after `gh pr checks` shows the new head's runs pending — armed against the stale `fail` it exits within a second.

**Defects found, recorded as `it.failing` (file:line in the tests and in the PR body), NOT fixed:** (1) medium — `importRecipe.ts:275` the catalog read in `respond()` runs outside `aiExtract`'s refund guard, so a DB failure after a successful `import` claim keeps the charge (the receipt.ts class of bug); (2) low — `barcode.ts:155` the impossible-conflict Error names the typed/OFF product and the dispatcher logs it (spec §Privacy says the route's one log line carries no request data); (3) low — a JSON `null` body throws a TypeError out of `barcode.ts:97` / `ideas.ts:101` / `importRecipe.ts:307` → 500 `internal` instead of 422. A fix PR should flip the matching `it.failing` to `it` in the same change.

**Other gotchas:** `git worktree remove` needs the `node_modules` symlinks gone first (they show as `??`); `gh pr create --fill` copies the commit body, so the required robot footer was set afterwards with `gh pr edit --body-file`; `merge-watch.sh` exports `GH_REPO` itself now, so `nohup … &` from anywhere works.

**Open:** #184 merges on green via the watcher; the three defects want a small fix PR (Everett's call on priority — (1) has a money consequence); re-read the barcode metering header once #182's 0063 lands.

## Pantry-group phone mocks (2026-09-05, ~02:20) — read-only design task, no repo edits
Built 20 shadow-root HTML mocks (5 screens × today + A/B/C from `~/agents/research/screens/pantry.json`) + `manifest.json` at `~/agents/research/mocks/pantry/<screen-slug>/`; generator kept at `~/agents/research/mocks/_build/pantry-mocks.py` (one shared CSS block, Ambry light tokens, Test Home content). Verified by headless-Chrome render of each 4-up preview. Illustrative content flagged in manifest `look_for`: zones "Door"/"Top shelf" (Test Home seeds none) and Bananas shown overdue on Expiring A/B to exercise the Overdue header. Open: none — hand the manifest to whoever runs the mock viewer.

## Ambry — add-capture phone mocks (product-design mock builder, 2026-09-05)
- Built 16 HTML-fragment mocks (390×844, shadow-root safe, no external assets) + `manifest.json` under `~/agents/research/mocks/add-capture/<screen-slug>/{today,<direction-slug>}.html` from `~/agents/research/screens/add-capture.json`; generator + viewer live in the session scratchpad only (`mocks/build.py`, `shared.py`) — re-create from the mocks if needed. Repo read-only; simulator untouched.
- Findings for Everett: `ConfirmSheet`'s CTA on main still fills with `accentFill` (design-system rule 2 says primaries are ink — visible in the Add-tab today mock); the viewfinder meter reads "5 of 8 AI scans left this month" (`captureMeterLabel`), the requested "3 of 8 scans used" string is Profile/paywall `usageLabel`; 3C's toast lands over the shutter row at Toast's fixed offset.
- ~02:45 EDT: #184 (Worker route tests, 242 cases) MERGED by its watcher; three `it.failing` defects → fix agent on `fix/worker-route-defects-2026-09-05`. #182 fix agent pushed 3 commits (contracts corrected, retired figures swept, privacy line pulled) then stalled on CI; orchestrator rebased the branch onto main (one MIGRATIONS.md conflict: kept main's 0054 row, reserved rows follow), pushed 5524788, MERGEABLE, CI running; round-2 critic launched. #142 reviews in (advocate 8 + critic 12 findings → `~/agents/reviews/pr142-findings.md` with lead decisions D1–D6; fix agent running). Pantry mocks done (20 files); gallery rebuilt.

## Ambry (pantry) — account-group phone mocks (read-only design task)
- Built 32 HTML-fragment mocks (8 screens × today + 3 directions) from `~/agents/research/screens/account.json` into `~/agents/research/mocks/account/<screen-slug>/{today,<direction-slug>}.html` + `manifest.json` (slugs computed with the prescribed regex, names asserted against the JSON); generator kept at the session scratchpad only (`gen/build.py`) — not durable, regenerate from the mocks if needed.
- Open: "Welcome + Sign in + Sign up" today shows Sign-up only (one frame), the paywall mocks use the decided $4.99/$29.99/14-day figures while `STATIC_PACKAGES` still says $2.99/$24.99/7d, and the insights/recipe counts are illustrative where the seed has no numbers (12 eaten · 4 went off; Bananas ×3).

## pantry — K7 email verification specced (spec 57) → PR #185 (worker session, docs only)
- **Done:** Everett's 2026-09-04 22:20 EDT "sure" to audit-2026-09-04 DECISION 2 (K7) is written down: new `specs/email-verification.md` (**spec 57** — 57 verified free on main + every remote branch; 55/56 are PR #176's), `docs/adr/auth.md` **D12** (+ dated notes on D8 and O2, Governs line), `specs/README.md` row 57 + graph, audit K7 row → DECIDED + DECISION 2 resolution paragraph, `next-steps` §4.1, SPEC.md §Auth status note, `docs/privacy.md` (Resend row; confirmation step + 30-day prune disclosed ahead of the build, effective date bumped to 2026-09-05 — pages.yml publishes it on merge), `docs/ACCEPTANCE_TESTS.md` §Spec 57 `e2e: [manual]` + Scenario 1 precondition, spec 28 forward notes + §History. Branch `spec/email-verification-2026-09-05`, commit `beefa77`, **PR https://github.com/ezybg7/pantry/pull/185** (not merged — Everett reviews). Worktree removed.
- **Decision recorded as default-taken:** the **strict** gate — a password account has no session until its address is confirmed (`requireEmailVerification: true`); lenient gate (sign in now, withhold grant/digest/invites) rejected with reasons in the spec and the PR body; Apple/Google verified via the provider claim, `trustedProviders` and `autoSignInAfterVerification` rejected. Overturn word: "lenient".
- **Verified against `better-auth@1.7.2` source (`workers/node_modules`) + docs:** 403 `EMAIL_NOT_VERIFIED` only after the password matched; `sendOnSignUp`/`sendOnSignIn`; `expiresIn` default 3600 and the token is a signed HS256 JWT (replayable, inert — not stored, not single-use); `/send-verification-email` always `{status:true}` behind a 500 ms floor; `accountLinking.requireLocalEmailVerified` defaults true; `onExistingUserSignUp` + `onPasswordReset` exist; sign-up's generic-duplicate path is `requireEmailVerification || autoSignIn === false`.
- **Findings worth carrying:** (1) `scripts/seed-neon-dev-login.mjs`'s "already exists" branch has been dead since #162 — the generic-duplicate 200 returns a synthetic user id and the script would emit household SQL for a nonexistent row; spec 57 makes the implementation PR fix it (sign in first). (2) iOS re-reads the AASA only on install/update, so the Worker `/verify` fallback page is load-bearing at rollout; no new EAS build needed (domain unchanged). (3) PR #182's 0061 must move the grant trigger to `after insert or update of "emailVerified" … when (new."emailVerified")` and the cutover grandfather `update` must run BEFORE 0061 — needs a line in #182's text.
- **Open / Everett:** review #185; `RESEND_API_KEY` + Resend-verified `AUTH_EMAIL_FROM` (= R3; one real reset first) gate the build; the implementation PR (`feat/email-verification`) is queued work, not started. Gates on the branch: `npx jest --maxWorkers=2 tests/` 9/103 ✅, `npm run lint` ✅.

## PR #182 critic round 2 (spec 23 one-paid-tier, worktree at 5524788) — handoff
- Round-1 queue: 20/21 fully applied in text; item 15 missed `specs/audit-2026-09-04.md:371` ("Pricing is locked $2.99/$24.99") and the audit's K7 row (:92/:532) is unannotated. Registry test 51/51; privacy.md byte-identical; README row 23 = 7 fields; 0054→0058 table break removed by the PR.
- New defects filed to the lead: FIND-201 trial cap is per calendar month (spec :183) not "for its duration" (:58/:119/:163, ADR D2) → straddling trials get 120/40 ≈ $1.20; FIND-202 ledger hash `lower(trim(email))` does not bound Gmail +tag/dot aliases, "costs a real inbox" claim false; FIND-203 spec names `handle_new_neon_user` (dual installer, 0049:92 "REUSED UNCHANGED") as grant issuer + FK/trigger-order hazard; FIND-204 audit lines; FIND-205 §Acceptance gaps (deletion-regardless + refund-to-grant missing, :570 mis-pointed at deleteAccount.test.ts, four proofs unnamed); FIND-206 spec 11 lacks a forward note that the ledger survives deletion.
- Verdict: not merge-ready yet; one small fix commit away. Worktree removed.

## PR #182 fix (spec 23 one-paid-tier amendment) — round 1 applied, pushed, CI green, NOT merged (agent, 2026-09-05 ~01:30–02:15 UTC)

- **Branch** `spec/one-paid-tier-2026-09-04`, head **`cd041e12`** (3 commits on a9ba217: `38f94c3` spec/ADR/registry contracts + decisions · `fa347cb` repo-wide sweep of the retired price/cap claims · `cd041e1` privacy 13-month line pulled). Worktree `~/agents/worktrees/pr182-fix` removed. Docs only; 16 `.md` files vs merge-base; `docs/privacy.md` byte-identical to main.
- **Queue** `~/agents/reviews/pr182-findings.md`: all 21 applied; none found wrong against the code. Two comment-level items (7's `scanQuota.test.ts` "0059" comment, 19's `paywall.tsx:3` "two entrances") recorded for the implementation PRs — code files, this PR is docs-only.
- **Decisions written**: D2 CLOSED at $4.99/mo · $29.99/yr · 14-day trial with an in-trial cap 60 scans / 20 ideas (source "Everett, 2026-09-04, release-pass page"); spec §Price is a record; $2.99/$24.99/7-day retired everywhere but history (SPEC.md, specs/README row 23, site/guides, site/tasks, app-store checklist). Perks: priority learning dropped, themes/rich export deferred, basic export off the free list + lapse contract until `specs/export.md`. Grant ledger `welcome_grant_ledger` (sha256(lower(email)), 13 months, survives deletion) decided. **K7 email verification ON** recorded as a decided dependency (Better Auth + Resend; grant issued only on `"emailVerified"` true; ledger still applies; auth-spec amendment + implementation follow separately; 0061 must not apply before it). #176 / #142 noted as not monetization items.
- **Contract fixes**: 0062 re-creates `household_join_gate` (0045) not `join_household`; `ai_calls.funded_by` in 0061 with both meter subqueries filtering; grant sizes + member cap in an `app_config` row (Postgres trigger can't read Worker env); kill switch checked before the grant branch; grant branch decides funding only (daily check + insert always run); photo count paginated (R2 list caps at 1000); 0059 folded into 0061 (row kept, 🔒); 0060 grows `entitlements.period_type` for the trial cap; `shelf_life` own daily pool in 0063 (`SHELF_LIFE_DAILY_CAP`); PDF cost row + L16 decided (5 pages, launch-flip dependency); free-bounded cost rows; D4 dated revision; client copy from the payload; comparables softened; `## Acceptance` added; spec 19 §D gate retitled.
- **Gates**: `npx jest --maxWorkers=2 tests/` 9 suites / 103 pass; `npm run lint` clean; `scripts/check-disclosures.mjs` clean. **CI on cd041e12: checks pass (6m38s) · disclosures pass · gitleaks pass** — `gh pr checks 182` explicit pass lines (first `--watch` returned "no checks reported" before GitHub registered the runs; re-read after).
- **PR body** rewritten via `gh pr edit --body-file`: existing body kept, "Decisions taken by Everett (2026-09-04)" (six readings) replaces the open price question, "Review round 1 — what changed" table per finding.
- **Open / next**: Everett merges #182 (agent did not merge, per instructions). Follow-ups it creates: an auth-spec amendment for email verification (K7) before 0061; the implementation PRs (0061→0062→0063 in numeric order, 0060 with `period_type`, `STATIC_PACKAGES` → 4.99/29.99/14, the 5-page PDF cap, the prune + privacy line + effective-date bump, the two stale code comments). A `specs/export.md` is the first post-launch free feature.
- **Correction (~02:20 UTC):** the main session rebased the branch onto current main (`768531ba`) right after my push — same four commits, new SHAs: `005a6db7` (original spec commit) → `408ce6b9` contracts+decisions → `702d714b` sweep → **`55247889` head** (privacy pull). PR diff unchanged (the same 16 `.md` files, no code). **CI on 55247889: checks pass (6m38s) · disclosures pass (7s) · gitleaks pass (6s); PR MERGEABLE.** The `cd041e12` SHA above is superseded. Worktree dir was already gone when I went to remove it; `git worktree list` no longer lists it.
## pantry — lists-recipes mocks built (mock-builder agent, read-only against main)
- Wrote 28 mocks + manifest to `~/agents/research/mocks/lists-recipes/<screen-slug>/{today,<direction-slug>}.html` (7 screens × today + A/B/C; slugs from the JSON names; every file one `<style>` + one 390×844 `.screen`, ≤175 lines) via `~/agents/research/mocks/_build/lists-recipes.py`; all 28 rendered and checked in the browser at 1:1 through the gallery's shadow-root wrapper. Feeds `~/agents/artifact/build-gallery.py`.
- Content deviations to know: Bread is `grains_pasta` in the seed (no "Bakery" category exists), restock rows are Milk+Butter→Fridge, Bread→Pantry, Dish soap unlinked; C. Cart bar is drawn as the bar expanded into its bought-list panel (the only way to show all three regions on one screen); Editor today is scrolled to Servings→Ingredients so the slider and ingredient rows are visible.

## Ambry — 06:50–07:05 EDT: second limit reset; finishing the cut-off agents by hand (orchestrator)
- Acceptance suite **12/12** (02:23 EDT run, main@dfcd0f0 build, iPhone 17 Pro, branch with 0054/0053/0047): flows agent had committed flows + runbook (b2382ae) → pushed, **PR #186** opened, watcher armed.
- **#142 (spec 54) MERGED** 06:54 EDT by the orchestrator (Everett delegated the review; advocate 8 + critic 12 findings applied by the fix agent; CI green; body carries round-1 + decisions D1–D6, D3 = build-from-db default). Local branch/worktrees cleaned.
- **PR #187** opened: the three Worker route defects from #184 (import refund guard, product name out of logs, null body → 422; shared body reader also hardened receipt/shelfLife); gates 344/344, tsc, typecheck, lint green; watcher armed.
- **Gallery artifact published** (03:00 EDT, before the limit): https://claude.ai/code/artifact/35a97b24-9c2f-4841-b4e1-d3f130bb31d9 — 24 screens × (Today + A/B/C) = 96 mocks, research + sources, per-screen decision recorder on `db` collection `decisions`.
- Relaunching: #182 round-2 fix (trial window, gmail-alias hash, `issue_welcome_grant` trigger, doc gaps) and the #185 combined review. Still pending after that: the release-pass artifact (publish once #186/#187 merge), applies/deploys/consoles = Everett.
- 07:20 EDT: **release-pass artifact published** https://claude.ai/code/artifact/5a8fb5ae-ad96-49a5-8388-bfcc3bdf3906 (269 manual checks: device 67 · runbook [manual] 136 · release gates 66; 10 actions; decisions with Everett's six answers rendered as decided; evidence table; db collection `checks` for his ticks/notes). Republish with final numbers after #186/#187 merge and the coverage wave lands.

## pantry — PR #185 (spec 57 email verification) advocate+critic review — 07:10 EDT
- Reviewed beefa77 read-only in a detached worktree (removed). Output: `/Users/orchestrator/agents/reviews/pr185-review.md`.
- Verdict: approvable with fixes. Two Highs are spec-text fixes before the implementation PR: (1) the `onExistingUserSignUp` notice mail sits outside the per-address cooldown → mail-bomb + Resend-quota DoS; (2) the 30-day prune's `not exists session` guard deletes provider-unverified social accounts that own households once they sign out / their session expires (cascade user → profiles → household_members).
- Verified nine Better Auth 1.7.2 source citations (all accurate) + four docs pages; `sendOnSignIn` docs drift noted.
- Open: #182's text still issues the grant from the signup trigger; spec 57 names `grant_welcome_if_verified()` — align before either merges.
- ENV DEFECT (not mine, not fixed): `/Users/orchestrator/code/pantry/workers/node_modules` is a self-referential symlink since 07:03 — needs `rm` + `npm ci` in `workers/`.

## PR #182 fix — review round 2 applied, pushed, CI green, NOT merged (agent, 2026-09-05 ~06:55–07:25 EDT / 11:10 UTC CI)

- **Branch** `spec/one-paid-tier-2026-09-04`, head **`6579dfb1`** (one commit on 5524788; 7 `.md` files, docs only). **CI on 6579dfb1: checks pass (4m22s) · disclosures pass (9s) · gitleaks pass (6s)** — explicit `gh pr checks 182` pass lines; PR MERGEABLE. Body gained "Review round 2 — what changed" (per-finding table). Worktree `~/agents/worktrees/pr182-r2` removed. Everett/lead merges.
- **All six findings applied.** FIND-201: the in-trial cap was a *calendar-month* cap ("only the limit changes / no function change") → 0060 gains `entitlements.trial_started_at` beside `period_type` (webhook `purchased_at_ms`, sync `purchase_date`); `claim_ai_call` counts the pool from it while the row reads `'trial'` (`coalesce(trial_started_at, v_month_start)`), `scanQuota.ts` mirrors the window and names the trial's end as `resets_at`; **0060 now lands before 0061** (chain 0060→0061→0062→0063); env vars renamed `QUOTA_TRIAL_TOTAL`/`IDEAS_TRIAL_TOTAL`; $0.60 worst case holds on any start date; straddle stated as solved. FIND-202: ledger key = `welcome_grant_email_key(email)` — lower/trim, `+tag` cut for every domain, Gmail dots removed + `googlemail.com` folded; farming claim now "a fresh verified address, not a fresh inbox"; `/auth/*` per-IP limits + 50/day error log named as the residual bound. FIND-203: issuer is a NEW `issue_welcome_grant()` on `on_app_auth_user_verified` (`after insert or update of "emailVerified" … when (new."emailVerified")`), named to sort after `on_app_auth_user_created` (profiles FK), idempotent via ledger + `on conflict`; `handle_new_neon_user` untouched (second installer `db/neon-auth-wiring.sql:62-69`, 0049:92 "REUSED UNCHANGED", insert-only trigger); cross-referenced to spec 57 both ways — grandfather `update` runs BEFORE 0061; spec 57's `grant_welcome_if_verified()` sketch defers to this name. FIND-204: audit :92/:371/:535 annotated — **the two K7 sites carry PR #185's exact wording** so either merge order rebases clean. FIND-205: §Acceptance proofs named (`photos.test.ts` not `deleteAccount.test.ts`; `revenuecat.test.ts`, `digest.test.ts`, `firstRun.test.tsx`) + deletion-regardless / refund-to-grant / lapsed-remainder lines. FIND-206: spec 11 step 7 forward note (ledger survives deletion). Nits: §Goal "zero-marginal-cost", member cap cites `household_join_gate` (0045), SPEC.md ledger comment + 0060 columns.
- **Gates (worktree):** `npx jest --maxWorkers=2 tests/` 9 suites / 103 pass · `npm run lint` clean. Contradiction grep (`handle_new_neon_user` as issuer, `only the limit changes`, `no function change`, `sha256(lower`, `for its duration`, `QUOTA_TRIAL_MONTHLY`) — only historical/negated mentions remain.
- **⚠️ Environment defect found and fixed — read this before symlinking node_modules again.** At 07:03 `~/code/pantry/node_modules` and `workers/node_modules` were **self-referencing symlinks** (ELOOP): every `npx`/`npm run` in main and in the three worktrees linked to it (`coverage`, `pr182-r2`, `pr185-rev`) printed only `npm notice run …` and **exited 194**. Cause: main's real install had been deleted and an agent ran `ln -sfn ~/code/pantry/node_modules node_modules` with cwd = main. Worse, **PR #187's merge (722b410 = origin/main) COMMITTED both symlinks** (mode 120000) — `.gitignore`'s `node_modules/` matches directories only. Fixed on the mini: both links removed, `npm ci` in root and `workers/` (npm 12.0.1 skipped `unrs-resolver`/`workerd` postinstalls by policy; gates still green). **Fix PR #188** (`fix/untrack-node-modules-symlinks`): `git rm --cached` both + `.gitignore` patterns without the trailing slash. Until #188 merges, `git status` on main shows ` D node_modules` / ` D workers/node_modules` — that is expected, do NOT `git checkout -- node_modules` (it would recreate the loop). CI not watched for #188.
- **Open / next:** merge #182 (lead) and #188 (trivial; lead). Implementation follow-ups now read: 0060 first (two trial columns + `last_event_at`), then 0061 (`issue_welcome_grant`, `welcome_grant_email_key`, `claim_ai_call` window), 0062, 0063; spec 57 (#185) keeps its cross-reference — consider renaming its `grant_welcome_if_verified()` sketch to `issue_welcome_grant()` in #185 or its implementation PR.
- 08:05 EDT: #182 round 2 applied (6579dfb, CI green, MERGEABLE → Everett's approval). Defect: my `git add -A` in the worker-defects worktree committed the `node_modules` + `workers/node_modules` symlinks to main via #187; the round-2 agent restored main's node_modules (`npm ci`) and opened **#188** (untrack + slash-less ignore) — watcher armed. Lesson saved to auto-memory (feedback-worktree-symlink-gitignore). #185 review in (`~/agents/reviews/pr185-review.md`, approvable with fixes) → fix agent running; coverage wave running.

## Ambry (pantry) — coverage wave: six new Maestro flows + sixth acceptance run → PR #189 (worker, ~07:00–08:00 EDT)
- **PR:** https://github.com/ezybg7/pantry/pull/189 — branch `test/acceptance-coverage-wave-2026-09-05`, ONE commit `e321363` rebased onto main `e6a0ac2` (#186 had merged as squash `8f02ed0` mid-task, so `git rebase --onto origin/main b2382ae`; one conflict in `specs/barcode-capture.md` vs #184's citation, resolved keep-both). Base `main`, no watcher armed (per brief). Worktree `~/agents/worktrees/coverage` removed; the local branch remains in the main repo (fully pushed) — prune after merge. Main checkout untouched.
- **Authored, run alone until green, then in the full suite:** `04-item-screen` (green 1st), `05-status-lifecycle` (green 1st — the row chip is tapped BY COORDINATE: an inventory row is one a11y element, chip not in the hierarchy; layout pinned by filtering to one row + dismissing the out-rows hint; 17 Pro geometry row y=274, chip x≈338 stocked/low, x≈304 out; `retryTapIfNoChange: false`), `06-households` (green 1st; leaves the invite code ROTATED by design — now `d62340d6ee94b2da` after two runs), `07-profile` (green 4th), `08-paywall` (green 2nd — covers the Plan group; the paywall screen is unreachable in a dark build), `15-zones` (green 1st). `config.yaml` flowsOrder updated.
- **Full suite (sixth run) 07:38–07:51 EDT: 17/18 in 12m30s.** All six new flows passed in the batch. The failure is `13-waste-scorecard` on its count (20 vs 21): the fifth run's swallowed-403 ledger insert, again — one `status 403` 106 ms after a DELETE's 204. **Lone re-run failed identically** (24 vs 25; Eggs this time, Butter in the batch). 2 of 2 today → escalated in the PR as the thing to look at before the release pass. Ledger on the branch now 24.
- **Recorded `[manual]` on device evidence:** spec 43 invite link (`simctl openurl` raises SpringBoard "Open in Ambry?" for `pantry://`; https opens Safari's fallback page then the same dialog; the token is never on screen) and spec 18 dark mode (system scheme; nothing in the hierarchy differs). Capture (6/7) and recipe import: explicit `e2e:` lines added to barcode-capture.md / receipt-capture.md. `specs/monetization.md` NOT touched (#182) — its e2e line lives in the runbook §Spec 23; port when #182 lands.
- **Gotchas learned (all in the runbook):** (1) Maestro `eraseText` raises the software keyboard and leaves it up (hardware keyboard notwithstanding) — a Save under it took the tap as an "s"; a centring scroll overshoots by the keyboard height when it dismisses; fix = neutral tap on a label (default keyboardShouldPersistTaps) or return key. (2) Non-pressable `SettingsRow` = two a11y elements (`Plan` / `Free`); pressable = one merged label. (3) Never `openurl` at the simulator mid-suite — the SpringBoard dialog stalls `_launch`; I had to dismiss three by hand via the simulator tool, and the app then showed "No storage locations yet." until relaunched (empty RLS read rendered as truth). (4) `git rebase … | tail` masks the conflict exit code — a push went out mid-rebase (harmless, force-with-lease fixed it); never pipe the rebase.
- **Branch residue after the run:** Milk `out` + `needed` list row (12), invite code rotated, ledger 24; 05/07 self-heal their preconditions; clear 12's list row 09's way before a re-run (done today via a temp flow, not SQL).
- **Gates:** lint 0; `npx jest --maxWorkers=2 tests/` 9/104 on the rebased tree. **Open:** Everett reviews #189; decisions flagged in the PR body (code rotation residue, Plan-group-not-paywall, coordinate taps); the 13/403 finding; release-pass artifact republish once #189 lands.
- 08:20 EDT: coverage wave → **PR #189** (six new golden paths: 04-item-screen, 05-status-lifecycle, 06-households, 07-profile, 08-paywall (dark build: plan group only), 15-zones; 16-invite-link + 17-dark-mode recorded `[manual]`; capture/import `e2e:` lines added). Full suite 17/18 in 12m30s: 13-waste-scorecard fails 2/2 today — one `food_outcomes` INSERT 403 ~106 ms after the item DELETE's 204 (policy is `is_household_member(household_id)`, so the row's household_id is missing/wrong or auth unresolved) → fix agent on `fix/food-outcomes-403-after-delete`. Watchers armed on #188, #189. Note: #189's flow 06 leaves the seeded invite code rotated (nothing reads it).

## pantry — PR #185 (spec 57 email verification) review round 1 APPLIED, pushed, NOT merged (fix agent)
- **Branch** `spec/email-verification-2026-09-05`, rebased onto main `e6a0ac2` (#188) to clear the `specs/README.md` board conflict (rows 54 and 57 both appended; kept both). Head **`c298af2b`** (one commit on the rebased spec commit `ab5ae0b`). Docs only: `specs/email-verification.md`, `docs/adr/auth.md` (D12 bullets), `docs/ACCEPTANCE_TESTS.md` (step 8 hedged, step 11 added), `specs/README.md` row 57; **`docs/privacy.md` reverted byte-identical to main** (FIND-009 — the lines live in the spec's new §First implementation PR, #182-style). Worktree `~/agents/worktrees/pr185-fix` removed.
- **All nine findings + decision 4 applied** (`~/agents/reviews/pr185-review.md`). Substantive design changes to know: (1) the per-address bound is now a **Postgres ledger** `app_auth.email_sends` (1/min AND 5/UTC-day, alias-normalised sha256 key, one atomic upsert) shared by all four mail triggers incl. the existing-account notice — the `AUTH_EMAIL_SEND_LIMIT` ratelimits binding is gone because a Cloudflare binding period is 10 or 60 s only; so spec 57 is **"one small migration"** now (number claimed at build time; 0064 unless the registry moves). Reset mail deliberately outside the ledger (claim path). (2) Prune = credential-only + unverified + 30 d + no `household_members` (+ no session as belt). (3) Found against the 1.7.2 source: `runInBackgroundOrAwait` (`context/create-context.mjs:214-217`) **catches and logs** a hook's throw on sign-up/sign-in/social, and the anonymous resend awaits directly — so the spec now says callbacks never throw; they record `sent|refused|failed` and `handleAuth` decides (sign-up 500 `EMAIL_SEND_FAILED`, sign-in 503 `EMAIL_NOT_CONFIGURED`/500, resend always neutral). The review's FIND-008 reading (400 reset message) was itself wrong for the same reason. (4) Resend floor raised to jittered 800–1200 ms in the Worker (FIND-005 — chose raise over accept). (5) `onPasswordReset` clears `name`/`display_name` on the claim path (FIND-006). (6) The spec's `libpg-query` test claim named a library the repo does not have — replaced with the `digest.test.ts` postgres-stub pattern.
- **PR body** extended via `gh pr edit --body-file` (existing body kept): "Review round 1 — what changed" table per finding + Decisions table (1 strict — **Everett re-confirms**, overturn word "lenient"; 2–5 as taken). Open item recorded for Everett: spec 28's reset mail has the same rotating-IP exposure (own per-address minute wall, no daily ceiling — a spec 28 amendment).
- **Gates on c298af2b:** `npx jest --maxWorkers=2 tests/` 9 suites / 104 tests ✅ · `npm run lint` exit 0 ✅.
- **CI on c298af2b (explicit pass lines from `gh pr checks 185 --watch`): checks pass (6m49s) · disclosures pass (8s) · gitleaks pass (9s).** Not merged — Everett approves specs (decision 1 strict re-confirm on the PR).
- 09:20 EDT: #185 round 1 applied (c298af2, CI green, MERGEABLE → Everett's approval; re-confirm strict gate). Spec 57 now carries one small migration (per-address `app_auth.email_sends` ledger, number claimed at build). Follow-up queued: spec 28 reset mail should reuse the ledger.
- 09:45 EDT: #189 (coverage wave) MERGED; release pass republished (44 PRs, 274 manual checks, acceptance 17/18 with the 403 defect noted, #182/#185 marked ready for approval). Changelog PR **#190** opened (watcher armed). Pending: the food_outcomes 403 fix agent.

## Ambry (pantry) — food_outcomes 403 after delete: cause proven, client fix → PR #191 (worker, ~08:00–08:45 EDT)
- **PR:** https://github.com/ezybg7/pantry/pull/191 — branch `fix/food-outcomes-403-after-delete`, one commit `aa725b9` on main `e6a0ac2`. Watcher armed (`merge-watch-191.log`). Worktree `~/agents/worktrees/outcomes-403` removed; local branch left in the main repo (pushed).
- **Cause (proven, not the brief's hypothesis):** the refused row carried the right `household_id` (183-byte body = the full Eggs row; 129 without it) and a token the DELETE had just used. Reproduced OFF-device with `~/agents/scripts/probe-outcome-race.mjs` + `probe-outcome-variants.mjs` (write nothing: `outcome:'probe'` fails the CHECK after RLS; DELETEs hit a random uuid): the identical INSERT draws `403 42501` in **2/65 rounds** when it follows a DELETE and rides alongside the app's refetches, **0/360** in every other shape (alone after DELETE, GETs without DELETE, 6 POSTs at once, fresh token); 200 GET rounds never returned RLS-empty `[]`. So `auth.uid()` (pg_session_jwt 0.5.0 `uid_wrapper`, PG 17.11 on the branch) resolves NULL for one request in a post-write burst — a Neon Data API defect, not our row/token. The cached-row/closure theory was checked file-by-file and is sound (`[id].tsx:700-716`, `hooks.ts:398,415,448,466`, `serverRepository.ts:325-330`).
- **Fix:** `useRecordOutcomes` → `writeLedger` retries a `42501` after 250 ms then 1 s with identical rows (a refused INSERT provably wrote nothing → cannot double); nothing else retried (lost answer may have written; CHECK would fail again); `LedgerRowError` for a row without household (never sent); `LedgerWriteError` (code+attempts, no names) to `reportError` when the schedule is spent + dev `console.warn`. `isPrivilegeRefusal` (42501) added to `src/lib/failure.ts`. Order stays DELETE→INSERT per spec 39. New `useOutcomes.test.tsx` (7) + 2 in `failure.test.ts`; spec 39 §Decisions/§Client/§Acceptance/§Test plan/e2e line/new §History; runbook fifth-run item 5 postscript. `out-lifecycle.md` untouched (no toast/ledger path in it).
- **Gates:** tsc 0 · eslint 0 · targeted jest 14 suites/280 · new suites in all 5 pinned zones · full `npx jest` 175/3169.
- **Open / Everett (defaults taken, in the PR body):** retry schedule (2 retries) · Sentry report of the spent-schedule loss (inert until a DSN exists) · whether to raise the `auth.uid()` NULL flake with Neon (probes are ticket-ready). **13-waste-scorecard needs a new Release build** (`acceptance-cycle.sh`) before it can pass — not built here. Follow-ups: the same NULL on a DELETE = silent 204 deleting nothing / on a GET = empty page (not reproduced; consider `count=exact` on delete hooks); flow 13's header still says "swallowed by design" (PR #189 owns the file this week).
- **Correction (~08:50 EDT):** #189 and #190 merged mid-task → #191 was CONFLICTING on `docs/ACCEPTANCE_TESTS.md` (the "Latest tally" callout). Rebased onto `be86ee1`, kept main's sixth-run callout and added the PR #191 pointer there and in sixth-run item 2; head is now **`851e52d0`** (`aa725b9` superseded). tsc 0 + the three ledger/delete suites green on the rebased tree; force-pushed with lease. Worktree removed again.
- 10:10 EDT: the 13-waste-scorecard 403 = Neon Data API defect (auth.uid() NULL for one request in a post-write burst, 2/65 rounds off-device; row complete, token fresh). **PR #191** retries a 42501 with identical rows (250 ms, 1 s), typed ledger errors; spec 39 + runbook updated; watcher armed. Follow-ups: raise with Neon (Everett), same NULL on DELETE/GET not reproduced, RPC for a shared transaction. Next: after #190/#191 merge, `acceptance-cycle.sh c4` (new Release build) to re-prove 13 and the full 18.
- 10:35 EDT: #190 (changelog) and #191 (ledger retry) MERGED; main 7f40d5a. Cycle **c4** launched (new Release build of main against the branch + all 18 flows) to re-prove 13-waste-scorecard; release pass republishes with the result.
- 09:00 EDT: cycle c4 (new Release build of main@7f40d5a): **15/18 — 13-waste-scorecard passes** (ledger retry proven). 05-status-lifecycle's coordinate taps went astray (two Milk rows `out`, an auto-added "Milk, run out" grocery row) and cascaded into 09/11. Branch residue cleaned by SQL on the disposable branch (5 inventory rows incl. Bananas/Leftovers leftovers, 3 grocery rows) → 0 items, 0 grocery, 3 locations. Agent launched: status chip gets its own accessibility element (real VoiceOver gap too) + flow 05 rewritten to use it and to restore state; builds from its worktree via `REPO=… acceptance-cycle.sh --build-only`.

## Ambry (pantry) — status chip a11y + flow 05 rewrite → PR #192 https://github.com/ezybg7/pantry/pull/192 (worker, ~09:05–09:45 EDT)
- **Cause of c4's 05/09/11 failures:** `ItemRow`'s outer Pressable was ONE accessibility element, so the status chip was not in the XCUITest hierarchy and the flow tapped a coordinate; the coordinate missed. Same fact was a real VoiceOver gap (chip + trash unreachable by touch on every inventory list).
- **Code (branch `fix/status-chip-a11y-and-flow-05`, worktree removed):** row = container (`accessible={false}`) of separate elements in reading order — body Pressable (name/subtitle; label "Milk, added Sep 5, 7 days left", rotor kept), chip button `Status: Stocked` + hint `Changes to Low` + `testID status-chip-<id>` (new `id` prop at 3 call sites), trash `Delete Milk`. Body label dropped "status …" (chip says it; avoids double announcement). `UrgencyBadge` hidden from the tree. `nextStatus` moved to `lib/types.ts` (re-exported from inventory hooks). Layout unchanged. `ItemRow.test.tsx` rewritten (structural "no accessible host ancestor" test).
- **Flow 05:** taps `Status: …` by label, asserts chip label + anchored body sentence per state, still filters to one row, `retryTapIfNoChange: false`; ends restoring Stocked and removing any Milk list row on the Lists tab (swipe → Remove) — this also retires the "clear 12's list row before a re-run" hand step (05 runs before 09). Comments in 02/03/12/13 refreshed. Specs (location-screen §Each row + checklist, stock-levels/out-lifecycle e2e lines, design-system §Chip) + docs/ACCEPTANCE_TESTS.md (§Scenario 3 rewrite + runbook note, preconditions, retired coordinate gotchas) in the same PR.
- **Runs (Release build of the branch, branch URL baked, sim 70893D75):** 02 alone — first attempt hit `_login`'s cold-start guard ("No storage locations yet.", cold compute; keep-alive first probe 1.3 s/0 rows), green on re-run. 05 alone green ×3 (clean; Milk already Stocked; Milk seeded on the list via a throwaway flow → Remove branch proven). 09 alone green, 11 alone green. Full suite: **18/18 in 12 m 3 s** (09:28–09:40 EDT; 05 in 36 s in the batch) — the eighth run, recorded as the Latest tally in docs/ACCEPTANCE_TESTS.md; c4 (seventh) stays recorded as 15/18 under §Scenario 3.
- **Tooling:** `~/agents/scripts/acceptance-cycle.sh` now `REPO=${REPO:-$HOME/code/pantry}` and pulls only on `main` (backup `acceptance-cycle.sh.bak-2026-09-05`). Build from a worktree needs the gitignored `.env` copied in (AUTH/WORKER URLs are baked from it) — done for this run.
- **Gates:** typecheck 0, lint 0, targeted jest 144/144, sweep 55 suites/805 tests. Watcher: armed — `nohup merge-watch.sh 192` → `~/agents/logs/merge-watch-192.log` (squash + delete-branch on `checks pass`). Commit `176a118`, one commit, 18 files. Local branch `fix/status-chip-a11y-and-flow-05` remains in the main repo (pushed) — prune after merge.
- **Open:** Everett — the three defaults in the PR body (body label without status; `Delete <name>`; rotor kept). Branch residue after the full run is the documented set (Milk out + Milk list row from 12, Bananas, ledger) — reset before the release pass or let 05 self-heal the Milk parts.
- 10:05 EDT: **18/18 in 12m03s** on a Release build of `fix/status-chip-a11y-and-flow-05` → **PR #192** (ItemRow becomes a container of body / status chip / trash accessibility elements — VoiceOver-by-touch fix; flow 05 taps by label and restores state; runbook §Scenario 3 rewritten; `acceptance-cycle.sh` honours REPO= and pulls only on main). Watcher armed. After merge: final release-pass republish; everything else is Everett's.
- 10:30 EDT: **#192 MERGED** (main c2c851a). Suite 18/18. Release pass republished (final for this pass: 47 PRs since Sep 3, 274 manual checks, 10 actions, evidence table). All orchestrator-side work for the readiness goal is done; the queue now waits on Everett: approvals #182/#185, applies 0047/0053/0054 (rebase #156/#161 first), gallery decisions, phone pass, Neon support ticket, deploys/consoles per the release pass.
- 10:50 EDT: Everett: "yes go ahead and download the USDA datasets" → three FDC zips fetched to `~/agents/usda/` (foundation 6.7 MB, SR Legacy 211 MB, FNDDS survey 66 MB unzipped; `surveyDownload.json` symlinked to the script's expected name). `export-usda-foods.py --selftest` 29/29; real run 4.5 s → `~/agents/usda/usda-foods-seed.sql` (3.0 MB, 13,587 rows). Rehearsed on the production-copy branch: applied in 3.8 s, counts exact, 0047 asserts 7/7 after, prefilter worst case 8.3 ms seq scan (no index needed). Production apply line added to the release pass (after 0047). #156's MIGRATIONS.md entry gets the record once its rebase lands.

## PR #161 rebased onto main a second time (agent, 2026-09-05 ~10:45–11:05 EDT)

- **Base**: `c2c851a` (#192). **New head**: `56b72d32` = the 9 branch commits replayed (range-diff: only 1, 2 and 9 differ, and only by context). Force-pushed with an explicit lease on `e50bda57`. `gh pr view 161` → **MERGEABLE / CLEAN**. CI on 56b72d3, explicit pass lines from `gh pr checks 161 --watch`: **checks pass (6m35s) · disclosures pass (8s) · gitleaks pass (8s)**. **Not merged** — held for Everett's production apply of 0053 (apply-before-merge). No PR comment left (nothing new to record on the PR).
- **Conflicts (2, same file, same spot)**: `specs/MIGRATIONS.md` registry table only — #183 appended the 2026-09-04 evening rehearsal sentence to the **0054** row, and the branch inserts its **0053** row immediately above it (commit 1 adds the row, commit 9 rewrites it with the rehearsal record), so both replays conflicted on the adjacency. Kept the branch's 0053 row + main's 0054 row, dropped the stale 0054 copy, both times. 3-way line check afterwards: all 45 branch-added lines present, all 6 branch-removed lines absent, main's 3 additions (0054 entry's THIRD-rehearsal sentence, the "As of 2026-09-04 (evening)" outstanding-migrations sentence, the 0054 row note) present, nothing from neither side; the FIRST bash block in §Pending applies carries 0053 + 0054 migration lines and the 16/20 assert lines (tripwire green); ⏳ kept on 0053's bullet and row.
- **Auto-merged and hand-verified**: `src/features/inventory/hooks.ts` (main's `nextStatus` re-export from lib/types + the branch's `BulkAddCapError`/`MERGE_ADD_MAX_ITEMS` import and cap check both present), `specs/household-management.md`, `specs/invite-links.md`, `specs/zones-ui.md` (main's e2e/[manual] rewordings on different lines than the branch's 0053 paragraphs). `BasketReview.tsx` / `RestockReview.tsx` were NOT touched by main since dfcd0f0 — byte-identical to e50bda5, cap branch inside both `catch (error)` blocks intact. `docs/ACCEPTANCE_TESTS.md` / `specs/README.md`: no conflict (the branch does not touch them).
- **Gates** (worktree, node_modules symlinked): typecheck 0 · lint 0 · jest 12 suites / 222 tests (bulkAddCap, mergeAddCap, migrations-registered + all 9 `*.test.tsx` beside the two screens; console.error noise = act() warnings only, as before).
- **Housekeeping**: worktree `~/agents/worktrees/pr161b` removed; local branch `fix/db-integrity-2026-09-04` remains in the main repo at 56b72d3 (fully pushed) — prune after merge. Main checkout untouched.
- **Still Everett's**: production apply of 0053 (and 0047/0054), then merge #161. Note for the next rebase: any main PR that edits the 0054 registry row will conflict with #161's 0053 row again — trivial, same resolution.

## PR #156 (spec 45, catalog nutrition matching) rebased onto main (agent, 2026-09-05 ~10:55–11:30 EDT)

- **Base**: `c2c851a` (#192). **New head**: `919050a5` = the 15 branch commits replayed + one follow-up commit ("Record 0047's rehearsal on acceptance-2026-09-03 and restate the suite counts"). Whole-tree check: the replayed +/- lines are byte-identical to the original branch diff vs its merge-base (2,564 lines). Force-pushed with explicit leases (1573c27 → 2bcf49b → 919050a). `gh pr view 156` → **MERGEABLE / CLEAN**. CI on 919050a5, explicit lines: **checks pass 5m2s · disclosures pass · gitleaks pass**. Gotcha: `gh pr checks --watch` returns exit 8 ("pending") immediately when stdout is not a TTY — poll `gh pr checks` every 30 s instead. **NOT merged** — apply-before-merge; waits on Everett's production apply of 0047.
- **Conflicts (4 stops, 3 files)**: `specs/MIGRATIONS.md` ×2 (commits 1 and 11) — main's "Nothing else was pending" paragraph (#183's 2026-09-04-evening sentence naming 0053/0047) vs the branch's inserted "Also pending" paragraph → main's paragraph verbatim + the branch's paragraph (commit 11's trimmed version). `workers/src/routes/shelfLife.ts` (commit 4) — main's `readJsonObject` import (#187) vs the branch's `usdaMatch` import at the same spot → both kept; #187's null-body hunk and `fillUsdaNutrition` do not overlap. `docs/ACCEPTANCE_TESTS.md` (commit 5) — main's new §Spec 23 + §Spec 18 vs the branch's §Spec 45 at the same anchor → main's sections in full, §Spec 45 placed after §Spec 18, immediately before §Spec 12 (its original neighbour). Everything else auto-merged (0047 entry/row/block, HISTORY header, closing paragraph, shelfLife.test.ts).
- **Follow-up commit**: the 0047 entry, row and block comment carry the rehearsal (2026-09-04 ~21:45 EDT, `acceptance-2026-09-03` / `br-damp-art-a6r6pyld`, psql direct endpoint as `neondb_owner`, `-v ON_ERROR_STOP=1 -1`, after 0054 and 0053: clean, **7/7**, 0053 still 16/16; table EMPTY). **Deviation from the task text** ("seed still not generated"): the 10:50 EDT entry above plus `~/agents/usda/usda-foods-seed.sql` (3,018,572 bytes, 13,587 tuples: foundation 363 · sr_legacy 7,793 · survey_fndds 5,431 — verified on disk) say it IS generated, so the record reads "rehearsed EMPTY; the seed came the next morning", the seed data-apply entry flipped to "GENERATED 2026-09-05, NOT applied to production", README row 45 and the spec's History line likewise. **Still to add**: the seed's OWN rehearsal record (3.8 s apply, counts exact, 7/7 after, 8.3 ms prefilter — orchestrator's log, not verified by me). Main's "Nothing else was pending" paragraph still says the seed "is not generated because the downloads await his yes" — kept verbatim per the task, stale on main too; fix it with the seed record.
- Suite counts restated as jest counts (usdaMatch **34**; shelfLife **78**, up from main's **66** — #187's null-body case is in the base now) in ACCEPTANCE_TESTS §Spec 45, README row 45 and the spec's §Test plan; the old 30 / 47 / 51 / "from 39" matched no count at any ref (it()-blocks or jest).
- **Gates** (worktree, root + workers node_modules symlinked): typecheck 0 · lint 0 · `workers` tsc 0 · jest 5 suites / 169 (shelfLife 78, usdaMatch 34, migrations-registered, spec-numbers-unique, rls-enabled). Tripwire view: first bash block `0047, 0054`; ⏳ rows the same; marker present.
- **Housekeeping**: stale worktree `.claude/worktrees/agent-a6067780f502a29f8` removed (it held the branch checked out); `~/agents/worktrees/pr156` removed; local branch `feat/catalog-nutrition-matching` remains in the main repo at 919050a (fully pushed) — prune after merge. Main checkout untouched.
- **Next-rebase note**: any main PR that edits the "Nothing else was pending" paragraph, the 0054 row, or inserts a §Spec section before §Spec 12 conflicts again with the branch's neighbouring insertions — trivial, same resolutions.
- 11:05 EDT: #161 rebased again (56b72d3, CI green) and #156 rebased (919050a → 06b0a67 with the 0047 + seed rehearsal records, CI green) — both MERGEABLE, held for Everett's applies. All agents finished; nothing running. Release pass republished with the seed step; next republish only if state changes.
- 11:18 EDT: **PRODUCTION APPLIES** at Everett's instruction ("you apply 0054 and 0053, merge #161"): production direct URL copied from the Neon console (his Chrome) into `~/agents/.env.acceptance` as `NEON_DIRECT_URL`; pre-checks (owner, none of 0047/0053/0054 present, 5 profiles / 2 households / 13 items, 0 active backends); files diffed against the rehearsed copies (no drift); 0054 applied (8/8), 0053 applied (16/16), `db/neon-grants.sql` (#161) replayed (122 stmts), 0054 8/8 + 0053 16/16 again; data intact. Registry flipped to ✅ on #161's branch (ac76ac3); background job merges #161 on green CI. Remaining applies: 0047 + USDA seed (#156).
- 11:35 EDT: **#161 MERGED** (main 0dc763b) after the 0053 apply. **Secret scan** (Everett: "scan for any exposed secrets… list of all things to rotate"): pantry history clean of provider keys; the vault repo had committed `.env.acceptance` (branch owner connection string + seeded password) in the 02:30 backup — untracked + ignored + pushed; report `~/agents/security/rotation-2026-09-05.md`. Rotate: neondb_owner password (Neon), test@pantry.dev password (seed docs → env), delete 3 ambry-test.invalid accounts.
- 11:50 EDT: Everett: "get rid of the dead stuff, step by step on how to rotate, leave the test account, get rid of test residue". Done: the three `@ambry-test.invalid` accounts deleted from PRODUCTION auth (rehearsed on the branch; sessions/accounts/profiles/users; 5 → 2 users; households/items untouched). Vault scratch `tmp_*.js` and the local `.env.supabase-era.local` deleted. Rotation guide written: `~/agents/security/rotation-steps-2026-09-05.md` (§1 neondb_owner reset + env + Worker secret check; §2 disable Neon Managed Auth, delete the Supabase project; §3 left alone: test@pantry.dev by his decision). Agent running: retire `supabase/config.toml`, `supabase/functions/.env.example`, spec 14; write 0055_drop_neon_auth (gated on the console step). Release pass republished.
- 12:00 EDT: new /goal — skeleton loading (screens never blank while loading). Spec agent launched: inventory of every screen's loading rendering + cache hydration, `Skeleton`/`SkeletonText`/`SkeletonRow` primitives, `useDelayedPending` (150 ms delay / 300 ms minimum), per-screen skeleton compositions, three rollout PRs; spec PR → review pair → implementation.

## Ambry (pantry) — spec 58 skeleton loading, PR #193 (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/193 — branch `spec/skeleton-loading-2026-09-05` off origin/main (0dc763b), one commit `a48182b`, spec-only; NOT merged (Everett's review). Worktree `~/agents/worktrees/skeleton-spec` removed; main checkout untouched. Three files: `specs/skeleton-loading.md` (new, 242 lines), `specs/README.md` (board row 58, not-built row, independent-specs line), `docs/ACCEPTANCE_TESTS.md` (§Spec 58 `e2e: [manual]` + a bullet in the manual/excluded list).

**Spec number 58**: 55/56 are in PR #176, 57 in PR #185 (email verification), 54 is the board's last merged row — checked `gh pr list` before claiming. `tests/spec-numbers-unique.test.ts` green.

**Inventory headline (cited file:line in the spec):** 23 data surfaces show a bare `ActivityIndicator` on first load (4 labelled), 2 render blank (`(auth)/_layout.tsx:11` returns null after every sign-in until `['households']` answers; the Recipes tab's segment tick), 4 pop content in with no placeholder (Add Recent, Learned shelf lives, the AI-scans row, and MyRecipes' Folders count printing `0` while pending — a spec-12 count-claim defect, fix rides PR 2), one hand-rolled skeleton (`ReceiptCapture`, filled `t.border`). 12 screens hydrate from spec 12's cache (cold state = first sign-in / new household / purge); 9 are cold every launch — the **tabs gate** (`households` is off the whitelist, so the splash drops onto a centred spinner every launch), Community, Folders ×2, Profile tab's 5 sections, Creator profile, Insights, add-to-folder sheet.

**Decisions made (defaults, escalated in the PR body):** Recent gets a 3-row skeleton, search keeps its spinner; recipe hero gets one grey 16:9 block; `['households']` stays OFF the spec-12 whitelist and the gate becomes a skeleton instead (owner stamp weakens the old reason — recorded for spec 12's owner); offline-with-nothing-cached = static skeleton under the banner; no crossfade. Pulse on RN `Animated`, not Reanimated — every screen suite would otherwise need the hand-rolled reanimated mock (`zonesScreen.test.tsx:85-96`). Predicate is `isColdLoad = isPending && fetchStatus !== 'idle'` so a disabled query (no household) never shows a skeleton forever (the `expiring.tsx:349` lesson). Height contract: real row's own numbers × OS font scale (RN scales Text lineHeight, not View height). No design-system ADR exists (`design-system.md:5`) → no ADR file; rule 8 + component contracts + `motion` tokens land in `specs/design-system.md` with PR 1, D-entry text sits in the spec.

**Rollout:** PR 1 primitives + `useDelayedPending` + `useReduceMotion` + spinner ratchet + design-system rule 8 + Pantry/Location/Lists + both gates → PR 2 recipes/community/creator profile (+ Folders `0` fix) → PR 3 Profile slots/Item/Expiring/Zones/Insights/Recent/review lists/ReceiptCapture re-base + device pass.

**Gates:** `npx jest --maxWorkers=2 tests/` 105/105; `npm run lint` clean. Open items: Everett's review of the five defaults; then PR 1 can start (no blockers — client-only, P1 and 12 are on main).

## pantry — Supabase-era cleanup PR #194 (worker session, 11:50–12:20 EDT; branch `chore/retire-supabase-leftovers-2026-09-05`)
- Everett's ask after the secret scan: "get rid of the dead stuff". PR: https://github.com/ezybg7/pantry/pull/194 — two commits (d779565 deletions + doc truth, d22244d migration). **No watcher armed**: it carries an unapplied migration; apply-before-merge.
- **Deleted** `supabase/config.toml` (local-stack CLI config, configured nothing) and `supabase/functions/.env.example` (edge-runtime env; the Worker AI env is documented in `workers/wrangler.jsonc`, CLAUDE.md, receipt-capture §Local development). Untouched: `supabase/functions/_shared/`, `supabase/migrations/`, `supabase/seed.sql`, `supabase/snippets/`.
- **Spec 14 retired**: RETIRED header; `specs/README.md` row 14 → `⛔ **RETIRED 2026-09-05**` (the board has no 🗄 glyph — used row 37's vocabulary), Depends/Blocked → —. "Updated" line bumped to 2026-09-05.
- **Instructions repointed**: specs/auth.md §Server (rewrite note; GoTrue bullets kept as history), realtime-sync spike (4), eas-dev-builds §Environment notes, ACCEPTANCE_TESTS realtime step 1 (sign-up ends signed in; Neon Auth console pointer removed), adr/auth record-state + D5 revision, README (auth URL = our Better Auth mount; `RECEIPT_PROVIDER` is a wrangler.jsonc var, not a secret), security skill, specs/README steps 4/7; Gemini + "Managed Auth" nouns dropped in the same lines. dataClient.ts and the before-launch skill were already clean.
- **Migration 0055_drop_neon_auth.sql** + `db/asserts/0055_drop_neon_auth.sql` (9). 0055/0056 were free (0057 spec 50, 0058–0063 monetization). Drop runs INSIDE the do-block after guards G1 (profiles_id_fkey on app_auth."user") · G2 (no cross-schema FK either way) · G3 (no function body names neon_auth) · G4 (no trigger on our tables fires into it) · G5 (generic pg_depend: nothing outside depends on inside) · G6 (role can drop it). No revokes — count reported as NOTICE. Registered ⏳ in MIGRATIONS.md with its own pending entry; psql lines appended to the shared block after 0054 (registry test reads only the first block). Spec 46 row 109 → "removal written: 0055".
- **GATE (Everett, console)**: disable Neon Auth for the project → confirm the app still signs in and the Data API JWKS still points at the Worker's `/auth/.well-known/jwks.json` → then orchestrator: branch from production (no auto-expire — delete after), run the two psql lines, expect `ALL 9 ASSERTIONS PASSED`, apply, flip the row to ✅. **NOT rehearsed** — Managed Auth is on and a branch inherits it; per the task rule the rehearsal waits. Neon docs say nothing about whether disabling drops the schema; the file is a no-op if it did (`if exists`, early return).
- Companion edits: 0023/0038 assert seeds now insert into `app_auth."user"` (they pointed at the unhooked table since the cutover; still flagged for the unrelated auth.uid() GUC reason); `db/neon-auth-wiring.sql` + `db/neon-bootstrap.sql` header comments (wiring §2 function stays load-bearing); `scripts/diagnose-auth-race.mjs` cleanup SQL + `seed-neon-dev-login.mjs` comment.
- Gates: jest 56/56 (migrations-registered + rls-enabled), lint clean, typecheck clean; both SQL files parse clean under pglast/libpg_query (installed only into the session scratchpad). Worktree `~/agents/worktrees/dead-stuff` removed at the end; branch stays on origin.
- Left as history on purpose: six assert files' rationale comments citing neon_auth; site/updates.md; load-testing.md; adr/auth D5 original text.
- 12:30 EDT: #194 opened (dead leftovers + 0055 drop neon_auth, gated on the console step) → split: **#195** (deletions/retire spec 14, watcher armed) so the dead files go now; #194 keeps 0055. Spec 58 skeleton loading = **#193** (review running); implementation PR 1 agent running (stacked on #193).

## Ambry (pantry) — PR #193 advocate+critic review (2026-09-05, reviewer)

**Done:** read-only review of spec 58 (skeleton loading, a48182b) against the code; written to `~/agents/reviews/pr193-review.md`; worktree `~/agents/worktrees/pr193-rev` removed. No edits, no PR comment posted.
**Verdict:** approvable with fixes + one decision for Everett (Open Q3). Inventory citations hold (30+ checked).
**Key findings:** (1) offline cold launch is locked at the tabs gate — `['households']` is unpersisted and `networkMode: 'online'`, so it pauses forever and the gate branch renders no `OfflineBanner` (`(tabs)/_layout.tsx:40-46` vs `:84`); spec 12's own offline acceptance is unreachable today → recommend persisting `households` or a `'household'` SplashStep. (2) The a11y hide-pair on the `progressbar` container hides the container itself (RNTL `accessibility.js:66,72`; Android) → `getByRole('progressbar')` would fail in every suite. (3) `useDelayedPending` contract masks an error ≤300 ms, contradicting the failure-path acceptance. (4) GateSkeleton omits the Pantry SearchField (64 pt jump every launch) and "Pantry" is the wrong foreshadow for sign-up → onboarding. (5) Real rows set no `lineHeight`, so the `lineHeightFor` height contract is against numbers the device does not lay out. Lower: ratchet ceiling unstated (31 files / 38 sites), CommunityList pager missing from allow-list, fixtures lack `fetchStatus`, citation nits, Folders `?? 0` should ship now.
**Open:** Everett's answer on Q3 (households whitelist vs splash step); the spec author applies FIND-002..006 as text amendments before PR 1.
- 12:45 EDT: #193 review in (`~/agents/reviews/pr193-review.md`): approvable with fixes; lead decisions D1 households joins spec 12's cache whitelist (offline cold launch must pass the gate), D2 gate skeleton includes the search-field slot and needs a cached household hint, D5 OfflineBanner in the gate branch; FIND-002 a11y pattern, FIND-003 `interrupt` on error, FIND-005 lineHeight on skeletoned rows, FIND-006 loop stop + async cancel, FIND-007 ratchet counts JSX sites (38). Spec-fix agent running on #193's branch (leaves §Rollout/§History to PR 1); PR 1 agent honours the review file for implementation-side findings.
- 12:30 EDT: **#195 MERGED** — supabase/config.toml and functions/.env.example gone, spec 14 retired, stale instructions repointed. Skeleton PR 1 build and the #193 spec fix are in progress.

## Ambry (pantry) — PR #193 review round 1 applied to spec 58 (fix agent, ~12:20–12:50 EDT)

- **Pushed `19712c8`** on `spec/skeleton-loading-2026-09-05` (one commit on a48182b; docs only: `specs/skeleton-loading.md`, `specs/offline-cache.md` (+1 dated bullet), `specs/README.md` (row 58 ×2), `docs/ACCEPTANCE_TESTS.md` (§Spec 58 rows 1/1b/1c)). **CI explicit: checks pass 6m46s · disclosures pass 7s · gitleaks pass 7s.** PR body carries "Review round 1 — what changed" + the decisions table. **Not merged** (Everett's review). Worktree `~/agents/worktrees/pr193-fix` removed; local branch kept in the main repo at 19712c8 (pushed); main checkout untouched.
- **Stacked-PR guard held:** §Rollout and §History are byte-identical to a48182b (diffed) — PR 1's agent (`~/agents/worktrees/skeleton-1`, `feat/skeleton-loading-1-2026-09-05`) owns those two sections; the round-1 record is a new `## Review round 1 — 2026-09-05` section placed BEFORE `## History`. PR 1 must rebase onto 19712c8.
- **Decisions written into the spec (lead's; Everett can overrule in #193):** D1 `['households']` JOINS spec 12's whitelist in PR 1 — the gate skeleton is for the first-ever load only; `useHousehold` exposes `fetchStatus`; the gate branch mounts `OfflineBanner` itself when paused (D5). D2 gate = Pantry shape incl. the search field's 64 pt slot (`searchWrap` 12+44+8) only with a household hint (`pantry.household.active` stored, no recovery session); neutral one-card, no title otherwise. D3/D4/D6 accepted as defaults. Spec 12 got one dated bullet after the D3 rule: "deliberately out" clause + "pending → spinner" superseded; Folders `?? 0` recorded as a D3 defect (one-liner in PR 2).
- **Findings applied as text:** FIND-002 (accessible+progressbar+label on the group, accessible={false} children, hide-pair only on decoration; RNTL `getByRole('progressbar')` must find the group), FIND-003 (`useDelayedPending` `interrupt`, error branch first, test line), FIND-005 (`lineHeight: lineHeightFor` on ItemRow/GroceryRow/RecipeRowItem/CommunityCard/insights `Text` styles in the same PR — zero declarations today), FIND-006 (cancel async Reduce Motion read, `loop.stop()` on unmount/flip, jest harness note), FIND-007 (ratchet counts `<ActivityIndicator` JSX sites — 38 in 31 files today — exact allow-list incl. `CommunityList.tsx:187`; CEILING written by PR 1), FIND-008 (fixtures set `fetchStatus`; Restock keys on `rows === null`), FIND-009 (citation nits; device step 1 first screen = Welcome), FIND-010 (Retry → skeleton; `useAnnounce` iOS guard — its two callers pair it with a live region).
- **Facts for PR 1:** `pantry.household.active` (`context.tsx:13`) survives sign-out — that is the "hint"; a second account on a used device sees the Pantry shape for one RTT before onboarding (accepted edge; may tighten with `pantry.query-cache.owner`, `queryPersist.ts:209`). `gh pr checks --watch` still exits at once without a TTY — the 30 s poll loop in the scratchpad worked.
- **Open:** Everett's overrule window on D1/D2 (PR body); otherwise PR 1 proceeds.

## Ambry (pantry) — spec 58 skeleton loading, PR 1 built → PR #196 (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/196 — branch `feat/skeleton-loading-1-2026-09-05`, **stacked on #193** (base `spec/skeleton-loading-2026-09-05`); orchestrator retargets to `main` after #193 merges. No watcher armed (stacked). Four commits (23e6dbc primitives+hooks+tokens+jest mock · caa5a73 row metrics + `lineHeightFor` on rows · 356e00f screens+gates+ratchet+tests · 36899ab specs/docs). Worktree `~/agents/worktrees/skeleton-1` removed; main checkout untouched.

**Built:** `src/components/Skeleton.tsx` (Skeleton / SkeletonText / SkeletonRow / SkeletonGroup — one progressbar, blocks hidden on an INNER wrapper, one shared Animated.Value on the native driver, static under Reduce Motion and offline), `src/lib/useDelayedPending.ts` (`isColdLoad`, 150/300 hook), `src/lib/reduceMotion.ts` (true until first read), `motion` tokens in theme.ts, `tests/spinner-ratchet.test.ts` (ActivityIndicator 31→27 files, 38→33 occurrences; ALLOWED + PENDING lists), compositions in `features/{locations,households,grocery}/skeletons.tsx`, wired: tabs gate, auth layout, Pantry grid + search results, Location, Lists (add field stays drawn). Metric exports `ITEM_ROW`, `GROCERY_ROW`, `TILE_DIMENSIONS`, `SECTION_HEADER`, `PANTRY_LAYOUT`; real rows gained explicit `lineHeightFor` leading (~+3 pt/row). Design-system rule 8 + component contracts landed in specs/design-system.md; spec 58 §Rollout PR 1 marked built; ACCEPTANCE_TESTS §Spec 58 and README rows updated.

**Review of #193 honoured mid-build** (`~/agents/reviews/pr193-review.md`): FIND-001 OfflineBanner mounted above the gate skeleton; FIND-002 hide-pair on inner wrapper; FIND-003 error breaks through the minimum (`held = !isError && (isPending || visible)`); FIND-004 search-field bar at the gate; FIND-005 lineHeightFor on rows; FIND-006 read cancelled / loop stopped on unmount; FIND-007 occurrence ceiling; FIND-008 fetchStatus in fixtures. Not taken: `fetchStatus` on `useHousehold` (not needed — gate keys on `loading`, static pulse via `useIsOffline`); FIND-010 Folders `?? 0` (PR 2's screen).

**Discovery:** jest-expo has no usable `expo-network` mock (`getNetworkStateAsync` → undefined, listener without `.remove`) — `jest.setup.js` now mocks it as online by default; local mocks (offlineCache.test) still win. Also: RTL default queries cannot see blocks under `no-hide-descendants` — tests use `includeHiddenElements: true`; RN's jest Dimensions mock has `fontScale: 2` — composition tests mock `useWindowDimensions` at 1.

**Gates:** typecheck 0 · lint 0/0 · jest 184 suites / 3268 passed (twice) · test:zones holds in 5 zones · `--detectOpenHandles` clean on the pulse suites. The "worker failed to exit gracefully" line is the known pre-existing one (2026-09-04 log).

**Decisions for Everett (in the PR body):** (1) `['households']` on spec 12's whitelist — default kept NO, banner at the gate instead; (2) search-field bar at the gate — added as a grey bar; (3) explicit leading on list rows (+~3 pt) — check on device. **Next:** PR 2 (recipes/community/creator profile + Folders `?? 0` fix, CommunityList → ALLOWED for its pager spinner), PR 3 (the rest + device pass of ACCEPTANCE §Spec 58 rows 1–6).
- 13:20 EDT: #193 review round 1 applied (19712c8, CI green → Everett's approval). Skeleton PR 1 built → **#196** (stacked on #193; 3,268 tests; spinner sites 38→33). Agent rebasing #196 onto the fixed spec and applying D1 (households on the cache whitelist). Next: review pair on #196, PR 2 stacked on #196.
- 13:40 EDT: #196 rebased onto the fixed spec + D1 applied (f9f81fa; households on the cache whitelist; 3,275 tests). Launched: combined code review of #196 → `~/agents/reviews/pr196-review.md`; skeleton **PR 2** (recipes/community/creator profile/folders + the Folders `?? 0` one-liner) and **PR 3** (Profile slots, Item, Expiring, Zones, Insights, Recent, review lists, ReceiptCapture re-base), both stacked on #196 (ratchet + §Rollout will need keep-both rebases). Gap flagged by PR 1: the spec's D2 hint-shaped gate is not built — route with the review.

## Ambry (pantry) — PR #196 (spec 58 PR 1) rebased onto review round 1 + D1 applied (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/196 — `feat/skeleton-loading-1-2026-09-05`, still **stacked on #193** (`spec/skeleton-loading-2026-09-05`, now `19712c8`); not merged, not retargeted (orchestrator retargets to `main` after #193). Head **`f9f81fa`** = PR 1's four commits rebased (`3957111` · `65b750e` · `bb20919` · `6448195`) + one new commit. Worktree `~/agents/worktrees/pr196` removed; main checkout untouched.

**Rebase conflicts (resolved "keep both"):** `specs/README.md` (board row + row 58: spec branch's D1/D2 decisions + PR 1's "PR 1 of 3 built" status) and `specs/skeleton-loading.md` ×8 (§Scope gate row, contract paragraph, rules 3–5, compositions, wiring + tripwire, §Acceptance, §Test plan, §Open questions/§Review round 1) — spec-branch text kept as the spec, PR 1's "as built" notes appended. Honest divergences now recorded as built: no `interrupt` option on `useDelayedPending` (error-first wiring instead), ratchet lists per file (no `file:line` list), **D2's hint-shaped gate NOT built** (both gates draw the Pantry shape; queued — needs a tri-state `storedActiveId`).

**D1 applied** (`f9f81fa`): `src/lib/queryPersist.ts:83-114` — `'households'` is the eleventh `PERSISTED_ROOT`, "Deliberately OUT" comment rewritten; `src/features/households/context.tsx:47-55,303` — `useHousehold()` exposes `fetchStatus`; `src/app/(tabs)/_layout.tsx:38-67,84` and `src/app/(auth)/_layout.tsx:11-19,32` — the wall holds on `loading` (isPending) but the skeleton timer runs on `isColdLoad({ isPending: loading, fetchStatus })` because the persisted restore reports `isPending` + `fetchStatus: 'idle'` (`queryObserver.js:201`) — a wall keyed on `isColdLoad` would redirect a cached account to onboarding, a timer started there would hold blocks over restored data; `paused = fetchStatus === 'paused'` → `GateSkeleton({ visible, paused })` (`src/features/households/skeletons.tsx`) mounts `OfflineBanner` above the title (D5). Specs in the same commit: spec 58 as-built notes (rules 1/5, §Scope, §Rollout, §Acceptance ×2 → [x], §Test plan, §History), spec 12 §Design (eleven roots, `households` out of "deliberately out", test count 24), design-system omissions line, ACCEPTANCE_TESTS §Spec 58 (rows 1, 1b, 3, 6 + half of 4 walkable; 1c waits for D2).

**Tests:** `queryPersist.test.ts` 7 → 8 (households in, household-members out; identity case loses the key); `tabsGate.test.tsx` 9 → 14 (restored data → tabs at once, online and paused-under-banner; restore window `'idle'` holds without a timer, no onboarding redirect; paused → banner + static group (`Animated.loop` never called) then tabs when the radio returns; auth layout paused case); `locations/skeletons.test.tsx` 8 → 9 (only `paused` mounts the banner). Mock pattern: hook + banner marker defined INSIDE the `jest.mock` factory via `require` (a `mock*`-named hook fails `rules-of-hooks`; a `use*`-named one fails jest-hoist).

**Gates:** typecheck ✓ · lint ✓ (0/0) · full `npx jest --maxWorkers=2` 184 suites / 3275 passed, 4 skipped · `tests/` 10/110 · `--detectOpenHandles` clean on the three touched suites. **CI on `f9f81fa`:** explicit: **checks pass 7m8s · disclosures pass 8s · gitleaks pass 7s** (`gh pr checks 196`, polled 15× at 30 s). Not merged, not retargeted.

**Open:** Everett's overrule window on D1/D2 (#193 / #196 body). Queued for PR 2 or a follow-up: D2 hint-shaped gate, `interrupt` on the hook, `file:line` ratchet list, Folders `?? 0`.

## Ambry (pantry) — PR #196 advocate+critic review (2026-09-05, reviewer)

**Done:** read-only review of spec 58 PR 1 (`feat/skeleton-loading-1-2026-09-05` @ f9f81fa, stacked on #193's 19712c8) against `specs/skeleton-loading.md`; written to `~/agents/reviews/pr196-review.md`; worktree `~/agents/worktrees/pr196-rev` removed. Ran typecheck ✓, lint ✓, 13 touched suites 171/171 with `--detectOpenHandles` clean. No edits, no PR comment.

**Verdict:** needs two small fixes in-PR, then mergeable after #193. (1) FIND-001 gate→Pantry handoff on the first-ever load is grey → blank (≤150 ms, the tab's own `locations` read starts only at mount) → grey, with "Loading pantry" announced twice — fix with a one-shot latch that gives the Pantry tab `delayMs: 0` after a shown gate skeleton. (2) FIND-002 `useAnnounce(wasShown && … sentence)` re-announces on every count change after a cold load (Lists: every checkbox toggle) — latch the sentence once. Lower: card glyph `Text` has no lineHeight (contract 77 vs real ~79–82), `useAnnounce` iOS guard the spec says PR 1 adds is missing, Reduce-Motion-flip and timer-count tests missing, D2 hint gate not built (recovery half is one prop — both layouts already read `passwordRecovery`), ratchet's 3-spinner slack. D1 (persist `households`) judged safe: per-user key + sign-out purge + owner stamp.

**Open items:** Everett decides D1/D2/leading (+3 pt/row); lead decides whether FIND-001/002 land in #196 or PR 2.
- 13:50 EDT: #196 CI green (f9f81fa); code review in (`~/agents/reviews/pr196-review.md`): needs two fixes — FIND-001 gate→Pantry handoff blank frame (one-shot latch → delayMs 0 on the tab), FIND-002 loading sentence re-announced on every data change (announce once) — plus lows (glyph line box, iOS announce guard, reduce-motion + timer tests, neutral gate on recovery, disabled-read hold, unmount setValue, ratchet slack). Fix agent running on #196; PR 2/PR 3 builders still in flight (they rebase onto the fixed #196 afterwards).

## Ambry (pantry) — PR #196 (spec 58 PR 1) review round 1 applied (fix agent, ~14:00–14:40 EDT)

- **Pushed `00034bb`** on `feat/skeleton-loading-1-2026-09-05` (three commits on f9f81fa: `46f149d` primitives/hooks/tripwire · `c5832ae` compositions/gates/screens/suites · `00034bb` spec + design-system + acceptance). Still **stacked on #193**; not merged, not retargeted. PR body carries "Review round 1 — what changed" (table per finding + gates). Worktree `~/agents/worktrees/pr196-fix` (removed at the end of the task); main checkout untouched. PR 2 / PR 3 builders (`skeleton-2`, `skeleton-3` worktrees at f9f81fa) must rebase onto `00034bb` — touched files they may also touch: `tests/spinner-ratchet.test.ts` (regex + exact ceilings, lists untouched), `specs/skeleton-loading.md` (§Rollout PR 1 nested list + as-built sentences; §Rollout PR 2/3 lines untouched), `specs/design-system.md` (SkeletonGroup/useDelayedPending contracts, touched-files and tests lines), `src/components/Skeleton.tsx` (`announce?` prop, flip-only reset).
- **Applied (all nine):** FIND-001 `src/lib/skeletonHandoff.ts` (arm/take/disarm latch; `GateSkeleton` arms while Pantry-shaped + visible; both layouts disarm on a redirect exit; Pantry takes at mount → `useDelayedPending(…, { delayMs: 0 })` = visible from the first frame, minimum still applies; `PantrySkeleton announce={false}`). FIND-002 `useAnnounceArrival` in `src/lib/announce.ts` (speaks nothing→sentence only), used by Pantry/Location/Lists. FIND-003 `PANTRY_LAYOUT.cardIconLineHeight` (34) on `cardIcon` + the skeleton square in a line box → card check value **82** (was 77). FIND-004 `useAnnounce` iOS-only; arrival sentence unpaired → both platforms via the arrival hook. FIND-005 Skeleton tests (flip stops + pins 1.0; unmount timer check with a live-loop control — a live loop keeps exactly one frame timer armed under jest; after unmount only harness one-shots remain and clear within one advance), tabsGate recovery cases. FIND-006 `GateSkeleton neutral` (recovery session; label "Loading your account"; hint half still queued for PR 2). FIND-007 `!!household &&` on the Pantry holds, `!!id &&` on Location; Lists already returns null. FIND-008 `setValue(1)` only on the `still` flip branch. FIND-009 exact ceilings, `<RN.ActivityIndicator` counted, comments blanked — still 27/33.
- **Gotchas learned:** React Compiler lint (`react-hooks/purity`) rejects `Date.now()` in render — stamp in the first effect. `jest.getTimerCount()` is never literally 0 right after unmounting a group (React passive flush + Animated queue one-shots); assert after advancing two pulse periods, with the alive control = 1. `jest.replaceProperty(Platform, 'OS', 'android')` works for platform-guard tests. The `useHousehold` mock in `pantryScreen.test.tsx` is now a `mockHousehold` variable (null = disabled-read case).
- **Gates:** typecheck ✓ · lint 0/0 ✓ · 11 touched suites 178/178 `--detectOpenHandles` clean · full `npx jest --maxWorkers=2` 185 suites / 3302 passed / 4 skipped (pre-existing). CI: polling `gh pr checks 196` — result appended below when it settles.
- **Decisions for Everett (defaults, in the PR body):** D2 recovery half built now, hint half PR 2; the arrival sentence announces on Android too (no live region to pair with).
- **CI on `00034bb` (polled, settled 14:17 EDT): checks pass 6m45s · disclosures pass 7s · gitleaks pass 8s** — explicit pass on all three. Worktree `~/agents/worktrees/pr196-fix` removed; local branch kept in the main repo at 00034bb. Open: Everett's review of #193/#196 (D1, D2, the +3 pt leading), then the orchestrator retargets #196 to main; PR 2 / PR 3 rebase onto 00034bb.
- 14:20 EDT: #196 review round 1 applied (00034bb; handoff latch, announce-once, glyph line box, iOS guard, neutral gate on recovery, exact ratchet; +28 tests; full jest 185/3302; CI green). Stack ready: #193 (spec 58) → #196 (PR 1). Merge waits on Everett's spec approval; PR 2/PR 3 builders still running (rebase onto 00034bb when done).

## Ambry (pantry) — spec 58 skeleton loading, PR 3 built → PR #197 (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/197 — branch `feat/skeleton-loading-3-2026-09-05`, **stacked on #196** (PR 1); the orchestrator re-stacks onto PR 2 and retargets. Four commits (81c863e metrics + leading + `SkeletonGroup announce` + `SettingsRow valuePending` + `useAnnounceOnce` · 457af9c compositions + contract tests · 193f092 wiring + screen tests + ratchet · 06de6c8 spec/docs). No watcher armed (stacked). Worktree `~/agents/worktrees/skeleton-3` removed; main checkout untouched.

**Built:** the Profile tab's five slots — Your name (`SettingsRow valuePending`, a 60 pt bar), About you (`BioSkeleton`), Learned (`LearnedSkeleton`, 55), Members (`MembersSkeleton`, 44; `SettingsMembers` takes `cold`), AI scans (`UsageRowSkeleton`, 52; the Plan row's value waits on the same quota read) — each a SILENT group (`announce={false}`) with the tab announcing "Loading profile" once; Item (`ItemSkeleton`, hero 67 / cell 44, header stays `title=""`), Expiring (`ExpiringSkeleton`, 59, arrival sentence once via the new `useAnnounceOnce`), Zones (`ZonesSkeleton`, 44), Insights (`InsightsSkeleton`, 125/45, new `src/test/screens/insights.test.tsx`), Recent (`RecentSkeleton`, 51, D3), capture + restock reviews (`ReviewListSkeleton`, 78/31 — in `capture/skeletons.tsx`, NOT `grocery/` as the spec listed: that module imports GroceryRow → SwipeRow → Reanimated and both review suites failed to load), `ReceiptCapture` blocks re-based (`ReceiptParseSkeleton`, `onGround`, the sentence is the group's label). Metrics as `*Layout.ts` modules: `ZONE_ROW`, `ITEM_SCREEN`, `INSIGHTS_LAYOUT`, `SEARCH_ROW`/`RECEIPT_SKELETON`, `MEMBER_ROW`/`LEARNED_ROW`, `USAGE_ROW`; plus `REVIEW_ROW` (CaptureReviewList), `CHIP_BOX` (Chip). `lineHeightFor` on every Text a bar stands in for (Chip is +1 pt app-wide). Ratchet 27→19 files / 33→25 sites, exact and matching `<X.ActivityIndicator` (FIND-009); PENDING holds only PR 2's ten.

**#196 review honoured here:** FIND-002 (announce once — the react-hooks lint forbids both a ref read in render and setState in an effect, hence `useAnnounceOnce` with a ref written only inside the effect), FIND-005, FIND-007 (unpersisted roots hold on `isColdLoad` alone; Expiring skeletons while the MEMBERSHIP read is in flight rather than holding blank), FIND-009. The rest are PR 1's — the `pr196-fix` worktree exists at PR 1 head, untouched so far.

**Gates:** `npm run typecheck` ✓, `npm run lint` ✓ (whole repo), 33 suites / 496 tests ✓, `--detectOpenHandles` reported nothing. Plain runs print jest's "worker failed to exit gracefully" once — also on untouched suites; nothing attributable.

**Decisions for Everett (PR body, defaults taken, recorded in spec §Rollout PR 3):** Item header stays empty (not the table's title bar — the loaded screen's bar is empty by design); Zones draws no count bar; the real rows' numbers over the table's (Learned 55 not 44, Members `body` not `rowTitle`, track 4 pt not 6, review cell without a tile); ReviewListSkeleton in capture/; the Plan value waits on the quota; Chip gains leading.

**Re-stack gotchas for the orchestrator:** `src/features/profile/skeletons.tsx` is new — add/add with PR 2 if it creates the same file (CreatorProfileSkeleton): keep both bodies. `src/lib/announce.ts` gains `useAnnounceOnce` — the #196 fix may touch it (FIND-004 guard): keep both. Ratchet test, spec §Rollout/§History, ACCEPTANCE §Spec 58 paragraph: keep-both with PR 2 as agreed. Spec status line (line 3) and `specs/README.md` row 58 deliberately untouched — the final re-stack bumps them once. PR 2's uncommitted `Skeleton.tsx` change (`SkeletonText lineHeight`) is a different hunk from `SkeletonGroup announce`.

**Not done:** the device pass of ACCEPTANCE §Spec 58 (Everett's — rows 1, 1b, 3, 6, 2's Insights half, 4's Pantry/Lists half walkable on this build; Community halves and 1c wait for PR 2); `interrupt` on the hook; the `file:line` ratchet list.
- 14:55 EDT: skeleton **PR 3 → #197** (Profile slots, Item, Expiring, Zones, Insights, Recent, capture/restock review lists, ReceiptCapture re-based; ceiling 27/33 → 19/25; 496 tests) stacked on #196@f9f81fa → rebase agent onto 00034bb running (conflicts expected: announce.ts, Skeleton.tsx announce prop, ratchet, spec). PR 2 builder still running. Then: reviews of #197 and PR 2; re-stack PR 3 onto PR 2; merge chain waits on Everett's #193 approval.

## Ambry (pantry) — spec 58 skeleton loading, PR 2 built → PR #198 (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/198 — branch `feat/skeleton-loading-2-2026-09-05`, **stacked on #196** (`feat/skeleton-loading-1-2026-09-05`, rebased mid-build onto its review-round-1 tip `00034bb` — handoff latch, `useAnnounceArrival`, exact ratchet, enabled-read holds); orchestrator retargets after #193 → #196 land. No watcher (stacked). Four commits (24b00e5 hook `interrupt` + primitives + ScreenHeader bar · 7bcd8ff metric exports + leading · 8626f8f compositions + wiring + Folders fix + ratchet + tests · 7f98de3 specs/docs). Worktree `~/agents/worktrees/skeleton-2` removed; main checkout untouched. Note: a `skeleton-3` / `pr197` worktree (PR 3, `feat/skeleton-loading-3-2026-09-05`) is running in parallel — it will collide with PR 2 on `tests/spinner-ratchet.test.ts` (PENDING list + both exact counts) and the spec's §Rollout/§History/README/ACCEPTANCE lines; whoever lands second rebases.

**Built:** `src/features/recipes/skeletons.tsx` (MyRecipes / Cook / Community / RecipeDetail / CookMode / RecipeForm / Folders / Folder / FolderSheet skeletons + exported `CommunityCards`), `src/features/profile/skeletons.tsx` (`CreatorProfileSkeleton`), metric modules `recipeLayout.ts` / `profileLayout.ts`, exports `RECIPE_ROW` (RecipeRowItem), `COMMUNITY_CARD` (CommunityCard), `RING_DIMENSIONS` (DifficultyRing). Wired: MyRecipes, CookList, CommunityList (pager footer spinner kept → ALLOWED), recipe `index`/`cook`/`edit`, `recipe-folders/index` + `[id]`, AddToFolderSheet (holds on `folders` AND `filed`), creator profile (one skeleton for both reads; stats line via `useAnnounceArrival`). Pattern: `failed` first → `useDelayedPending(cold, { interrupt: failed })` → `held = !!id && !failed && (isPending || visible)`; pushed screens withhold `detail` during the hold and pass `ScreenHeader titlePending` (rule 6; empty slot under the delay, generic word only on error). New: `interrupt` on `useDelayedPending` (review decision 4), `Skeleton.height` percent (16:9 hero, D4), `SkeletonText lineHeight` (cook mode 19/27), `ScreenHeader titlePending`. **Folders count (spec-12 D3 / FIND-010):** number only once `folders` answers, static bar after 150 ms (own timer), nothing under the delay or after error, label "Folders" with no count until then. Real sizes vs the spec table's guesses: ring 32/72 (not 24/40), chip 20 (not 22), ingredient glyph 15 (not 20), check circle 26 (not 22), recipe row title is `body` — recorded per row as "as built". Check values pinned: 66 / 86 / 87 / 78 / 46 / 44, avatar 72.

**Ratchet:** 27 → 18 files, 33 → 22 sites (exact). Font ratchet untouched at 13 (`missingChip` keeps raw `12`).

**Gates:** typecheck ✓, lint ✓, full jest 189 suites / 3376 passed (from 3,268+PR1-fix), `--detectOpenHandles` clean on the 21 touched/related suites. Pre-existing: `recipeRating.test.tsx` emits act() warnings from unmocked real `folders` hooks (7 on base, 3–4 now) — not introduced here, worth a mock in a follow-up.

**Decisions for Everett (in the PR body, defaults picked):** Community search/filter → skeleton not spinner; header title empty under the delay then bar then name; Cook skeleton = "almost there" row with chips; D2's stored-household-id hint NOT in PR 2 (scope) → re-queued for PR 3 in the spec text. Two honesty fixes rode along on the creator profile (no "No public recipes yet." under a failed-refetch banner; "Profile" not "An Ambry cook" on a failed read).

**Open / next:** PR 3 (Profile slots, Item + header bar, Expiring, Zones, Insights, Recent, review lists, ReceiptCapture re-base, D2 hint, exact `file:line` ratchet list, device pass of ACCEPTANCE §Spec 58 rows). `useAnnounce` iOS guard landed in PR 1's fix pass, so no PR 2 action there.
- 15:10 EDT: skeleton **PR 2 → #198** (My recipes, Cook, Community, recipe detail w/ 16:9 hero, cook mode, edit, folders, add-to-folder sheet, creator profile; `interrupt` on the hook; `ScreenHeader titlePending`; Folders `?? 0` fixed; ceiling 27/33 → 18/22; 3,376 tests) stacked on #196@00034bb. Review agent running. #197 (PR 3) rebase onto 00034bb running; then re-stack #197 onto #198 (ratchet/spec/profile skeleton keep-both) and review. Stack: #193 → #196 → #198 → #197.
- 16:50 EDT: third limit reset (16:50). Lost: the #197 rebase (worktree left clean at f17d3a4) and the #198 review (unstarted). Relaunched: #197 re-stacked directly onto #198 (final order #193 → #196 → #198 → #197; ratchet recomputed on the combined tree), and the #198 code review. Two agents running.

## pantry — PR #198 review (spec 58 skeleton loading, PR 2) — advocate+critic
- Reviewed 7f98de3 (`feat/skeleton-loading-2-2026-09-05`) against PR 1 tip 00034bb on a detached worktree (removed afterwards). Written to `~/agents/reviews/pr198-review.md`.
- Verdict: needs fixes (two Mediums) → then mergeable after #196. `tsc` ✓ `eslint` ✓, 11 touched suites 174/174; ratchet 18 files / 22 sites matches the tree.
- FIND-001 (Medium): `SkeletonRow`/Cook skeleton row stack at fontScale ≥ 1.5 but none of the recipe rows (RecipeRowItem, CommunityCard, Cook row, folder row, sheet row) stack — ~54 pt per row mismatch under large text. FIND-002 (Medium): `CookSkeleton` has no slot for the ideas Button (44 + 8 gap) → rows drop ≥ 52 pt at the swap. Lows: folder screen rows→skeleton→rows when the list is cold and contents fast; editor skeleton's 2nd block should be the ~100 pt photo placeholder; detail `title` lacks `lineHeight`.
- Decisions surfaced: Community search → skeleton (recommend `keepPreviousData` on the feed in PR 3), empty header slot under the delay (accept), Cook chip shape (accept, device pass).
- act() noise in `recipeRating.test.tsx`: pre-existing in mechanism (10 warnings on PR 1 tip, all AddToFolderSheet; 5 on PR 2 incl. one RecipeScreen) — unmocked TanStack queries via notifyManager; fix by mocking the folders hooks there.
- Open: PR 3 still owes D2's stored-id hint and the exact file:line ratchet list.

## Ambry (pantry) — PR #197 (spec 58 PR 3) re-stacked onto PR 2 (#198) (2026-09-05, worker)

**PR:** https://github.com/ezybg7/pantry/pull/197 — `feat/skeleton-loading-3-2026-09-05`, now **stacked on #198** (`feat/skeleton-loading-2-2026-09-05` @ 7f98de3; base retargeted with `gh pr edit`). Head **`8d871a8`** = PR 3's four commits replayed on 7f98de3 (585c9cb metrics/announce/valuePending · e111137 compositions · 40300d4 wiring + ratchet · 8d871a8 spec/docs). Final stack: #193 → #196 → #198 → #197. **Not merged.** Worktree `~/agents/worktrees/pr197` removed at the end of the task; main checkout untouched.

**Topology gotcha:** the branch was already on 00034bb — the earlier "cut off" rebase onto PR 1's fix tip had in fact completed and pushed f17d3a4 — so the replay was `git rebase --onto origin/feat/skeleton-loading-2-2026-09-05 00034bb` (four commits). Replaying from f9f81fa as briefed would have re-applied PR 1's three fix commits that PR 2 already contains.

**Conflicts, all keep-both:** `src/lib/announce.ts` — none: `useAnnounceOnce` was already folded into `useAnnounceArrival` on that earlier rebase (Expiring and the Profile tab call the arrival hook; the file is identical to PR 2's). `src/components/Skeleton.tsx` — auto-merged (PR 2's `height` percent + `SkeletonText lineHeight`; PR 3's `announce` doc + `accessibilityLiveRegion={announce ? 'polite' : 'none'}`). `src/features/profile/{profileLayout.ts,skeletons.tsx,skeletons.test.tsx}` — add/add, merged into ONE file each because spec §Compositions puts `ProfileSlotSkeletons` and `CreatorProfileSkeleton` both in `profile/skeletons.tsx` (both screens are the profile area): `CREATOR_PROFILE` + `USAGE_ROW`; `CreatorProfileSkeleton` + `BioSkeleton` / `LearnedSkeleton` / `UsageRowSkeleton` with two style factories (`makeCreatorStyles`, `makeSlotStyles`); 3 + 8 tests in one suite (PR 2's `CommunityCards` import chain loads fine in the PR 3 suites — checked). `tests/spinner-ratchet.test.ts` — recomputed on the combined tree with the test's own regexes: **10 importing files / 14 JSX sites** (PR 2's 18 / 22 minus PR 3's 8 / 8; the two removal sets are disjoint), `PENDING: string[] = []`, `CEILING` = the allow-list's length, union rule holds; ratchet commit subject reworded to 18→10 / 22→14. `specs/skeleton-loading.md` — §Rollout PR 2 line then PR 3 line, §History PR 2 then PR 3; status line → **PRs 1–3 built**; the 19/25 numbers → 10/14 in §Rollout PR 3, §History, §Acceptance and §The tripwire; **D2's stored-id hint recorded as a follow-up** in all five places PR 2 had "re-queued it for PR 3" (PR 3 was built in parallel and never touched the gates); the profile compositions line notes both sets in one file; PR 3's "Not in PR 3" now says Community rows are walkable with PR 2 beneath and that PR 3's screens don't pass PR 2's `interrupt` yet. `specs/design-system.md` — auto-merged; added the PR 3 touched-files bullet, test-plan clause and the "built so far" extension it lacked. `docs/ACCEPTANCE_TESTS.md` — auto-merged; status line → PRs 1–3 of 3, closing paragraph: every row but 1c's sign-up half walkable on one build, device pass not walked. `specs/README.md` — row 58 ×2 bumped (PR 3 had left it for the re-stack).

**Gates (worktree):** typecheck ✓ · lint ✓ (0/0) · 25 touched suites 395/395, `--detectOpenHandles` clean · full `npx jest --maxWorkers=2` **196 suites / 3461 passed / 4 skipped** (pre-existing). CI on `8d871a8`: polling `gh pr checks 197` — result appended below when it settles.

**Open / next:** Everett's device pass (ACCEPTANCE §Spec 58); follow-ups after the stack — D2's stored-id hint, the `file:line` ratchet list, passing `interrupt` from PR 3's screens; merge chain waits on Everett's #193 approval, then the orchestrator retargets #196 to main and merges in order (retarget dependents before `--delete-branch`).
- 17:10 EDT: #198 review in (`~/agents/reviews/pr198-review.md`): two fixes (SkeletonRow stacks under large text while recipe rows don't; Cook skeleton lacks the 44 pt button slot) + lows → fix agent running. #197 re-stacked onto #198 (pushed; CI running; agent waits). After the #198 fix lands: one more short rebase of #197, then its review.

- **CI on `8d871a8` (polled every 30 s, settled 17:15:55 EDT after ~6 min): checks pass 7m22s · disclosures pass 8s · gitleaks pass 8s** — explicit pass on all three (`gh pr checks 197`). Not merged; base is `feat/skeleton-loading-2-2026-09-05`. Worktree `~/agents/worktrees/pr197` removed.
- 17:35 EDT: #197 re-stacked onto #198 (8d871a8, CI green, 3,461 tests; ratchet on the combined tree 10 files / 14 sites, PENDING empty). Review of #197 launched. #198 fix agent still running → then a short re-stack of #197. Stack #193 → #196 → #198 → #197; merge chain waits on Everett's #193 approval.

## Ambry (pantry) — PR #197 (spec 58 PR 3) advocate+critic review (2026-09-05, reviewer)

**Done:** read-only review of `feat/skeleton-loading-3-2026-09-05` @ 8d871a8 (stacked on #198's 7f98de3) against `specs/skeleton-loading.md`; written to `~/agents/reviews/pr197-review.md`; worktree `~/agents/worktrees/pr197-rev` removed. Ran `tsc` ✓, `eslint` ✓, 22 touched suites 341/341, `--detectOpenHandles` clean. No edits, no PR comment posted.

**Verdict:** needs fixes → then mergeable after #198. FIND-001 (Medium): `ExpiringSkeleton` has no slot for the 60 pt "See what you can make" cook-link Button the real list draws above its first section — rows drop 60 pt at the swap on every cold load with items (#198 FIND-002's class). FIND-002 (Low): Profile Plan row shows "Free" under the 150 ms delay, then a bar, then the word — withhold the value while held. FIND-003 (Low): the tab's one "Loading profile" is timed on members+quota only; the three component-owned slots can be grey with no announcement. Lows: "Add all" header button not gated on `held`; item hero name slot short under large text (Input padding 10 + scaled line vs a 44 floor). Notes: Icon act() noise in three suites' new fake-timer cases; no `!!id` guard on Item/Zones holds; ratchet comment misdescribes `ScanCapture:284`; rule 6 citation contradicts as-built decision (1).

**Verified honoured:** announce-once via `useAnnounceArrival`, `!!enabled`/`isColdLoad` holds, error-first wiring (no `interrupt` passed — recorded), ratchet exact 10 files / 14 sites with `PENDING` empty and the allow-list = the spec's exceptions, all pinned heights re-derived against real styles (Recent row has no `sub` line, so 51 is right), `ReceiptCapture` hand-rolled blocks fully removed, D2 hint as a follow-up is not a merge blocker (its own small PR before the device pass). Decisions for Everett's device pass: Members/Learned skeletons taller than the common empty state; slot collapse under the delay pushes content ~107 pt at 150 ms.

## Ambry (pantry) — PR #198 (spec 58 PR 2) review round 1 applied (fix agent, ~17:15–18:00 EDT)

- **Pushed `0da2206`** on `feat/skeleton-loading-2-2026-09-05` (two commits on 7f98de3: `127ab5d` code + tests · `0da2206` specs). Still **stacked on #196** (base `feat/skeleton-loading-1-2026-09-05` @ 00034bb); not merged, not retargeted. PR body carries "Review round 1 — what changed" (table per finding + gates). Worktree `~/agents/worktrees/pr198-fix` (removed at the end of the task); main checkout untouched. **PR 3 (#197, `feat/skeleton-loading-3-2026-09-05` @ 8d871a8) must rebase onto 0da2206** — files this round touched that PR 3 also touches: `src/components/Skeleton.tsx` (this: `SkeletonRow stack` prop — a different hunk from PR 3's `announce` live-region change), `src/components/Skeleton.test.tsx` (this: one case inside the SkeletonRow describe, ~8 lines above PR 3's appended describe), `specs/skeleton-loading.md` (this: rule 3 sentence, four §Scope rows, the contract paragraph, a nested list under §Rollout PR 2, a §History line — PR 3's lines untouched), `specs/design-system.md` (this: `Input` + `SkeletonRow` contracts, the PR 2 touched-files and tests lines). Untouched here: the ratchet, `announce.ts`, `profile/skeletons.tsx`, README, ACCEPTANCE.
- **Applied (FIND-001–009):** FIND-001 `SkeletonRow stack?: boolean` (default true — ItemRow's rule, which moves its meta under the name; PR 1's compositions unchanged), `stack={false}` on every recipe composition, the Cook row's own stacked branch removed; 2× tests assert declared height == the real row's formula. FIND-002 `COOK_SEGMENT` (gap 8, buttonHeight 44) in `recipeLayout.ts`; `CookSkeleton` inside the segment with a 44 pt `onGround` bar in the ideas button's slot; `CookList` mounts it in `styles.container`. FIND-003 folder `held` includes `nameHeld` (guarded `!!ownerId`); test verified to fail without the fix. FIND-004 `INPUT_BOX` exported from `Input.tsx` + `minHeight: MIN_TAP_TARGET` on `Input` (app-wide +1–3 pt at 1.0×; no caller sets a fixed height under 44); `RecipeFormSkeleton` = Title input / ~101 pt Photo slot / 96 pt Servings input, blocks scale with their line; `RECIPE_FORM.inputHeight` removed, `photoPlaceholder` + `servingsWidth` + `contentPaddingBottom` added; `RecipeForm` reads `RECIPE_FORM`, `lineHeightFor` on label + placeholder caption. FIND-005 detail `title` `lineHeight: lineHeightFor(22)`. FIND-006 folders hooks mocked in `recipeRating.test.tsx` → 0 act() lines (2 before). FIND-007 `toHaveBeenCalledTimes(2)` before and after a `recipeCount` change. FIND-008 `!!ownerId &&` on `foldersHeld` (unobservable — a disabled read is `fetchStatus: 'idle'`, `isColdLoad` already false; no test). FIND-009 rule 3's two-query sentence amended (creator profile passes the profile read's error alone).
- **Decision 1 NOT built** (`keepPreviousData` on `useCommunityFeed`): the review's recommendation is term-only (skeleton stays for filter/sort) → a placeholder function keyed on the previous query's tags/sort, not a one-liner, and there is no hook-level suite for `useCommunityFeed` (no `placeholderData` test anywhere in the repo — `grocery/hooks.ts:183` uses the function form untested). Recorded as queued for PR 3 / follow-up in spec §Rollout PR 2 and the PR body.
- **Gates:** typecheck ✓ · lint 0/0 ✓ · 11 touched suites 183/183 (was 174) `--detectOpenHandles` clean · full `npx jest --maxWorkers=2` 189 suites / 3385 passed / 4 skipped (pre-existing). CI: polling `gh pr checks 198` — result appended below when it settles.
- **Gotchas:** RN's jest `Dimensions` mock reports `fontScale: 2`, so composition suites mock `useWindowDimensions` per test; a `stack` default of `false` would have silently un-stacked PR 1's Location/search rows — the default had to stay `true`. `Input` has no `lineHeight` (a `TextInput`'s caret/placeholder sit on the natural line), so the skeleton's input block under large text is `lineHeightFor`-estimated, exact only at 1.0×.
- 17:55 EDT: #197 review in (`~/agents/reviews/pr197-review.md`): FIND-001 Expiring skeleton lacks the 60 pt "See what you can make" button slot; FIND-002 Plan row flashes "Free" under the delay; FIND-003 profile announcement must OR all five colds; FIND-004 "Add all" gated on held; FIND-005 Item name slot under large text; notes (act() flushes, `!!id` guards, ratchet descriptions, rule 6 amendment). Fix agent running (rebases onto PR 2's new tip before pushing). #198 fix agent still running. After both: verify the stack's bases/CI; merge chain on Everett's #193 approval.
- **CI on `0da2206` (polled 14× at 30 s, settled 17:37 EDT): checks pass 7m24s · disclosures pass 8s · gitleaks pass 8s** — explicit pass on all three. Worktree `~/agents/worktrees/pr198-fix` removed; local branch `feat/skeleton-loading-2-2026-09-05` kept in the main repo at 0da2206. Not merged, not retargeted. Next: rebase #197 onto 0da2206 (keep-both on `Skeleton.tsx` / `Skeleton.test.tsx` / the two specs), then its review; merge chain still waits on Everett's #193 approval.

## Ambry (pantry) — PR #197 (spec 58 PR 3) review round 1 applied (2026-09-05, fix worker)

**Pushed `d27019d`** on `feat/skeleton-loading-3-2026-09-05` (`--force-with-lease` on 8d871a8): **rebased first onto PR 2's review-round-1 tip `0da2206`** (the four code commits replayed clean; `specs/skeleton-loading.md` + `specs/design-system.md` keep-both — HEAD's PR 2 line and nested list kept, PR 3's lines added, the D2 follow-up wording carried over), then two commits: `f9d3904` code + tests · `d27019d` specs/docs. Still **stacked on #198** (base `feat/skeleton-loading-2-2026-09-05`); not merged, not retargeted. PR body carries "Review round 1 — what changed" (table per finding + gates). Worktree `~/agents/worktrees/pr197-fix` removed at the end of the task; main checkout untouched.

**Applied (`~/agents/reviews/pr197-review.md`):** FIND-001 `ExpiringSkeleton` reserves the cook link's 60 pt slot (44 pt `radii.control` bar in 8/8 margins; `EXPIRING_SCREEN` in new `inventory/expiringLayout.ts`, read by `expiring.tsx` too) · FIND-002 Plan `value` withheld for the whole `usageHeld` · FIND-003 the Profile tab subscribes to `useDisplayName` (extracted → `auth/useDisplayName.ts`), `useProfile`, `useLearnedShelfLife` and ORs all five colds for the one announcement · FIND-004 nav-bar "Add all" on `!held && sections.length > 0` · FIND-005 `ITEM_SCREEN.namePaddingVertical` (= `INPUT_BOX.paddingVertical`) on the name slot, `nameInput` declares `lineHeightFor(title)` + padding (44 / 64 on both sides at 1×/2×) · FIND-006 the Icon act() warnings were the FIRST mount's per file (font load after a sync test), not the fake-timer cases' — `src/test/iconFont.ts` (`Ionicons.loadFont()`) in `beforeAll` in the three suites: 7 → 0 · FIND-007 `!!id` on Item/Zones holds · FIND-008 ratchet comment (two camera-permission waits + ScanCapture's `looking` overlay) · FIND-009 rule 6 as-built Item note, §Inventory line, Item/Expiring §Scope rows, nested "As built — review round 1 of PR 3" under §Rollout PR 3, §History line; design-system.md touched-files/tests; ACCEPTANCE §Spec 58 names decisions 3/6 as things to watch for on the device pass (common-state heights → `MEMBER_ROWS = 1` + footnote bar; slots appearing at 150 ms → `minHeight` reservation) — recorded as follow-ups, not built.

**Gates (worktree):** typecheck ✓ · lint ✓ (0/0) · 14 touched + required suites 217/217, `--detectOpenHandles` clean · full `npx jest --maxWorkers=2` **196 suites / 3477 passed / 4 skipped** (pre-existing). +7 tests. Repo-wide 36 Icon act() warnings remain in OTHER suites (out of scope; the same `loadIconFont()` `beforeAll` would clear them — a follow-up).

**Open / next:** CI on `d27019d` (polled every 30 s — line appended below when it settles); Everett's device pass (ACCEPTANCE §Spec 58, the two watch-fors); follow-ups after the stack — D2's stored-id hint, the `file:line` ratchet list, `interrupt` from PR 3's screens, `keepPreviousData` on the Community feed (PR 2's D1). Merge chain waits on Everett's #193 approval; the orchestrator retargets #196 → main and merges in order (retarget dependents before `--delete-branch`).
- **CI on `d27019d` (polled every 30 s, settled 17:59:17 EDT after ~7 min): checks pass 6m21s · disclosures pass 8s-class (6s) · gitleaks pass 9s** — explicit pass lines on all three (`gh pr checks 197`; the first poll misread "no checks reported" seconds after the push as settled — re-polled with a settle condition requiring the three named checks terminal). Not merged; base is `feat/skeleton-loading-2-2026-09-05` @ 0da2206. Worktree `~/agents/worktrees/pr197-fix` removed.
- 18:30 EDT: **skeleton stack complete and green**: #193 (19712c8) → #196 (00034bb) → #198 (0da2206) → #197 (d27019d); bases contiguous; every PR MERGEABLE with checks/gitleaks/disclosures green; final tree 10 files / 14 spinner sites (all allowed exceptions); 3,477 jest. Merge chain (squash bottom-up with retargets) runs on Everett's approval of #193. Device-pass follow-ups recorded on the release pass.

## Model-roles rule (Everett, 2026-09-05)
- Fable (orchestrator) plans PRDs and specs and runs ops/merges only; **all coding is delegated to Opus 5 agents** (`Agent` with `model: "opus"`). Fable must not edit source itself. Saved as memory `feedback-model-roles`; applies to every implementation, fix and review-fix agent from here on (spec 23 / K7 implementation, gallery changes, skeleton follow-ups).

## Evening approval wave (Everett 19:40 EDT: "approved… do 0047 and whatever else migrations… i approve everything, do merges, clean all trees and branches")
- **Production:** 0047 applied 19:48 EDT (asserts 7/7), USDA seed 13,587 rows in 4 s (7/7 after), worst-case prefilter 50.9 ms. **Incident:** the first grants replay used PR #156's pre-0053 `db/neon-grants.sql` → `authenticated` regained table-level UPDATE on households + INSERT/UPDATE on grocery_items for ~3 min; caught by 0053's suite (N12 FAIL), fixed with the three-way-merged file; after: 0047 7/7 · 0053 16/16 · 0054 8/8 · 0050 12/12. Lesson saved as memory `feedback-grants-replay-from-main`. Log: `~/agents/runs/prod-apply-2026-09-05/0047/apply.log`.
- **Merged (squash, bottom-up with retargets + `rebase --onto`):** #193 spec 58 → #196 → #198 → #197 (skeleton stack), #182 spec 23, #185 spec 57, #156 spec 45 (after the apply; merge of main committed on the branch, registry/board/spec flipped to applied). Main at 03077b4, CI green. Ledger appended.
- **Closed:** #155 dependabot majors (CI red; majors go as a deliberate upgrade PR).
- **Cleanup:** 44 local branches deleted (bundle backup `~/agents/backups/pantry-local-branches-2026-09-05.bundle`), remote-tracking refs pruned, closed-PR remote branches (#58, #97) deleted; origin = main + #194 + #176 branches; no worktrees.
- **#194 (0055):** rebased onto final main; 0055 now sits in its own FIRST pending block; tripwires green; pushed; still gated on Everett's console step (production: neon_auth owned by role neon_auth, neondb_owner can drop it, tables empty except jwks/verification/project_config).
- **#176:** parked (Everett), now conflicts with main (docs/adr/README.md, specs/README.md) — rebase on his word.
- **Neon skills:** installed at Everett's request (`npx neon@latest skills -s neon -s neon-postgres -y`) → `.agents/skills/neon*`, symlinks `.claude/skills/neon*`, `skills-lock.json`, all untracked; CLI unauthenticated (profile DEFAULT, account "-") — needs `npx neon auth` on the mini or NEON_API_KEY.
- **Model roles:** Fable plans/orchestrates, Opus 5 codes (memory `feedback-model-roles`). No source edits were made by Fable tonight (conflict resolutions were markdown only).
- **Open for Everett:** rotate neondb_owner (then env file), Neon CLI sign-in, disable Neon Auth (→ 0055), Worker deploy, EAS build, device pass (spec 58 rows 1–6, spec 45 rows), Anthropic alert, Sentry, RevenueCat gate, Neon ticket, decisions (#176, Neon skill files, dependabot majors, gallery picks), go for the spec 23/57 coding waves.