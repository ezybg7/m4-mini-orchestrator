---
title: '2026-07-17'
type: note
permalink: agents/daily-log/2026-07-17
---

## Mac mini stack audit + fixes (session ending ~17:30 EDT)

Audited all 8 phases of the implementation plan against live state (not just files). Found and fixed:

1. **Critical**: `~/.hermes/config.yaml` had a YAML structural bug — `file_read_max_chars` was
   inserted mid-`mcp_servers:` block, breaking the whole file's parse. Hermes was silently running
   on total defaults (no Ollama routing, no MCP servers, no guardrails) despite the file looking
   correct. Fixed the ordering, restarted the gateway, confirmed live via `hermes mcp list` (both
   basic-memory and codegraph now enabled) and the startup log (max_turns=60 now loading from config).
2. Qwen3-8B's real serving context is 40960, not the 65536 configured — confirmed via llama.cpp
   runtime logs (`n_ctx_slot = 40960`). Ollama's Modelfile PARAMETER has no rope-scaling/YaRN knob,
   so 65536 was silently clamped. Researched the fix paths (pre-baked YaRN GGUF vs. local conversion
   vs. accept ceiling); user chose to accept 40960 as the real ceiling and updated
   `context_length: 40960` in config.yaml to match reality rather than chase 64K.
3. Repo hygiene: committed the untracked `code-graph-usage` skill + gitignored `.DS_Store`/
   `.curator_state` in ~/agents/skills; untracked `.DS_Store` in ~/agents; cleaned stray
   `queue/test.task.txt` (wrong extension, never processed by the worker's `*.task` glob).
4. `scratch-test` GitHub repo was already deleted (not a live issue) — only an orphaned local
   folder at ~/code/scratch-test remains, left alone pending explicit ask.
5. Tailscale CLI wasn't on PATH (App Store install). A symlink at /opt/homebrew/bin/tailscale
   crashed (`BundleIdentifiers.swift:47: unknown bundleIdentifier`) — the app checks argv[0]
   against a name registry and a symlink named lowercase `tailscale` fails that check. Fixed with
   a wrapper script that `exec`s the real binary by full path instead, preserving argv[0]. Confirmed
   working: `tailscale status` now shows m4-mini connected.

Open item: `sudo systemsetup -getremotelogin` still needs to be run interactively by the user to
definitively confirm Remote Login's toggle state (can't supply an interactive sudo password from
here). Strong indirect evidence it's already on (SSH port responds with an auth challenge, not
connection-refused).
## Pantry kickoff (Claude session)
- Interviewed Everett on requirements for the new "pantry" app (11 questions, 3 rounds). Key decisions: Expo/React Native + Supabase; public multi-user with shared households; capture via type-ahead + barcode + photo AI; 3-state quantities; shelf-life table with AI fallback for expirations (manual printed date always wins); recipes = deterministic matching + AI ideas, private-by-default with publish opt-in; Expo push daily expiry digest; online-required v1. PWA was explicitly rejected — Everett wants App Store now, Google Play later.
- Created private repo ezybg7/pantry, cloned to ~/code/pantry. README on main; SPEC.md + CLAUDE.md on PR #1 (feat/initial-spec) per github-workflow — **awaiting Everett's review/merge**.
- Wrote ~/agents/memory/projects/pantry.md (stack decision, schema sketch, open questions).
- Open items: Everett merges PR #1; pick App Store display name ("Pantry" is crowded); decide catalog seed source (verify USDA FoodKeeper licensing); then M0 scaffold (Expo app, Supabase project, auth/households, CI, EAS dev build).

## Pantry scaffold (Claude session, continued)
- Everett decided recipe visibility: three tiers — private (owner only, default) / household / public. SPEC.md updated on PR #1.
- Built the scaffold on feat/scaffold → **PR #2** (CI green: typecheck, lint, 19 unit tests): Expo SDK 57 + TypeScript strict + expo-router tabs; Supabase migration 0001_init.sql (full schema per SPEC, deny-by-default RLS, create_household/join_household RPCs, new-user profile trigger) + ~90-item catalog seed with per-storage-kind shelf lives; email auth + household onboarding; storage location CRUD; item CRUD (type-ahead catalog add, computed expiry, manual date override with recompute-on-move rule); category/zone grouping with urgency sort; GitHub Actions CI.
- Ran `codegraph init` in ~/code/pantry (index is local, gitignored) — Everett asked; it had not been run before.
- Notes: create-expo-app is broken with npm 12 (can't parse `npm pack --dry-run` output) — worked around by extracting the expo-template-default tarball manually. Supabase CLI is not installed on this machine; supabase/config.toml is a hand-written minimal version.
- Open items: Everett merges PRs #1 and #2; provision hosted Supabase + .env; EAS dev build; M2 smart capture next (barcode, photo AI, zones UI).

## typescript-style skill (Claude session, continued)
- Everett supplied a TypeScript-readability article (Convex blog) and asked for a personal ruleset based on it. Added `typescript-style/SKILL.md` to ~/agents/skills (live immediately via the ~/.claude/skills symlink), committed to main and pushed (d447417).
- Distilled rules: flat control flow/early returns, braces dropped on single-line ifs, const-by-default, object params at 3+ args or any bool flag, discriminated unions tagged `kind` + assertNever, leaf-level fetching with dedupe-aware query layers, inline handlers by default, PEER self-review rubric. Generalized the Convex-specific advice to any reactive query layer; explicit precedence note that existing repo conventions beat the ruleset.

## Pantry local setup (Claude session, continued)
- Everett merged PRs #1 and #2 (main = f8a1b4c + follow-ups).
- Stood up the local backend on the m4-mini: OrbStack was installed but never launched — started it headlessly (`orb start`; /var/run/docker.sock symlink still pending first GUI launch, so supabase commands need DOCKER_HOST=unix://$HOME/.orbstack/run/docker.sock until Everett opens the app once and approves the helper). `supabase start` succeeded; wrote ~/code/pantry/.env pointing at http://10.0.0.74:54321 (LAN IP, for Expo Go on the phone) with the standard local demo anon key.
- **Found and fixed a real bug during verification:** migration 0001 created tables with no grants for anon/authenticated/service_role (CLI 2.x local doesn't apply default privileges) → every REST query 42501'd; only the SECURITY DEFINER RPCs worked. Amended 0001_init.sql with the standard Supabase API-role grants (revokes for anon on the two RPCs re-applied AFTER the blanket routines grant), `supabase db reset`, re-verified end-to-end: signup+profile trigger, catalog count 91, ilike search, create_household RPC seeding Pantry/Fridge/Freezer, anon sees [] and cannot execute the RPC. Fix committed straight to main (8d935fa, single-file fix per convention — PRs were already merged). Lesson saved to session memory: supabase-cli-migration-grants.
- No Supabase cloud account exists yet — everything is local Docker; hosted project (free tier) needed before TestFlight/off-LAN use. Everett asked about this; answered in session.
- Remaining for Everett: open OrbStack once (approve privileged helper), `git pull` on main, `npm start` + Expo Go scan, run the smoke-test script; later `supabase login` + link for hosted.

## Pantry SDK downgrade (Claude session, continued)
- Expo Go on Everett's iPhone reported supported SDK 54 (client 1017756); App Store Expo Go is stuck at SDK 54 by Apple's review backlog (SDK 55 pending since ~May per expo.dev changelog), so the SDK 57 scaffold couldn't load. Options were downgrade / $99 Apple Developer + eas go / Xcode simulator — Everett chose downgrade.
- Downgraded to SDK 54 on feat/sdk-54 → **PR #3**: package.json to canonical sdk-54 template versions (expo ~54.0.35, RN 0.81.5, react 19.1.0, expo-router ~6, reanimated ~4.1.1/worklets 0.5.1, TS ~5.9, jest-expo ~54, eslint-config-expo ~10), clean reinstall + `expo install --fix`, typecheck/tests/lint all green. Everett must restart Metro with `npx expo start -c` after pulling.
- Plan of record: stay on SDK 54 while Expo Go is the dev loop; upgrade to current SDK in one hop when switching to development builds (M4 push requires dev builds + Apple Developer enrollment anyway).

## Pantry device-testing fixes (Claude session, continued)
- Everett's first on-device run surfaced: unreadable input text + "signup doesn't work". Kong/auth logs showed the phone reached the backend fine but sent only /token sign-in attempts (400 invalid credentials) and ZERO /signup requests — root cause was UX: the create-account button silently disables under 8 password chars, and dark mode turned input text white on the hardcoded white forms so nothing was readable.
- Fixes on feat/sdk-54 (78a373a, PR #3): forced userInterfaceStyle "light" until a real dark theme exists; explicit color + placeholderTextColor + white background on every TextInput; visible "needs 8+ characters" hint; 40%-opacity disabled buttons. Seeded a permanent local test login in seed.sql — **test@pantry.dev / password123**, member of "Test Home" (invite code `testhome`) with default locations; verified via password-grant curl after db reset. Typecheck/tests/lint green.
- Pattern for future mobile work: never ship hardcoded-light screens with userInterfaceStyle automatic; always set explicit TextInput colors.

## Global workflow rule added (Claude session, continued)
- Everett's standing instruction, now codified in ~/.claude/CLAUDE.md ("Spec-first, think-first"): think deeply before implementing anything; write a detailed spec file BEFORE building any new functionality (SPEC.md for projects, specs/<feature>.md for features, with flows/schema/edge cases/acceptance criteria); get sign-off when scope is new or ambiguous; keep specs updated in the same PR as behavior changes. Bug fixes/refactors exempt. Applies to all projects and all agents reading the global file — for pantry, M2+ features (barcode, photo AI, zones) each get a specs/ file first.
