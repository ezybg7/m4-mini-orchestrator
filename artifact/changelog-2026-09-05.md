## 2026-09-05 — Readiness evidence on a production copy; the suite grows to 18 flows; the paid tier and email verification specced for approval

- **Migrations rehearsed on a fresh copy of production.** `acceptance-2026-09-03` was reset from production (eight stale rehearsal branches deleted first); 0054 → 0053 (+ the grants replay) → 0047 applied in order — 8/8 · 16/16 · 8/8 again · 7/7 · 16/16 again. Production applies remain Everett's; #156 and #161 wait on them.
- **Readiness checks that pass on that copy:** RLS on 25/25 tables, zero privileges for the anonymous role, TRUNCATE only on the server role, a timeout on every Worker outbound call, only `EXPO_PUBLIC_*` in the client, a clean full-history secret scan, the privacy policy live, the RevenueCat webhook asserts 12/12 (go-live gate condition 1), and every numbered assert suite green (0046's F10 and 0045's E6 now fail by design after 0054 and 0046 — stale assertions, not defects).
- **Acceptance: 12/12, then 17/18.** The flows were re-selectored for the design wave and run green (#186); the coverage wave (#189) added six golden paths — item screen, status lifecycle, households, creator profile, the dark paywall, zones — and recorded invite links and dark mode as manual. Its run exposed a real defect: a waste-ledger insert answered 403 right after an item delete (2 of 2 runs); the fix is in flight and needs a new build to prove.
- **Worker routes tested end to end.** #184 gave barcode, ideas and import their route suites (242 cases) and caught three defects — an unrefunded import claim on a catalog failure, a product name in a log line, and `null` bodies answered 500 — fixed in #187. #188 untracked two `node_modules` symlinks #187 had let slip onto main and made the ignore pattern slash-less.
- **Spec 54 (load testing) merged (#142)** at Everett's delegation after an advocate + critic round: the load branch is built from `db/` like the acceptance branch (no production fork), identities are seeded, the load Worker is gated, JWKS registration is branch-scoped. The build itself stays deferred.
- **For Everett's approval:** #182 (spec 23, one paid tier — his six decisions written in: $4.99 · $29.99 · 14-day trial with an in-trial cap, perks trimmed to what exists, an alias-proof welcome-grant ledger, verification-gated grants) and #185 (spec 57, email verification at sign-up, strict gate). Two artifacts serve the rest: the screen-directions gallery (24 screens, 96 mocks) and the release pass (every manual check, apply, deploy and decision).
- **Still open:** #156 and #161 (applies), #176 (parked until the project is done), #155 (Dependabot majors), P8 (recipes session).
