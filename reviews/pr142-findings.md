# PR #142 (spec 54, load testing) — review queue. Advocate 2026-09-05 ~02:15 EDT (critic pending)

Verdict (advocate): approvable as a spec WITH fixes; the build stays deferred as the PR says. S = specs/load-testing.md, A = docs/adr/stack.md.

## FIND queue (advocate)
- FIND-001 · High · correctness/security — S:158-171 `TRUNCATE app_auth."user" … CASCADE` truncates every table with an FK path (ignores ON DELETE SET NULL; Postgres docs): `catalog_items.created_by → app_auth."user"` (0049:88) so the catalog and, via it, inventory_items/grocery_items/recipe_ingredients (neon-baseline.sql:1230/1270/1308) are wiped; S:169-171 "catalog survives" is false. Fix: `truncate app_auth.jwks, session, account, verification; delete from app_auth."user"; truncate public.households cascade;` in one transaction (DELETE fires cascades; households.created_by is SET NULL), then assert the five app_auth counts = 0, households = 0, catalog_items > 0 in the [manual] box.
- FIND-002 · High · process — conflicts with main on specs/README.md (mergeable UNKNOWN). Rebase.
- FIND-003 · High · env drift — S:96-104 misses main's wrangler.jsonc namespaces AUTH_LIMIT 1002, AUTH_CREDENTIAL_LIMIT 1003 (10/min per IP on sign-in/sign-up, index.ts:275-281), BILLING_SYNC_LIMIT 1004, REVENUECAT_ACCEPT_SANDBOX; D12's twin-or-record rule (A:132) unmet. Seeding 126 users via /auth/sign-up/email (S:130-133) and sign-in-per-VU (S:65) from one IP throttle at ~10/min → 429s indistinguishable from regressions. Fix: list the twins with load namespace ids; seed/sign-in pacing + per-VU token caching; note autoSignIn:false (authService.ts:288) and K7 (email verification ON) → the seed sets emailVerified by SQL on the branch.
- FIND-004 · Medium · correctness — S:67, S:320-322: #164 moved the socket token to `Sec-WebSocket-Protocol: ambry.auth.jwt, <jwt>` (realtimeAuth.ts:18-23); `?token=` is deprecated. Scenario 3's socket function must offer the subprotocol.
- FIND-005 · Medium · guard — S:188-203 condition 3 compares against gitignored `.env` (may hold an acceptance-branch URL) → a production LOAD_DATA_API_URL passes. Compare against eas.json:15-18 (committed production URLs).
- FIND-006 · Medium · env setup — S:120-129: a Neon JWKS provider added without `branch_id` applies to ALL branches (production's Data API would accept load-Worker tokens) → require branch-scoped JWKS and make S:387-389 its check; console-created branches auto-delete after 1 day by default → create with expiration off; branch creation via CLI/API exists on Free.
- FIND-007 · Low · auth model — S:320-322 points at a vault script; cite the in-repo recipe: /sign-in/email → cookie → /token (authClient.ts:386-389), Origin = auth origin (scripts/diagnose-auth-race.mjs:45,65).
- FIND-008 · Low · template — no ## Server/## UX headings (nightly-sync precedent OK); `e2e: n/a` should appear in both Test plan and Acceptance (ACCEPTANCE_TESTS.md:555).

## Decisions (advocate's table; lead defaults to be set after the critic)
1. Workers plan → request ceiling 2,000/run; count DO requests too (separate 100k/day Free quota). 2. Fork production + purge vs build the branch from db/ + catalog seed as acceptance does (ACCEPTANCE_TESTS.md:191) — advocate recommends build-from-db. 3. `GET /version` anonymous → put behind requireUser. 4. Threshold table product-derived after 4 calibration runs — keep. 5. Alert channel = issue from run 5 — keep.

## Sources verified (advocate): Artillery MPL-2.0 CLI, `ensure` thresholds, WebSocket engine headers, no-account local results / Cloud pricing, `artillery report` removed (v2.0.22); Workers Free 100k req/day account-wide; wrangler env naming + non-inheritable bindings + `crons: []`; DO SQLite-only on Free + 100k DO req/day; Neon Free limits, branch API/CLI, 1-day console default; project-wide JWKS without branch_id; TRUNCATE CASCADE semantics.

# Critic (2026-09-05 ~02:25 EDT) — additional/confirming findings
- C-FIND-001 (= FIND-001) confirmed; critic's fix: truncate, then re-seed the catalog from `scripts/hosted-seed-catalog.sql` (421 rows) and assert `count(*) = 421`.
- C-FIND-002 · High — identity minting impossible under the per-IP limiter (`authRateLimited`, index.ts:262-289: 10/min per CF-Connecting-IP, also on the per-isolate map); phase 1 needs 225 JWTs; scenario 1's 10 sign-ins/s is 60× the limit; the "exempt the load host" workaround would be a production limiter bypass. Fix: seed sessions once; per run exchange via `/auth/token`; scenarios never touch credential paths; no limiter code change.
- C-FIND-003 · High — JWKS registration is project-wide without `branch_id` → state branch scope; make "load JWT rejected by production's Data API" the branch-setup ritual.
- C-FIND-004 · Medium — an open-signup public load host on the account-wide Free quota can starve production → `LOAD_GATE` secret checked at the top of fetch only when set (env.load), or undeploy between runs.
- C-FIND-005 · Medium — guard condition 3 reads `.env`, which `scripts/run-acceptance.mjs:70-75` treats as the branch target → compare against a checked-in production-hosts list (eas.json hosts).
- C-FIND-006 · Medium — scenario 3's "receive lag p95 < 500 ms" measures the Data API membership round-trip (index.ts:483-521, 10 s timeout) + Neon autosuspend, not fan-out → record `publish.rtt` and `fanout.lag = receive − publish_response`; `delivered` counts sockets `send()` did not throw on (attachment, not receipt).
- C-FIND-007 · Medium — post-dates Everett's 22:19 decisions: free scans 8 + welcome grant + in-trial 60/20; K7 verification ON → env.load needs `AUTH_EMAIL_DEV_LOG` (else `EmailNotConfiguredError`, authService.ts:357-358) and the seed sets `emailVerified`; scenario 4 asserts against `/scan-quota`'s reported limit, not a literal 10.
- C-FIND-008 · Medium — README conflict; D12 "spec merged 2026-09-04" already false → rebase; date on merge.
- C-FIND-009 · Low — `Env.PHOTOS: R2Bucket` non-optional but absent in env.load → uncaught 500; spec should require optional → 503 `not_configured` (implementation note).
- C-FIND-010 · Low — socket auth transport: `Sec-WebSocket-Protocol: ambry.auth.jwt, <jwt>`.
- C-FIND-011 · Low — a standing branch is a permanent slot of 10; `NEON_DATABASE_URL` must be the pooler (db.ts:10); prefer reset-from-parent over re-fork; record branch id + fork date.
- C-FIND-012 · Note — `e2e: n/a — <reason>` belongs in Acceptance (ACCEPTANCE_TESTS.md:820-822) as well; seed addresses must not use `@pantry.dev` (domain not owned) → `@example.invalid`-style.

# Lead decisions (orchestrator — Everett delegated this review, 2026-09-04 22:20 "no you review it")
D1 ceiling stays 2,000/run, DO requests counted separately, plus the LOAD_GATE secret on env.load. D2 `GET /version` served only when `WORKER_BUILD` is set (env.load); production unchanged (404). D3 DECISION (default taken): the load branch is BUILT from `db/` + `scripts/hosted-seed-catalog.sql` exactly as acceptance does (ACCEPTANCE_TESTS.md:191) — no production fork, so no purge is needed; keep the purge rule in the ADR for any future fork and the 421-row assertion as the setup check. D4 identities seeded once by SQL (emailVerified = true), tokens via `/auth/token`, credential paths never load-tested. D5 quotas asserted from `/scan-quota`. D6 merge as a spec once FIND-001/002/003 + C-FIND-002/003/007 are in the text; the build stays deferred; the orchestrator merges (review delegated) and reports.
