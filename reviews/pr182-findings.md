# PR #182 — consolidated review queue (advocate + critic, 2026-09-04 ~22:20 EDT) with the lead's decisions

Every item below is DO unless marked DECISION (default taken, escalate in the PR body). Cite the spec's own lines when you edit; keep the spec's voice.

## Contracts that contradict live code
1. **join gate (both reviews, High/Critical).** `specs/monetization.md:524`, `specs/MIGRATIONS.md` 0062 row, `docs/adr/monetization.md:23,126`, lapse contract ~:80 say 0062 re-creates `join_household` (0008) which "hard-codes >= 2". Since 0045 the literal lives in `household_join_gate(h)` (`supabase/migrations/0045_invite_links.sql:69-88`), called by `join_household` and `join_household_by_invite`. Fix: 0062 re-creates `household_join_gate`; asserts cover both join paths; rename in all four docs.
2. **grant-funded rows need a column (both, High).** `ai_calls` has no funded-by mark; 0061's DDL adds none; `workers/src/routes/scanQuota.ts:92-102` (the meter) counts every pool row, so a new user who spent the 10 grant scans reads "All 8 used" while enforcement allows 8 more. Fix: `ai_calls.funded_by` (`'plan' | 'grant'`, not null default 'plan') in 0061; the claim excludes grant rows from the monthly count; BOTH meter subqueries (monthly, and the daily one after 0063 — the spec even pins "counting all kinds" at ~:539) filter the same way; extend the pinned-pool test.
3. **grant size / member cap cannot be Worker env (both, High).** `:252` says `GRANT_SCANS`/`GRANT_IDEAS` are Worker vars "read where 0061's signup trigger sets the row" — the trigger is Postgres (`0049:103-105`, `handle_new_neon_user`). Fix: one `app_config` row (0062 already hedges about one at ~:524) holds grant sizes and the member cap; "env-turnable" becomes "an UPDATE"; the Worker reads nothing for these.
4. **kill switch must cover the grant (critic, High).** `:203-210` vs `:275`: the grant branch precedes the monthly comparison, so `QUOTA_FREE_MONTHLY=0` no longer stops a free account with grant left. Fix: limit 0 refuses grant-funded claims too; say so in the incident lever paragraph.
5. **claim order (critic, Medium).** `:197-211` vs `:216,:520`: the IF/ELSIF chain puts the daily check and the `insert` after the grant branch, so a grant-funded claim skips both. Fix: the grant branch decides funding only; daily check and insert always run.
6. **photo count via R2 list (critic, Medium).** `workers/src/index.ts:459` lists with `limit: MAX_PHOTOS_PER_USER + 1`; R2 caps `limit` at 1000, so 2001 either clamps (never refuses) or throws. Fix: §Server work specifies a paginated count or a DB counter.
7. **0059 chain (advocate, Medium).** `:534` chains 0061→0063 only; reserved 0059 also re-creates `claim_ai_call`. Fix: fold the UTC cast into 0061 and mark 0059 as folded in `specs/MIGRATIONS.md` (or state the 0059→0061→0063 order explicitly — pick fold); re-point `scanQuota.test.ts`'s content pin note.

## Product / scope
8. **DECISION — unspecced Plus perks (both).** Themes, basic/rich export and "priority shelf-life learning" exist nowhere (`grep -ri csv src` → 0; no `specs/export*`/`theme*`; spec 40 already applies a learned value immediately, so "priority learning" sells nothing unless it waives the three-in-a-direction rule, which spec 40 rejects). Default taken: DROP "priority learning" entirely; move themes and rich export to a "Plus perks pending their own specs (not launch-day)" list; remove basic export from the free list AND from the lapse contract's guarantees/tests until `specs/export.md` exists (note it as the first post-launch free feature). Launch-day Plus = 300/100 AI, unlimited members in owned households, photos 2000. Escalate: Everett may prefer to spec export/themes before launch.
9. **sequencing 10→8 (advocate, Medium).** `:471` vs `:286`. Fix: `QUOTA_FREE_MONTHLY` stays 10 until the RevenueCat key is set (ADR O3 posture); 8 lands with the paywall going live; say it once in §Server work and once in the ADR.
10. **DECISION — grant farming bound (both).** Deletion is immediate and cascades; re-sign-up re-fires the trigger (≈$0.10–0.18 per loop), bounded only by `/auth/*` rate limits; K7 (email verification) is open. Default taken: a `welcome_grant_ledger` keyed by sha256(lower(email)) written when a grant is issued, retained 13 months, consulted by the signup trigger (no second grant for a known hash), disclosed in `docs/privacy.md` as an abuse-prevention record that survives account deletion; plus a daily grant-issue alert. Escalate the alternative (grant only after verified email, K7) — Everett decides.
11. **DECISION — trial cost and length (critic FIND-007, advocate D1).** Add the worst-case row: 14 d × 20/day × $0.008 ≈ $2.24 per Apple Account with zero revenue (7-day ≈ $1.12); the ":82 grant already spent" line is false for subscribe-early users. Default taken: keep the 14-day recommendation but cap in-trial AI at 60 scans / 20 ideas (the trial spends against a trial pool, not Plus's 300/100); Everett decides trial length and cap alongside the price.
12. **shelf_life gets its own daily pool (advocate, Medium).** `shelfLifeFallback.ts:27` fires per add batch; twenty unknown foods entered singly hit the 20/day cap on the welcome evening. Fix: 0063's per-kind daily pool includes `shelf_life` (≤$0.40/day worst case); walk the day-one scenario in one paragraph.
13. **PDF cost (advocate, Medium).** `:95` prices one image; `receipt.ts:39-42` accepts 10 MB PDFs billed per page; L16 undecided. Fix: a PDF cost line (page cap 5 as the default, L16 marked "decided: cap pages at 5" — it was in Everett's defaults list) and make L16 a launch-flip dependency.
14. **cost table rows (advocate, Note).** USDA matching (spec 45), push digest, invite pages: free, bounded (CATALOG_DAILY_CAP, DUE_LIMIT 500, INVITE_LIMIT) — one row each.

## Docs truth / ADR discipline
15. **stale docs (both).** `SPEC.md:252` still resolves $2.99/$24.99 with no re-open note and §Data model lacks `ai_welcome_grant`; `docs/store-listing.md:88-89` "ten a month free"; `docs/device-test-plan.md:61`; `specs/household-management.md:81,92` (cap 2, "3rd join raises member limit reached" marked verified); `specs/invite-links.md:63` "free-cap-2"; `specs/receipt-capture.md:212` (free 10, two kinds). Fix: dated one-line annotations in this PR.
16. **D4 revision (critic).** 0063 narrows D4's kind-agnostic bound; only D1 carries the revision. Fix: dated revision on D4 per `docs/adr/README.md` rule 2.
17. **client copy hardcodes numbers (critic).** `:457,463` "You've got 10 AI scans to start" / "then 8 a month" vs D4 "no quota constant in client code — ever" (restated :279). Fix: copy renders from the payload's `grant_remaining` and `limit`.
18. **privacy line (both, decision 3/4 → taken).** Pull the 13-month line from `docs/privacy.md` in this PR (Pages publishes it; the prune does not exist; the first prunable row is ~Aug 2027). Move the sentence into the spec's first-implementation-PR checklist with the §Changes effective-date rule. The plan-row wording (":288 never prunes a live plus row") must match when it returns.
19. **truth slips (both).** `:82` "grant already spent" (false for subscribe-early); privacy "plan row removed" (false for active subscribers); `ai_welcome_grant` absent from privacy.md's data list (add when the line returns — or now as a data item without the retention claim); `src/app/paywall.tsx:3` says two entrances, spec says three (fix the spec or note the code comment as stale for the impl PR); `grant_remaining` copy only when `plan === 'free'` (:174,283,461-465); spec 19 §D gate title says "(3.1.2)" while its text says the rule lives on Apple's subscriptions page — retitle; ":80 until somebody leaves" — say a 6-member lapsed household stays closed to joins until it is at the cap.
20. **comparables (advocate, Low).** `:136-154` sole source is Pantry Persona's blog (a competitor, whose own free tier is unlimited pantry + receipt scanning, contradicting "every free tier caps something entered" at ADR:60 and the PR body); "Pantry Check ~200" unsourced. Soften: "market shape from comparison write-ups; verify on the App Store before quoting".
21. **acceptance section (both).** Add `## Acceptance` (template line 48) with outcome lines: grant copy states (free with grant / free without / plus), both join paths at the cap, photo ceilings up and down, the kill switch with grant left, the prune (when it exists); name the jest modules for the pure rules (quota math → `scanQuota.test.ts` / the claim asserts; lapse deletion → `deleteAccount.test.ts`; first-run sentence → its screen test); keep `e2e: n/a` with the reason.

## Verified sources (no change needed)
Apple 3.1.1/3.1.2(c), prominence rule on the subscriptions page, one intro offer per group, price-increase consent thresholds, SBP timing, RevenueCat entitlement/offering/eligibility, Anthropic pricing + vision math, R2 list limit 1000 — all verified by both reviewers with URLs.

## Everett's decisions (typed into the release-pass page, 2026-09-04 22:19–22:20 EDT — read back from the page's saved state)
- **Price / trial: "sure go ahead"** → LOCK the recommendation: **$4.99/mo · $29.99/yr · 14-day trial**, with the in-trial cap (60 scans / 20 ideas during the trial). The $2.99/$24.99/7-day figures are retired everywhere (spec, ADR D2, SPEC.md, STATIC_PACKAGES note); the price DECISION section becomes a record, not an open question.
- **Unspecced perks: "works"** → the default stands (drop priority learning; themes + rich export deferred to their own specs; basic export out until specs/export.md).
- **Grant-farming bound: "sure"** → the hashed-email `welcome_grant_ledger` (13 months, disclosed) is decided.
- **Email verification at sign-up (K7): "sure"** → verification is ON: record it as decided in the spec's dependencies (Better Auth email verification through Resend); the grant is issued only to verified accounts AND the ledger applies. A separate auth spec amendment + implementation item follows; #182 only records the decision and the dependency.
- **Specs 55 + 56 (#176): "no, not right now, after we finish this project"** → parked; not a monetization item.
- **Spec 54 (#142): "no, you review it"** → the orchestrator runs the review pair on #142; not a monetization item.

## Cross-reference from PR #185 (spec 57 email verification, 2026-09-05 ~02:50 EDT)
- 0061 must issue the welcome grant when `emailVerified` becomes true (not at sign-up); the cutover grandfather `update` (existing verified users) runs before 0061. Apple/Google accounts arrive verified via the provider claim. Fold into spec 23 §Migrations/§Server if the round-2 critic has not already flagged it.
