# Standing context (prepended automatically to every queued task)

**You are in `~/agents`.** Read [`~/agents/CLAUDE.md`](../CLAUDE.md) (Layer 0 —
where you are) then [`~/agents/CONTEXT.md`](../CONTEXT.md) (Layer 1 — where to
go). Those two files route you; do not read the whole workspace.

- Projects live in `/Users/orchestrator/code/<name>`. Active project: **pantry** →
  `/Users/orchestrator/code/pantry` (display name **Ambry**). Read the repo's
  `CLAUDE.md` first and follow its read-before-work rule: the owning spec and its
  one row in `specs/README.md`, the owning ADR, `SPEC.md` only for a product
  decision — the parts that govern your area, never whole files. Workspace-side knowledge is
  the OKF bundle at `~/agents/memory/` — enter at `memory/index.md` and follow
  links; the Ambry hub is `memory/projects/pantry.md`.
- Stable operating rules are Layer 3 in `~/agents/references/` —
  `safety-rules.md` before any production action, `machines.md` for what runs
  where, `conventions.md` for the git/PR flow.
- If the task lacks a repo path, resolve it from `CONTEXT.md` and the vault
  before asking the user anything.
- Recurring work has a pipeline under `~/agents/pipelines/`. Follow its stage
  contracts — `## Inputs` says what to load, `## Verify` says when you are done.
  One-off work goes in `runs/<YYYY-MM-DD>-<slug>/`, never in the workspace root.
- **Autonomy: do the work, don't hand it back.** Restart services, apply
  migrations to a branch, install deps, run the app to verify. A blocked sandbox
  tool is a tooling limit, not a user question — note the exact command and
  continue. Escalate only genuine design decisions, and do it in the PR.
- When a task produces a PR, end your output with: PR: <url>
