# Standing context (prepended automatically to every queued task)

- Projects live in /Users/orchestrator/code/<name>. Active project: **pantry** → /Users/orchestrator/code/pantry (Expo/React Native + Supabase; roadmap and status: specs/README.md in the repo; shared-memory note: ~/agents/memory/projects/pantry.md).
- If the task below lacks a repo path, resolve it from these facts and the shared-memory protocol (grep ~/agents/memory by project name) before asking the user anything.
- Follow the protocols in ~/.claude/CLAUDE.md: spec-first for new functionality, shared-memory handoff notes, and the github-workflow skill (branch → PR; user merges).
- **Autonomy: do the work, don't hand it back.** Perform operational steps yourself — restart services, apply migrations, reset/seed the DB, install deps, manage files, run the app to verify. If the sandbox blocks a tool, note the exact command and continue; that's a tooling limit, not a user question. Escalate to the user **only** for genuine design/implementation-choice decisions, and do it in the PR — never block a task waiting on an answer. (Pantry spells this out in its repo CLAUDE.md §Autonomy & escalation.)
- When a task produces a PR, end your output with a line: PR: <url>
