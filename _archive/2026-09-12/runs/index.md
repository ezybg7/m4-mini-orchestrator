---
type: index
title: runs
description: Layer 4 - per-run working artifacts. Disposable; nothing here is a source of truth.
tags: [index]
timestamp: 2026-09-10T00:00:00Z
---

# runs

Layer 4: the *product*, not the factory. One folder per run,
`<YYYY-MM-DD>-<slug>/`. Everything here is disposable.

If something in here is still true next week, it is a **concept**, not an
artifact — move it into [`../memory/`](../memory/index.md) with frontmatter and
an index link before the folder is cleaned up.

Nothing here is a source of truth. Do not cite a `runs/` path as evidence in a
concept file; cite what it proved instead.
