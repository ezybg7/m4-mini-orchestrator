## add-household — 32/40  (P5 A5 R5 Fa4 Fl4 S4 C3 D2)
Two cards: Start a household (name field with "Home" / "Our place" suggestion chips, a disabled primary) and Join a household (code-or-link field, disabled Join), each with a sentence of context. Findings: the suggestion chips are a good touch; two disabled grey primaries on one screen (fine — they enable as you type); the card copy is right-sized.
Changes: none blocking.

## delete-account — 31/40  (P5 A5 R5 Fa4 Fl3 S3 C4 D1)
A red warning line, three cards (What gets deleted / What stays / Your subscription), a black "Manage subscriptions", a typed-phrase confirmation ("Type DELETE…") and a disabled "Delete my account" with a footer explaining why it is disabled. Findings: the typed confirmation is proportionate for account deletion (HIG: confirm destructive, irreversible actions); "Manage subscriptions" is a secondary action wearing the primary style (the same pattern as the recipe editor's Choose tags); the destructive button must render red when enabled — verify; the screen is long but every card earns its place.
Changes: "Manage subscriptions" as a bordered secondary; red enabled state.

## delete-account-confirm — 31/40  (P5 A5 R5 Fa4 Fl3 S3 C4 D1)
Same screen scrolled to the confirmation: "Enter DELETE above to enable this button." Findings and changes as above.

## first-run — 30/40  (P4 A5 R5 Fa4 Fl2 S4 C3 D3)
"You're all set" centred mid-screen, one glyph row ("Bring your household" with a three-line explanation of invite links and codes), a footer sentence saying the default locations are ready, a black Start adding and a Look around first text button. Reached by deep link (pantry://first-run) after the tour's sign-up. Findings: two explanations on a screen whose job is one tap; the skip path is present and secondary (Agency ✓); at the largest text size the title clips to "You're" instead of wrapping and the household row disappears entirely, so the screen loses its own headline — a fixed-height or single-line title (HIG: minimal truncation, never of the primary text); dark mode is fine.
Changes: let the title wrap at every size; one sentence for the household hint ("Invite people from the Profile tab — everyone sees the same pantry.") and drop the locations footer or fold it into the hint; keep both buttons.

## onboarding — 31/40  (P5 A5 R5 Fa4 Fl3 S3 C4 D2)
"Set up your kitchen": a one-line definition of a household, then two grouped cards — Start a household (explanation, a Name field prefilled "Home", Home / Our place suggestion chips, black Create household) and Join a household (explanation, an Invite code or link field, a grey Join that enables when filled) — and a Sign out text link. Reached by pantry://onboarding. Findings: three explanatory paragraphs before the first field, on a screen whose two group titles already say what each does; two black primaries on one screen is at the HIG's limit but acceptable here because they are alternatives in separate groups and only one is enabled at a time; the disabled Join reads as disabled (grey fill) ✓; the suggestion chips are a good touch; at the largest text size the title takes three lines and the first card's button is below the fold (the page scrolls, so reachable, but the person sees no control on first paint). Dark mode is fine.
Changes: cut each explanation to one line (the page intro can go entirely); at accessibility sizes shorten the title to "Your kitchen" or drop the intro so a field is visible on first paint; keep both cards.

## paywall — 29/40  (P4 A4 R5 Fa4 Fl3 S3 C3 D3)
"Ambry Plus" reached by pantry://paywall with billing off: an intro ("Everything you do by hand is free, forever. Plus is for the parts that cost us money to run — the AI that reads your receipts."), three green check bullets, Monthly / Yearly radio rows (Yearly selected, "Save 30%" pill), the auto-renew disclosure paragraph, then a grey "Ambry Plus is coming soon. Nothing to buy yet — your free scans reset on the 1st of each month, at 00:00 UTC." and Terms · Privacy links. Findings: a plan picker with nothing to buy — the most important line on the screen (coming soon) is the smallest and last; "cost us money" is first-person plural, which the HIG's writing guidance rules out; "00:00 UTC" is developer time on a consumer screen; the selected plan is fill plus a filled radio (not color alone ✓); the disclosure paragraph is required by App Review and reads correctly; the honesty about what stays free is the screen's strength (Responsibility 5).
Changes: while billing is off, render a single "coming soon" state — the three benefits, one line about the monthly free scans in local time, and one control (Done / Notify me) — and no plan picker; rewrite the intro without "us" ("Plus pays for the parts that cost money to run"); when billing goes live, a segmented Monthly / Yearly choice and one primary Continue that carries the price.
