# Findings 2026-09-11

Window: since the previous nightly (`reflect-2026-09-10`, done 2026-09-10 03:06).
The day's interactive work is already in `memory/daily-log/2026-09-11.md`; these
are the log-evidenced findings that bear on skills/process.

## Failures

- **CI-triage executed untrusted branch code under operator credentials (RCE).**
  Evidence: `logs/codex-review-2026-09-11T002928.last.txt` — critical finding,
  conf 0.98: `.github/ci-triage-prompt.md` Step 4 (lines 161–181) checks out
  `head_sha` and runs `npm run typecheck|lint|test`, all branch-controlled, inside
  `claude-worker.sh` which exports `CLAUDE_CODE_OAUTH_TOKEN` and can read
  `~/agents/.env.acceptance`. Acted on same session: trigger disabled on `main`
  (`if: false`, commit `1f51436`, per daily-log 2026-09-11). Failure, contained.

- **Lane scaffolding (#205) merged without its governing spec 56 / ADR D15.**
  Evidence: same artifact, medium finding conf 1.0 — `specs/codex-qa-lane.md`
  absent from the tree, no row in `specs/README.md`; `pantry-codex-lane/SKILL.md`
  lines 7–8 cite a contract that does not exist on `main`. Self-identified
  spec-first violation. Failure (process); rule already in CLAUDE.md, occurrence
  is new.

## Near-misses

- **The Codex execpolicy is bypassable; "probe.sh 14/14 passed" was narrower than
  it sounded.** Evidence: `logs/codex-review-2026-09-11T002928.last.txt` — high
  finding, conf 1.0: literal `git push` returns `forbidden`, but `git -C . push`,
  `/usr/bin/git push`, and `npx wrangler deploy` all return `{"matchedRules":[]}`.
  The 09-11 probe run exercised only the literal form. Lesson: a permission/exec
  gate proves only the exact argv forms you ran under it — the general class of
  the 2026-09-10 "running tells you what is true" finding. **Not in
  `autonomous-ai-agents/codex/SKILL.md`, which documents `--yolo` /
  `danger-full-access` but not this bypass** → candidate for stage 2.

- **Triage repro commands do not reproduce four of five watched workflows.**
  Evidence: same artifact, medium finding conf 0.99 — Secret scan / Disclosures /
  nightly bundle / branch-sync are not tsc/jest jobs, so Step 4's fixed command
  set can report a real failure as "not reproduced." Moot while the trigger is
  disabled, but re-arms with it.

## Repeated procedure

- **Cold-validating instruction-surface edits through the queue worker.**
  Evidence: `queue/done/probe-instruction-surface-2026-09-11.task` and
  `probe-control-old-state-2026-09-11.task`, processed per `worker-runner.log`
  at 00:59–01:01; results in `logs/claude-probe-*-2026-09-11-*.json`. Method
  (subagents carry the session-start CLAUDE.md snapshot, so validate via a queued
  `.task`) is recorded in memory `feedback-validate-instructions-cold`. Already a
  memory concept, not a SKILL.md — noted, not a skill edit.

## Infra (quiet-healthy; state, not an action)

- **Gateway quiet 3rd straight night.** Evidence: `~/.hermes/logs/errors.log`
  0 entries after its last line `2026-09-08 21:24:57` (basic-memory MCP parking);
  `gateway.log` last activity `2026-09-08 20:39:19`.
- **Curator carry-over still open.** Evidence: newest `~/.hermes/logs/curator/`
  dir is `20260904-191828` (Sep 4 19:18); weekly cadence fires in the afternoon,
  so at 03:00 the ~09-11 run flagged by the 09-10 log has not yet fired. Expected,
  not a fault — watch later today.
